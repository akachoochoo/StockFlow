# Phase 0.9.2 strategy config — D-2 (MovingAverageReentry) variant.
#
# Phase 0.9.2 baseline (`strategies-0.9.2.yaml`) 와 변수 1 차원만 다름 —
# reentry 정책 (hybrid cooldown=60 → moving_average window=20 sma).
# 종목 / drop_threshold / profit_target / max_split_count / per_split_amount
# / max_split_per_day / max_sells_per_day / allocation_policy 모두 동일
# (ADR 0002 §1.3 controlled-experiment 원칙).
#
# D-2 spec 출처: `config/strategies-D.yaml` + ADR 0002 §4.1.1 (D 폐기 →
# D-2 = N-day SMA anchor with same drop_pct threshold).
#
# 게이트 (ADR 0005 §1.2.3): Phase 0.9.2 baseline (F = hybrid cooldown=60)
# 과 동일 baseline strict 적용 — 본 D-2 variant 는 비교 분석용 (Phase 0.7
# / 0.5 에서 F 가 D-2 대비 우위였으나 Phase 0.9.2 5 종목 환경에서 결과
# 비교 — 분석 phase 사용자 자료).

version: "0.5"
allocation_policy: EQUAL
assets:
  "005930":
    name: "삼성전자"
    enabled: true
    buy_strategy: "price_drop"
    buy_parameters:
      drop_threshold_pct: 5.0
      max_split_count: 7
      per_split_amount: 5000000
      max_split_per_day: 1
    sell_strategy: "profit_target"
    sell_parameters:
      profit_target_pct: 10.0
      max_sells_per_day: 7
    reentry_strategy: "moving_average"
    reentry_parameters:
      window: 20
      ma_type: "sma"
  "005380":
    name: "현대차"
    enabled: true
    buy_strategy: "price_drop"
    buy_parameters:
      drop_threshold_pct: 5.0
      max_split_count: 7
      per_split_amount: 5000000
      max_split_per_day: 1
    sell_strategy: "profit_target"
    sell_parameters:
      profit_target_pct: 10.0
      max_sells_per_day: 7
    reentry_strategy: "moving_average"
    reentry_parameters:
      window: 20
      ma_type: "sma"
  "055550":
    name: "신한지주"
    enabled: true
    buy_strategy: "price_drop"
    buy_parameters:
      drop_threshold_pct: 5.0
      max_split_count: 7
      per_split_amount: 5000000
      max_split_per_day: 1
    sell_strategy: "profit_target"
    sell_parameters:
      profit_target_pct: 10.0
      max_sells_per_day: 7
    reentry_strategy: "moving_average"
    reentry_parameters:
      window: 20
      ma_type: "sma"
  "097950":
    name: "CJ제일제당"
    enabled: true
    buy_strategy: "price_drop"
    buy_parameters:
      drop_threshold_pct: 5.0
      max_split_count: 7
      per_split_amount: 5000000
      max_split_per_day: 1
    sell_strategy: "profit_target"
    sell_parameters:
      profit_target_pct: 10.0
      max_sells_per_day: 7
    reentry_strategy: "moving_average"
    reentry_parameters:
      window: 20
      ma_type: "sma"
  "015760":
    name: "한국전력"
    enabled: true
    buy_strategy: "price_drop"
    buy_parameters:
      drop_threshold_pct: 5.0
      max_split_count: 7
      per_split_amount: 5000000
      max_split_per_day: 1
    sell_strategy: "profit_target"
    sell_parameters:
      profit_target_pct: 10.0
      max_sells_per_day: 7
    reentry_strategy: "moving_average"
    reentry_parameters:
      window: 20
      ma_type: "sma"
