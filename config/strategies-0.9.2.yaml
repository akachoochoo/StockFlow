# Phase 0.9.2 strategy config — 분산 효과 검증 (5 종, 업종 다양화).
# ADR 0005 §8 (라운드 #14 — Phase 0.9.2 진입 결정) 박제 후속.
#
# 종목: Phase 0.9.1 (005930 + 005380) + 3 종 (055550 + 097950 + 015760).
# 변수 (vs 0.9.1): 종목 수 (2 → 5) + 업종 분산 (반도체/자동차 → 반도체
# /자동차/금융/소비재/에너지). 다른 모든 변수 동일 — 변수 통제 strict.
#
# 정책 동일성 강제 (Phase 0.9.1 = Phase 0.7.3 동일) — drop=5.0% /
# max_split=7 / per_split=5,000,000 KRW / target=10.0% / cooldown=60.
#
# allocation_policy=EQUAL (Phase 0.9.1 동일) → per_split_amount yaml 그대로.
#
# 게이트 (ADR 0005 §1.2.3 박제, Phase 0.7.3 baseline strict, 통과 ≥ 2/3):
#   H1 (capital_turnover): ≥ 0.3270
#   H2 (total_return_pct): ≥ 13.23%
#   H3 (sharpe_ratio):     ≥ 0.5255
#
# 가설 (ADR 0005 §6.6.3 / §8.2.2): 업종 다양화 → 분산 효과 회복 →
# 시나리오 C → A/B 변경 가능. 다만 모두 KOSPI 대형주 → 자산군 분산
# (Phase 0.7.3 골드 효과) 만큼 강하지 않을 가능성.

version: "0.5"
allocation_policy: EQUAL
assets:
  "005930":
    name: "삼성전자"
    enabled: true
    buy_strategy: "price_drop"
    buy_parameters:
      drop_threshold_pct: 5.0
      max_split_count: 7
      per_split_amount: 5000000
      max_split_per_day: 1
    sell_strategy: "profit_target"
    sell_parameters:
      profit_target_pct: 10.0
      max_sells_per_day: 7
    reentry_strategy: "hybrid"
    reentry_parameters:
      cooldown_days: 60
  "005380":
    name: "현대차"
    enabled: true
    buy_strategy: "price_drop"
    buy_parameters:
      drop_threshold_pct: 5.0
      max_split_count: 7
      per_split_amount: 5000000
      max_split_per_day: 1
    sell_strategy: "profit_target"
    sell_parameters:
      profit_target_pct: 10.0
      max_sells_per_day: 7
    reentry_strategy: "hybrid"
    reentry_parameters:
      cooldown_days: 60
  "055550":
    name: "신한지주"
    enabled: true
    buy_strategy: "price_drop"
    buy_parameters:
      drop_threshold_pct: 5.0
      max_split_count: 7
      per_split_amount: 5000000
      max_split_per_day: 1
    sell_strategy: "profit_target"
    sell_parameters:
      profit_target_pct: 10.0
      max_sells_per_day: 7
    reentry_strategy: "hybrid"
    reentry_parameters:
      cooldown_days: 60
  "097950":
    name: "CJ제일제당"
    enabled: true
    buy_strategy: "price_drop"
    buy_parameters:
      drop_threshold_pct: 5.0
      max_split_count: 7
      per_split_amount: 5000000
      max_split_per_day: 1
    sell_strategy: "profit_target"
    sell_parameters:
      profit_target_pct: 10.0
      max_sells_per_day: 7
    reentry_strategy: "hybrid"
    reentry_parameters:
      cooldown_days: 60
  "015760":
    name: "한국전력"
    enabled: true
    buy_strategy: "price_drop"
    buy_parameters:
      drop_threshold_pct: 5.0
      max_split_count: 7
      per_split_amount: 5000000
      max_split_per_day: 1
    sell_strategy: "profit_target"
    sell_parameters:
      profit_target_pct: 10.0
      max_sells_per_day: 7
    reentry_strategy: "hybrid"
    reentry_parameters:
      cooldown_days: 60
