# ADR 0004: Phase 0.8 Design Decisions — 지지선 기반 세븐스플릿 (매수 패러다임 차원)

> 누적 기록 문서. 새 결정은 아래에 섹션으로 추가.
> 작성 시작: 2026-05-05
> 마지막 업데이트: 2026-05-05 (§2 Phase 0.8.b 결정 박제 — 7 슬롯 매핑 (이탈, Phase 0.8.1 = 슬롯 1~5 / Phase 0.8.2 = 슬롯 6~7) + 우상향 전제 + 슬롯 즉시 활성 + indicators 범위 + hardcoded windows + 백테스트 기간)
> Phase 0.7.3 회고 + ADR 0003 §19 박제 후속.
> 선행 ADR: `docs/decisions/0001-phase-0-decisions.md` (Phase 0),
> `docs/decisions/0002-phase-0.5-decisions.md` (Phase 0.5),
> `docs/decisions/0003-phase-0.7-decisions.md` (Phase 0.7 시리즈).
> 회고: `docs/retrospectives/phase-0.7.3.md`.

---

## 1. Phase 0.8 진입 결정 라운드 #10 박제 (2026-05-05)

> ADR 0003 §15.2 (Phase 0.8 후보 박제) + §19 (Phase 0.7 시리즈 종료)
> 후속. Phase 0.7.3 회고 §19.5 부속 결정 (사용자 검토 후 진입) 의 사용자
> 명시 결정 라운드 박제. **Phase 0.8 정식 시작**.

### 1.0 박제 시점 + 위상

박제 일자: 2026-05-05. ADR 0003 §19 (라운드 #9 — Phase 0.7 시리즈 종료)
박제 후 사용자 검토 시간 후 진입 결정 라운드 #10 박제.

위상:
- 본 §1 박제로 Phase 0.8 정식 시작
- ADR 0004 신규 (ADR 0003 = Phase 0.7 시리즈 종료, 0004 = Phase 0.8
  시작) — Phase 별 ADR 분리 패턴 정합
- 0.8.a sub-step (결정 ADR 박제) = 본 commit

### 1.1 사용자 명시 결정 verbatim (2026-05-05)

> "Phase 0.7.3 게이트 3/3 통과 확인. Phase 0.8 진입 가능.
>
> 진입 결정 라운드 (라운드 #9 또는 #10):
> 1. Phase 0.7 종료 결정 — §15.5.1.C 진입 경로 정합 (이미 §19 박제)
> 2. Phase 0.8 진입 사전 결정 (라운드에서 박제):
>   2.1 학습 목표 명확화 — '패러다임 비교' (PriceDropStrategy vs
>       SupportLevelStrategy). 박영옥 원전 정신 폐기 아님 명시
>   2.2 슬롯 모델 호환성 — 추천 옵션 B-1 (SupportSlot 신설, 한 종목 한
>       전략)
>   2.3 보조 지표 처리 — 추천 옵션 3 (src/domain/indicators/ helper
>       모듈)
>   2.4 매도 정책 — 추천 옵션 c → 옵션 a sub-step 분리 (Phase 0.8.1
>       profit_target=10% 유지, 0.8.2 +3~5% 단기 매매)
>   2.5 가설/게이트 — H1/H2/H3 ≥ Phase 0.7.3 strict, 통과 ≥ 2/3
>   2.6 비교 baseline — Phase 0.7.3 (069500 + 132030, EQUAL,
>       PriceDropStrategy)
>   2.7 명시적 제외 — Phase 0.7.4 / §14.7 γ / 멀티 종목 + SupportLevel
>       조합
> 3. CLAUDE.md §16 갱신 — Phase 0.9 호환성 룰
> 4. ADR 신규 0004"

### 1.2 학습 목표 명확화 (사용자 §2.1 박제)

#### 1.2.1 패러다임 비교 (가치 → 기술적)

Phase 0.8 의 핵심 = **매수 패러다임 차원 변경**:

| 차원 | Phase 0 / 0.5 / 0.7 | Phase 0.8 |
|---|---|---|
| 매수 트리거 | `PriceDropStrategy` — `avg_price` 대비 N% 하락 (가치) | `SupportLevelStrategy` — 보조 지표 기반 지지선 도달 (기술적) |
| 의사결정 변수 | 평균 단가 + 현재가 + 임계값 | 가격 + MA / BB / RSI 등 보조 지표 |
| 슬롯 의미 | 차수 번호 (1~7, 누적) | 지지선 종류 (1~7, 매핑) |
| 박영옥 원전 정신 | 가치 투자 (분할 매수) | 기술적 분석 (지지선) |

#### 1.2.2 박영옥 원전 정신 폐기 아님

`PriceDropStrategy` (Phase 0~0.7) 는 박영옥 세븐스플릿의 핵심 정신
(분할 매수 + 평단 관리). **Phase 0.8 = 폐기 아닌 비교** — 두 패러다임
모두 유지:

- yaml `buy_strategy: "price_drop"` → 기존 strategy
- yaml `buy_strategy: "support_level"` → Phase 0.8 신규 strategy
- 둘 다 회귀 invariant 보존 (`PriceDropStrategy` 변경 zero)

→ Phase 0.7.3 baseline (PriceDropStrategy) 와 Phase 0.8 (SupportLevelStrategy)
는 같은 환경 (자산 + 자본 배분 + 매도 + 재진입) 에서 매수 패러다임만
변경한 직접 비교.

### 1.3 결정 1 = (B-1) — SupportSlot 신설 (한 종목 한 전략)

| 옵션 | 채택 | 근거 |
|---|---|---|
| A) `slot_number` 의미 다양화 (기존 `SplitSlot` 재사용) | 거부 | 의미 혼란 — 차수 번호 vs 지지선 종류 동시 |
| **B-1) `SupportSlot` 신설 (한 종목 한 전략 가정)** | **채택** | 의미 명확. yaml 의 `buy_strategy` 가 자산별 결정 → 자산은 한 가지 슬롯 모델만 사용 |
| B-2) `SupportSlot` 신설 + 자산이 두 슬롯 모델 동시 보유 | 거부 | 복잡성 ↑ — Phase 0.8 본질 (패러다임 비교) 와 다른 차원 |
| C) 슬롯 없는 전략 (단일 슬롯) | 거부 | 7-split 정신 (분할 매수 / 매도) 약화 |

채택 효과 — 도메인 모델:
- `SupportSlot` 신규 — `slot_number` (1~7) = 지지선 종류 매핑 (§1.6.1)
- `Position` 의 `slots` 필드 = `list[SplitSlot] | list[SupportSlot]`
  union (자산이 한 가지 슬롯 모델만 보유)
- `AssetContext` 의 strategy 가 슬롯 모델 결정 — 동일 자산 yaml 변경
  시 명시적 마이그레이션 필요 (Phase 0.8 동안은 zero)

### 1.4 결정 2 = (옵션 3) — `src/domain/indicators/` helper 모듈

| 옵션 | 채택 | 근거 |
|---|---|---|
| 1) `IndicatorPort` Protocol 신설 | 거부 | YAGNI — 도메인이 indicator 계산을 외부 의존으로 분리할 동기 약함 (Phase 0.7.2 §16.5.1 σ 산출 위치 결정 정합) |
| 2) Strategy 내부 직접 계산 | 거부 | strategy 비대화. 단위 테스트 분리 부족 |
| **3) `src/domain/indicators/` helper 모듈** | **채택** | 도메인 내부 응집 (CLAUDE.md §1.1 정합) + Strategy 분리 + 단위 테스트 단순. D-2 `MovingAverageReentry` 패턴 정합 (`src/domain/strategies/reentry.py` 의 SMA 산출) — 그러나 별도 모듈로 응집 |

채택 효과 — 신규 구조:
- `src/domain/indicators/__init__.py` — public API (calculate_sma /
  calculate_bb / calculate_rsi 등)
- `src/domain/indicators/moving_average.py` — SMA / EMA
- `src/domain/indicators/bollinger.py` — BB
- `src/domain/indicators/rsi.py` — RSI
- 모두 순수 함수 — `list[Decimal]` 입력, `Decimal` 또는 `tuple` 출력
- 도메인 외부 의존 zero (CLAUDE.md §1.1 — numpy / pandas 금지 정합)

Phase 0.8.b sub-step 책임.

### 1.5 결정 3 = (옵션 c → a sub-step 분리) — 매도 정책

| 옵션 | 채택 | 근거 |
|---|---|---|
| a) Phase 0.8 진입 즉시 +3~5% 단기 매매 | 거부 (즉시 적용) | 매도 정책 + 매수 패러다임 동시 변경 — 변수 통제 위반 |
| b) 절충 +5~7% | 거부 | 중간 — 의미 모호 |
| **c) sub-step 분리 (옵션 c → a)** | **채택** | 변수 통제 정신 정합. Phase 0.8.1 = 매수 패러다임 1 차원만 / Phase 0.8.2 = 매도 정책 추가 |

채택 효과 — Phase 0.8 sub-step 분할:

| sub-step | 매수 전략 | 매도 정책 | 검증 차원 |
|---|---|---|---|
| **Phase 0.8.1** | SupportLevelStrategy | profit_target=10% 그대로 | 매수 패러다임 |
| Phase 0.8.2 (선택) | SupportLevelStrategy | profit_target=3~5% | 단기 매매 추가 |

Phase 0.8.2 진입 결정 = Phase 0.8.1 종료 후 별도 라운드 (§1.10).

### 1.6 결정 4 = 가설 / 게이트 (사용자 §2.5 박제, strict baseline)

#### 1.6.1 가설

| 가설 | 의미 |
|---|---|
| H1 자본 회전 | SupportLevelStrategy 가 PriceDropStrategy 대비 **자본 회전 동등 이상** — 지지선 발화 빈도가 가치 임계만큼 활발 |
| H2 return | SupportLevelStrategy 가 동등 이상 return — 기술적 분석의 가치 입증 |
| H3 Sharpe | SupportLevelStrategy 가 동등 이상 Sharpe — 기술적 분석의 위험 조정 수익 가치 |

#### 1.6.2 게이트 임계 (strict — Phase 0.7.3 baseline 정확값)

| 가설 | 임계 | Phase 0.7.3 baseline (§18.14.2) |
|---|---|---|
| **H1** Capital turnover ≥ 0.3270 | strict | 0.3270 |
| **H2** Total return % ≥ 13.2280 | strict | 13.2280 |
| **H3** Sharpe ratio ≥ 0.5255 | strict | 0.5255 |
| **통과 기준** | ≥ 2 / 3 참 | (Phase 0.7 시리즈 그대로) |

부속 결정 (default):
- 1.5× H1 / VOL baseline 등 변형 옵션 거부 (사용자 명시 strict 채택)
- 게이트 미달 시 라운드 #11 (가칭) 진입

#### 1.6.3 §17.3 H3 정밀도 학습 적용

ADR 0003 §17.3 H3 임계 정밀도 명시화 학습 그대로 — Phase 0.7.3 baseline
정확값 (0.5255) strict 비교. 반올림 박제 회피.

### 1.7 결정 5 = 비교 baseline (사용자 §2.6 박제)

| 항목 | Phase 0.7.3 baseline (변경 없음) | Phase 0.8.1 (검증 대상) |
|---|---|---|
| 종목 | 069500 + 132030 | **동일** |
| 자본 배분 | EQUAL | **동일** |
| 매도 정책 | profit_target=10% / max_sells=7 | **동일** |
| 재진입 정책 | F (HybridTimeBasedReentry, cooldown=60) | **동일** |
| 자본 / 데이터 / 기간 | 100M / 2020-2024 / KRX_*_2019-2024.csv | **동일** |
| **매수 전략** | PriceDropStrategy (drop=5%, max_split=7) | **SupportLevelStrategy (Phase 0.8 신규)** |

**변수 1 차원 통제** — 매수 전략만 변경. 다른 모든 항목 Phase 0.7.3
baseline 그대로 → 직접 비교 가능.

### 1.8 결정 6 = 명시적 제외 (사용자 §2.7 박제)

Phase 0.8 (특히 0.8.1) 에서 작성하지 않을 코드:

- ❌ Phase 0.7.4 (가칭, 부동산 / 인프라 분산) — ADR 0003 §18.12.4 /
  §19.3 placeholder 보존
- ❌ §14.7 γ (자산별 다른 정책) — ADR 0003 §19.4 placeholder 보존
- ❌ 멀티 종목 + SupportLevel 조합 — Phase 0.8.1 후속 검토 (변수 통제
  정신상 동시 변경 금지)
- ❌ KIS API / 손절 / 텔레그램 — Phase 1+ (CLAUDE.md §16 그대로)

### 1.9 Phase 0.8 sub-step 분할

사용자 박제 (§2.7 작업 순서 d) 그대로:

| sub-step | 책임 | 종료 조건 |
|---|---|---|
| **0.8.a** (본 박제) | ADR 0004 §1 박제 (라운드 #10) | 본 commit |
| 0.8.a.1 | CLAUDE.md §16 갱신 (Phase 0.7 호환성 → Phase 0.9 호환성) + roadmap.md | 별도 commit (옵션 5=B 패턴 정합) |
| 0.8.b | `src/domain/indicators/` helper 모듈 + 단위 테스트 | 모듈 commit |
| 0.8.c | `SupportSlot` 모델 신설 + 단위 테스트 | 모델 commit |
| 0.8.d | `SupportLevelStrategy` 도메인 구현 + 단위 테스트 | strategy commit |
| 0.8.e | BacktestRunner 통합 + 회귀 (`PriceDropStrategy` 회귀 invariant) | 통합 commit + regression |
| 0.8.f | strategies-0.8.1.yaml 박제 + 백테스트 실행 | yaml + 백테스트 commit |
| 0.8.g | 결과 분석 + `phase-0.8.1-results.md` 박제 | results commit |
| 0.8.h | 게이트 판정 + ADR 0004 §X (게이트 결과) 박제 + narrative 회고 | 회고 commit |
| 0.8.i (선택) | Phase 0.8.2 진입 결정 라운드 (단기 매매) | 사용자 명시 결정 후 |

### 1.10 미박제 / 보류 항목 (Phase 0.8 진행 중 또는 후속)

- 지지선 7 슬롯 매핑 — 0.8.b/0.8.c sub-step 박제 시 구체화
- `IndicatorPort` 도입 시점 — 본 §1.4 옵션 3 채택. Phase 0.9 / 1+ 에서
  외부 데이터 소스 통합 시 재검토 가능
- Phase 0.8.2 진입 (단기 매매) — Phase 0.8.1 종료 후 별도 라운드
  (§1.5 / §1.9)
- Phase 0.7.4 (부동산 분산) / §14.7 γ — Phase 0.8 종료 후 검토 (§1.8)
- 지지선 strategy 가 트리거 안 되는 자산 (e.g., 골드의 MA 패턴 분석
  필요) — 0.8.f 결과 분석 시 명시 검토
- 멀티 종목 + SupportLevel 조합 — Phase 0.8.1 후속 (§1.8) 또는 Phase
  0.9+ 결정 라운드

---

## 2. Phase 0.8.b 결정 라운드 박제 (2026-05-05) — 7 슬롯 매핑 + indicators 범위 + windows

> §1.10 미박제 항목 ("지지선 7 슬롯 매핑 — 0.8.b/0.8.c sub-step 박제 시
> 구체화") 의 구체화 박제. 0.8.b sub-step (indicators helper 모듈)
> 코드 작성 직전, 슬롯 매핑 / indicators 범위 / windows / 백테스트
> 기간 결정을 사용자 명시로 박제.
>
> 박제 일자: 2026-05-05. ADR 0004 §1 (라운드 #10) 박제 직후 동일 일자.

### 2.0 박제 시점 + 위상

박제 직전 사용자 명시 입력 (3 결정 + 2 명확화):
- 결정 1: 7 슬롯 매핑 (이탈 채택, 단순 strict less than)
- 결정 2: indicators 모듈 범위 = (iii) Phase 0.8.1 = 슬롯 1~5 / Phase
  0.8.2 = 슬롯 6~7
- 결정 3: MA window default = (γ) hardcoded (Phase 0.8.1 단순화)
- 명확화 1: 슬롯 1 "우상향 확인" = **우상향 전제** (종목 선정 단계 가정,
  코드 필터 아님)
- 명확화 2: 진입 시 모든 종목은 충분한 이전 OHLCV 보유 → 모든 슬롯
  즉시 활성 + 백테스트 기간 lookback warmup 고려

위상:
- 본 §2 박제 후 0.8.b 코드 작성 (`src/domain/indicators/` helper 모듈
  + 단위 테스트) 진입
- ADR 0004 §1.9 sub-step 표 (0.8.b) 의 구체 결정 박제

### 2.1 사용자 명시 결정 verbatim (2026-05-05)

> "1. 우상향 확인은 다시 말해 우상향 전재야, 즉 타겟이 되는 종목은
>    우상향 할 것으로 분석되었다는 전재가 붙어, 이부분 박제 해줘
> 2. 진입시 모든 종목은 이전 OHLCV가 모두 존재하여, 모든 slot 들이
>    바로 활성화 될 수 있음을 명시해줘. 백테스트시 이점을 고려하여
>    기간을 설정해줘"

선행 명시 (3 결정 패키지):

> "결정 1: 7 슬롯 맵핑 (이탈 채택)
>
> 슬롯 1: first-buy 전용 / 트리거 split_level == 0 / indicator 없음
> 슬롯 2: MA5 이탈 / current_price < MA5
> 슬롯 3: MA10 이탈 / current_price < MA10
> 슬롯 4: MA20 이탈 / current_price < MA20 (생명선)
> 슬롯 5: 직전 전고점 / current_price < recent_high(60) (단순화)
>
> [Phase 0.8.2 추가]
> 슬롯 6: MA60 이탈 / current_price < MA60
> 슬롯 7: BB 하단 또는 RSI 30 이하 / OR 조건
>
> '이탈' 정의: strict less than. equal 케이스 트리거 안 함.
> tolerance/buffer 미적용 (Phase 0.8.1 단순함 우선).
>
> 결정 2: indicators 모듈 범위 — (iii) 채택
> 결정 3: MA window default — (γ) hardcoded 채택"

### 2.2 결정 1 = 7 슬롯 매핑 (이탈 채택)

| 옵션 | 채택 | 근거 |
|---|---|---|
| (a) ADR 0003 §15.2.2 그대로 (MA20 / BB-2σ / MA60 / RSI 30 / MA200 / 직전 저점 / 확장 예약) | 거부 | 박영옥 원전 "이탈" 정신과 배치 — RSI/BB 는 oscillator 성격, 직접 매수 트리거 의미 모호 |
| **(b) 사용자 명시 매핑 (이탈 채택)** | **채택** | 단기 → 중기 → 장기 + 가격 패턴 일관 (MA5/10/20 → recent_high → MA60 → BB/RSI). "이탈" 정신 명확 |
| (c) Phase 0.8.1 = 부분 슬롯만 + 6~7 후속 | 거부 (별도 차원) | 결정 2 (옵션 iii) 에서 sub-step 분리로 독립 결정 |

#### 2.2.1 슬롯 정의 (Phase 0.8.1 = 슬롯 1~5)

| 슬롯 | 이름 | 트리거 | indicator | 의미 |
|---|---|---|---|---|
| 1 | first-buy 전용 | `split_level == 0` | (없음) | 전체 비중의 중심. 우상향 전제 (§2.2.3) 자산 진입점 |
| 2 | MA5 이탈 | `current < MA5` | `moving_average(5)` | 단기 (주) 과열 해소 |
| 3 | MA10 이탈 | `current < MA10` | `moving_average(10)` | 단기 (2주) 눌림목 |
| 4 | MA20 이탈 | `current < MA20` | `moving_average(20)` | 심리적 저지선 (생명선) |
| 5 | 직전 전고점 (단순) | `current < recent_high(60)` | `recent_high(60)` | 직전 60 거래일 최고가 미만. 박영옥 "지지로 전환" 단순화 (Phase 0.8.x 정교화 가능) |

#### 2.2.2 슬롯 정의 (Phase 0.8.2 추가 = 슬롯 6~7)

| 슬롯 | 이름 | 트리거 | indicator | 의미 |
|---|---|---|---|---|
| 6 | MA60 이탈 | `current < MA60` | `moving_average(60)` | 중기 (분기) 추세 약화 |
| 7 | BB 하단 또는 RSI 30 이하 | `current < BB_lower OR RSI < 30` | `bollinger(20, 2.0)` + `rsi(14)` | OR 조건. 변동성/모멘텀 과매도 |

Phase 0.8.2 결정 라운드 시 슬롯 6~7 활성화. 본 §2 = Phase 0.8.1 (1~5)
박제만.

#### 2.2.3 "이탈" 정의 + 우상향 전제

**"이탈" 정의 (Phase 0.8.1 단순화)**:
- `current < trigger` (strict less than)
- equal 케이스 (`current == trigger`) 트리거 안 함
- tolerance / buffer 미적용

**Phase 0.8.x 후속 정교화 가능** (본 박제 이후 별도 결정 라운드):
- 옵션 C: 하향 돌파 (어제 위 → 오늘 아래)
- 옵션 D: 슬롯별 차등 tolerance (예: MA5 -1%, MA20 -3%)
- 슬롯 5 "돌파 후 되돌림" 패턴 인식

**우상향 전제 (사용자 명시 §2.1 박제)**:
- 슬롯 1 first-buy 의 코드 필터 = `split_level == 0` 만 (지표 없음).
- 우상향 확인 = **종목 선정 단계의 분석 전제** — 타겟 종목은 우상향
  할 것으로 분석되었다는 가정이 붙은 상태에서 시스템에 등록.
- 즉 Phase 0.8 의 SupportLevelStrategy 는 "우상향 종목의 일시적 이탈"
  을 매수 시점으로 본다 (가치 투자 정신 + 기술적 분석 결합).
- **코드 레벨 우상향 필터 (예: `current > MA200`) 도입 안 함** —
  yaml 종목 등록 자체가 우상향 분석 통과의 증거.
- Phase 1+ 실거래 진입 시 "우상향 분석" 의 형식화 (예: 명시적
  체크리스트 / 사람 승인 게이트) 검토 가능.

#### 2.2.4 매수 트리거 평가 흐름 (pseudocode)

`SupportLevelStrategy.evaluate(position, current_price, ohlcv_history,
..., excluded_slot_numbers)`:

```python
# 1. 슬롯 1 first-buy bypass
if position is None or position.split_level == 0:
    return self._first_buy(slot_number=1, ...)

# 2. 슬롯 2~5 평가 (이탈 = strict less than)
closes = [bar.close for bar in ohlcv_history]
triggers = []  # list[(slot_number, trigger_price)]

# 슬롯 2: MA5 이탈
if 2 not in excluded_slot_numbers and slot_2_is_empty:
    ma5 = calculate_sma(closes, 5)
    if ma5 is not None and current_price.value < ma5:
        triggers.append((2, ma5))

# 슬롯 3: MA10
# 슬롯 4: MA20
# 슬롯 5: recent_high(60)
#   recent_high = find_recent_high(closes, 60)
#   if current_price.value < recent_high: triggers.append((5, recent_high))

# (Phase 0.8.2 = 슬롯 6, 7 추가 — 별도 라운드)

if not triggers:
    return BuyEvaluationResult(
        decision=None,
        skip_reason=ALL_SLOTS_EMPTY_NO_TRIGGER,
    )

# 슬롯 우선순위 = 가장 작은 slot_number
target_slot, target_price = min(triggers, key=lambda x: x[0])

return self._build_buy_decision(target_slot, current_price, ...)
```

#### 2.2.5 PriceDropStrategy 와의 패턴 차이 (의도된 미묘한 차이)

| 항목 | PriceDropStrategy (Phase 0~0.7) | SupportLevelStrategy (Phase 0.8) |
|---|---|---|
| 비교 연산자 | `current ≤ trigger` (≤, equal 포함) | `current < trigger` (strict <) |
| 의미 | "임계 도달 시 매수" — 가치 투자 (분할 매수 포함) | "지지선 이탈 시 매수" — 기술적 (이탈 시점) |
| equal 케이스 | 트리거 (포함) | 트리거 안 함 (제외) |

**미묘한 차이의 의도**:
- 가치 투자 (PriceDropStrategy) = "임계 도달 = 매수 기회" → equal 포함
  자연스러움
- 기술적 분석 (SupportLevelStrategy) = "이탈 = 추세 약화 신호" → equal
  은 지지선 유지 (이탈 아님)
- **본질적 패러다임 차이의 코드 표현** (CLAUDE.md §13.3 "친절한 추가
  금지" 정합 — 의도적 차이)

#### 2.2.6 테스트 케이스 (0.8.d sub-step 책임, 본 §2 에서 박제)

`SupportLevelStrategy` 단위 테스트 필수 케이스:
- `test_support_level_first_buy` — 슬롯 1, `split_level == 0`
- `test_support_level_ma5_breach` — 슬롯 2 트리거
- `test_support_level_ma_at_value_no_trigger` — `current == MA`, 트리거
  안 함 (strict <)
- `test_support_level_ma_above_value_no_trigger` — `current > MA`,
  트리거 안 함
- `test_support_level_ma_below_value_triggers` — `current < MA`, 트리거
- `test_support_level_priority_smallest_slot` — 여러 트리거 중 가장
  작은 slot_number
- `test_support_level_excluded_slots` — Phase 0.5 §5.5 일관 (cooldown
  슬롯 제외)
- `test_support_level_insufficient_data` — window 부족 시 indicator
  `None` → 해당 슬롯 트리거 안 함

### 2.3 결정 2 = indicators 모듈 범위 (옵션 iii — Phase 0.8.1 = 슬롯 1~5)

| 옵션 | 채택 | 근거 |
|---|---|---|
| (i) Phase 0.8.1 필요 지표만 (슬롯 1~5) | (≈ 채택, 단 placeholder 거부) | 단순 |
| (ii) 7 슬롯 매핑 전체 (placeholder 포함) | 거부 | placeholder 함수 = 미해결 결정 dump 위험 (CLAUDE.md §13.1 "추측하지 말 것" 정합) |
| **(iii) Phase 0.8.1 = 1~5 + 6~7 sub-step 박제 (placeholder 없음)** | **채택** | 변수 통제 (sub-step 분리) + 미해결 결정 노출 (placeholder 회피) |

#### 2.3.1 Phase 0.8.1 indicators 모듈 (슬롯 1~5)

```
src/domain/indicators/
├── __init__.py            # 공개 API
├── moving_average.py      # calculate_sma(closes, window) — 슬롯 2, 3, 4
└── recent_high.py         # find_recent_high(closes, window) — 슬롯 5
```

슬롯 1 = indicator 불필요 (`split_level == 0` 만 체크).

#### 2.3.2 Phase 0.8.2 indicators 모듈 추가 (슬롯 6~7)

Phase 0.8.2 결정 라운드 시 추가:

```
src/domain/indicators/
├── moving_average.py      # window 60 추가 (이미 함수 존재 → 호출만)
├── bollinger.py           # 슬롯 7 (lower band)
└── rsi.py                 # 슬롯 7
```

#### 2.3.3 함수 시그니처 박제 (도메인 순수 함수)

```python
# moving_average.py
def calculate_sma(closes: list[Decimal], window: int) -> Decimal | None:
    """단순 이동평균. closes 길이 < window 이면 None."""

# recent_high.py
def find_recent_high(closes: list[Decimal], window: int) -> Decimal | None:
    """최근 window 거래일 최고가. closes 길이 < window 이면 None."""
```

CLAUDE.md §1.1 정합:
- numpy / pandas import 금지
- 표준 라이브러리 + Decimal 만 사용
- 외부 의존 zero, 도메인 응집

### 2.4 결정 3 = MA window default (옵션 γ — hardcoded, Phase 0.8.1)

| 옵션 | 채택 | 근거 |
|---|---|---|
| (α) yaml 인자화 (`support_strategy.ma_windows: [5, 10, 20]`) | 거부 (Phase 0.8.1) | 변수 차원 추가 — 매수 패러다임 1 차원 통제 위반 |
| (β) hardcoded 영구 | 거부 | CLAUDE.md §9.2 (하드코딩 금지) 정신상 미래 인자화 여지 보존 필요 |
| **(γ) hybrid — Phase 0.8.1 = hardcoded / Phase 0.9+ = 인자화 검토** | **채택** | 변수 통제 + 미래 인자화 여지 보존 (CLAUDE.md §13.3 "친절한 추가 금지" 정합) |

#### 2.4.1 Phase 0.8.1 windows (hardcoded)

```python
# src/domain/indicators/constants.py (또는 각 모듈 모듈 상수)
MA_WINDOWS_PHASE_0_8_1 = (5, 10, 20)  # 슬롯 2, 3, 4
RECENT_HIGH_WINDOW_PHASE_0_8_1 = 60   # 슬롯 5
```

#### 2.4.2 Phase 0.8.2 추가 windows

```python
MA_WINDOWS_PHASE_0_8_2 = (5, 10, 20, 60)  # 슬롯 6 추가
BOLLINGER_WINDOW = 20
BOLLINGER_K = Decimal("2.0")
RSI_WINDOW = 14
RSI_OVERSOLD = Decimal("30")
```

#### 2.4.3 미래 인자화 트리거 (Phase 0.9 / Phase 1+)

다음 조건 충족 시 yaml 인자화 도입 검토:
- 시장별 다른 윈도우 필요 (KRX vs NYSE)
- 종목별 다른 윈도우 필요 (변동성 차이)
- 명시적 사용자 요구 (CLAUDE.md §9.1 config 분리 정신)

### 2.5 백테스트 기간 + lookback warmup (명확화 2 박제)

#### 2.5.1 사용자 명시

> "진입시 모든 종목은 이전 OHLCV가 모두 존재하여, 모든 slot 들이
>    바로 활성화 될 수 있음을 명시해줘. 백테스트시 이점을 고려하여
>    기간을 설정해줘"

#### 2.5.2 데이터 가용성 검증

| 자산 | CSV | 시작 | 종료 | 2019 거래일 |
|---|---|---|---|---:|
| 069500 | `data/historical/KRX_069500_2019-2024.csv` | 2019-01-02 | 2024-12-30 | 246 |
| 132030 | `data/historical/KRX_132030_2019-2024.csv` | 2019-01-02 | 2024-12-30 | 246 |

각 자산 모두 2019 한 해 246 거래일 lookback 보유. Phase 0.8.1 max
lookback (slot 5 = 60 거래일) + Phase 0.8.2 max lookback (slot 6 MA60
= 60 거래일) 모두 만족.

#### 2.5.3 백테스트 기간 (Phase 0.7.3 baseline strict 정합)

| 항목 | 값 | 근거 |
|---|---|---|
| 평가 시작일 | **2020-01-02** | Phase 0.7.3 baseline 그대로 (변수 통제 §1.7) |
| 평가 종료일 | **2024-12-30** | Phase 0.7.3 baseline 그대로 |
| Lookback warmup | 2019-01-02 ~ 2019-12-30 (246 거래일) | 모든 슬롯 (max=60) 즉시 활성 |
| 평가 거래일 수 | 1231 | Phase 0.7.3 baseline 동일 (회귀 invariant 자료) |

**전제 박제**:
- 진입 시점 (2020-01-02) 에 **모든 슬롯이 즉시 활성**.
- 백테스트 첫 날부터 모든 indicator (MA5, MA10, MA20, recent_high(60))
  가 정상 산출 가능 — `None` 반환 케이스는 lookback 부족 시뮬레이션
  외에는 발생 안 함.
- BacktestRunner 가 OHLCV history (2019 lookback 포함) 를
  `SupportLevelStrategy.evaluate` 에 주입. 평가 구간 (`[start, end]`)
  은 strict 하게 2020-01-02 ~ 2024-12-30.

#### 2.5.4 lookback 처리 패턴 (Phase 0.7.2 σ 산출 학습 정합)

ADR 0003 §16.5 (σ lookback) 와 동일 패턴 — `ohlcv_by_asset` 은 평가
구간만 (`[start, end]`) 통과, lookback closes (2019 데이터) 는 별도
경로로 strategy 에 주입. 회귀 invariant 보존.

### 2.6 부속 결정 (default 적용)

| 항목 | default | 근거 |
|---|---|---|
| ADR 박제 위치 | ADR 0004 §2 (본 박제) | §1 = 진입 결정 / §2 = 슬롯/indicators/windows 결정 분리 |
| Decimal precision | 도메인 default `getcontext().prec = 28` | Phase 0~0.7 그대로 |
| SMA 산출 | `sum(closes) / Decimal(N)` | half-even rounding (Decimal default) |
| `excluded_slot_numbers` 매개변수 | Phase 0.5 §5.5 일관 — `SupportLevelStrategy.evaluate` 시그니처 동일 | cooldown 슬롯 제외 패턴 정합 |
| 슬롯 우선순위 | 가장 작은 `slot_number` | pseudocode §2.2.4 박제 |

### 2.7 미박제 / 보류 / 후속

본 §2 박제 후에도 미박제 / 후속 검토 항목:

- **Phase 0.8.2 진입 결정** (§1.5 / §1.9.0.8.i) — Phase 0.8.1 종료 후
  별도 라운드 (단기 매매 + 슬롯 6~7 동시)
- **슬롯 5 정교화** — "돌파 후 되돌림" 패턴 인식 (Phase 0.8.x 후속)
- **이탈 정의 정교화** — 옵션 C (하향 돌파) / 옵션 D (슬롯별
  tolerance) (Phase 0.8.x 후속)
- **MA window yaml 인자화** — Phase 0.9 / Phase 1+ 시점 (§2.4.3)
- **우상향 전제 형식화** — Phase 1+ 실거래 진입 시 명시 체크리스트 /
  사람 승인 게이트 (§2.2.3)
- **Phase 0.7.4 (부동산 분산)** / **§14.7 γ** — ADR 0003 §19.3 / §19.4
  placeholder 보존 그대로

### 2.8 작업 영향 박제

#### 2.8.1 ADR 갱신 (본 박제 = §2)

- §2.2 슬롯 매핑 (1~5 Phase 0.8.1 / 6~7 Phase 0.8.2)
- §2.3 indicators 범위 (Phase 0.8.1 = MA + recent_high)
- §2.4 hardcoded windows (5, 10, 20 / 60)
- §2.5 백테스트 기간 + lookback warmup
- §2.2.3 우상향 전제

#### 2.8.2 코드 작업 영향 (Phase 0.8.1)

- `src/domain/indicators/moving_average.py` (신규) — `calculate_sma`
- `src/domain/indicators/recent_high.py` (신규) — `find_recent_high`
- `src/domain/indicators/__init__.py` (신규) — 공개 API
- `src/domain/strategies/support_level_strategy.py` (0.8.d) — 슬롯 1~5
  평가
- `src/domain/strategies/support_slot.py` (0.8.c) — `SupportSlot` 모델
- `src/application/backtest_runner.py` (0.8.e) — OHLCV history 주입
  (lookback 포함) + `PriceDropStrategy` 회귀 invariant 보존
- `config/strategies-0.8.1.yaml` (0.8.f) — `buy_strategy:
  support_level` 박제

#### 2.8.3 시간 추정

- Phase 0.8.1: 3.5 ~ 4 주 (슬롯 1~5)
- Phase 0.8.2: 1.5 주 (슬롯 6~7 추가)
- 총 5 ~ 5.5 주 (원안 5 주 비슷)

### 2.9 박영옥 원전 정신과의 정합 박제

| 항목 | 박영옥 원전 | Phase 0.8.1 SupportLevelStrategy |
|---|---|---|
| 슬롯 2~4 (MA 이탈) | "이탈 시 매수" 그대로 | strict less than 채택 |
| 슬롯 6 (MA60 이탈) | 동일 | Phase 0.8.2 |
| 슬롯 1 (first-buy) | 우상향 종목의 첫 진입 | 우상향 전제 + `split_level == 0` 만 체크 |
| 슬롯 5 (직전 전고점) | "지지로 전환" | Phase 0.8.1 단순화 (단순 N일 max) — Phase 0.8.x 정교화 가능 |
| 슬롯 7 (BB / RSI) | 전통 박영옥은 OR 미지정 | Phase 0.8.2 OR 조건 (변동성/모멘텀 결합) |

박영옥 원전 정신은 슬롯 2~4, 6 에서 그대로 반영. 슬롯 1, 5, 7 은
Phase 0.8.1 단순화 / Phase 0.8.2 확장 시 명시 박제.
