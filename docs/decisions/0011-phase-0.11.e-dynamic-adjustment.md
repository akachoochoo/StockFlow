# ADR 0011 — Phase 0.11.e: Dynamic Adjustment Proposal Engine (L2/L3 — Parameter & Strategy/Universe Change Proposals)

> **Status**: §1 박제 완료 (ralplan #27 Round 2 APPROVE, 2026-05-12). Phase 0.11.e sub-step 0.11.e.2 진입 ready. §2 / §3 후속 박제 대기. §3 회고 commit 후 **별도 2 commit (fold 금지)**: (A) CLAUDE.md §16.4 ADR 0008 → 0012 재번호, (B) §16.1 항목 #2 정정 (ADR 0010 §3 회고 옵션 Z 정합).
> **Date**: 2026-05-12 (draft) / 2026-05-12 (§1 Round 1 ITERATE 14 patches 흡수 + 사용자 결정 6 정합 D14 `ProposalHistory` 정식 도출 + D15 invariant supersede 차단 governance 신규 + §1.6 6 invariant 측정 가능 verification + Critic default 정량 (drift bound ±30% / minimum interval 12 주 / file exists) 수락) / 2026-05-12 (§1 Round 2 APPROVE + R4 budget 정정 13→15) / `<TBD>` (§3 박제 + 별도 2 commit §16.4 + §16.1 항목 #2 정정).
> **Supersedes**: 없음.
> **Related**:
> - CLAUDE.md §14 "Phase 1 진입 전 작성 금지 통합 목록" — "score-based 종목 우선순위 / Hot reload — Phase 1+".
> - CLAUDE.md §16.1 "패턴 (의식 — 코드 추가는 금지)" — 본 phase 가 의식 → 코드 승격의 첫 trigger.
> - `docs/multi-asset-trading-system-design.md` §1.2 "LLM 자율 매매 시스템의 한계" — Reflexivity / 샘플 부족.
> - `docs/multi-asset-trading-system-design.md` §1.3 "AI는 파라미터 튜닝 X, 종목 선정 X, 매매 결정 X" — 본 phase 가 정면 충돌 가능 invariant. §1.6 mitigation.
> - `docs/multi-asset-trading-system-design.md` §8.1.2 "AI는 보조 안전장치 — 메인 의사결정에 절대 개입 금지" — 본 phase 의 design contract.
> - ADR 0007 (Phase 0.11.a) — `src/research/` 5th ring 격리 패턴.
> - ADR 0010 (Phase 0.11.d) — 자산별 전략 차별화 진단 (선행 — 본 phase 의 D 결정 입력).
> **Execution order**: ADR 0010 (Phase 0.11.d) 완료 후 진입. Phase 1 ADR (가칭 ADR 0012 — §1.9 재번호 박제 참조) 진입 전 본 phase 종료 의무.

---

## 1. Decision Drivers (ralplan #27 라운드 1 박제 대상)

### 1.1 사용자 spec verbatim

> "하나의 전략만 계속 사용하는것은 불가능하고 비 효율적이랑 생각되어, 상황을 주시하고 주식 자동매매 전략의 변경 + 파라미터 변경 또는 대상 종목을 제거 및 추가 하는 방법도 필요하다고 생각해. 물론 이것의 위험성에 대해서도 충분히 인지 하고 있어."
>
> 사용자 confirmation (2026-05-12):
> - L1~L4 4-tier 분리 모델 채택. **본 phase 는 L2/L3 만 박제** (L1 = Phase 2 차단기, L4 = 새 phase 시작).
> - **발굴 자동화는 후속 Phase 로 미룸** — 본 phase 범위 외.
> - Phase 1 진입 지연 비용 의식 — 본 phase 종료 후 Phase 1 진입.

### 1.2 L1~L4 4-tier 분리 모델 (본 phase = L2/L3 만)

| Tier | 변경 결 | 빈도 | 자동/수동 | Phase 매핑 | 본 phase 범위? |
| --- | --- | --- | --- | --- | --- |
| L1 | 실시간 차단 | 분~시간 | 자동 (AI 차단기) | Phase 2 | ❌ — 후속 phase |
| **L2** | **파라미터 조정** | **분기~연** | **반자동 (제안만, 사람 승인)** | **Phase 0.11.e (본 phase)** | **✅** |
| **L3** | **전략 교체 / 종목 변경** | **6개월~연** | **수동 (ADR 박제 의무)** | **Phase 0.11.e (본 phase)** | **✅** |
| L4 | 알고리즘 교체 | 1~2년 | 수동 + 새 phase 시작 | (CLAUDE.md §9.1) | ❌ — 영구 |

### 1.3 본 phase 의 비범한 위험성 (다른 phase 와 다른 점)

본 phase 는 0.11.b/c/d 와 본질이 다름 — **multi-asset-trading-system-design §1.3 "AI 일단위 파라미터 튜닝 거부" invariant 와 정면 충돌 가능 영역에 박제**. 따라서 §1.6 의 design contract 가 다른 phase 의 invariant 보존보다 더 strict.

핵심 risk 2 개 (§1.5 R1+R2 정밀화):

1. **Reflexive overfitting** — 시스템이 자기 거래 결과 보고 파라미터 변경 → 1000 거래 미만 단계에서 noise 적합. multi-asset-trading-system-design §1.2 "샘플 부족 (연 250 데이터 포인트 미만)" 정면 적용.
2. **결정 누락 cascading** — L2 변경 → 새 baseline → 다음 L2 변경 → ... → 6개월 후 추적 불가. CLAUDE.md §7.1 "결정 시점 입력값 전체를 JSON 보존" invariant 의 *발굴/조정 단계 확장* 의무.

### 1.4 Decision Questions (D1~Dn, ralplan #27 박제 대상)

> 본 절은 **skeleton**. ralplan #27 Round 1 Critic/Architect 가 D 항목을 추가/병합/거부할 수 있음.

- **D1** — **L2/L3 trigger 신호 정의 — 원천 데이터 제약 hard + AND-gate + sample size threshold + NULL proposal** (Round 1 ITERATE Patch 4):

  **원천 데이터 제약 hard**: L2/L3 trigger evaluator 입력 = **backtest OOS metric + 확정 OHLCV 데이터 only**. 실시간 가격 / VIX / 뉴스 / sentiment / 소셜 미디어 신호 = **절대 금지** (L1 Phase 2 영역). Enforcement: `scripts/check_namespace.sh` 확장 또는 별도 grep rule (`grep -rE "(실시간|VIX|뉴스|sentiment|twitter)" src/research/dynamic_adjustment/` empty).

  **Trigger 후보**:
  - (i) **Rolling OOS Sharpe 임계** (예: 36-month rolling Sharpe < 0.3, backtest 통계 기반) — **AND-gate 조건**: (a) sample size ≥ 100 거래 + (b) Drawdown 임계 동시 충족.
  - (ii) **Drawdown 임계** (MDD < ADR 0007 §3 G2 baseline × 1.50 = -50% 악화 한계, backtest 통계 기반) — **drawdown trigger ≠ 손절 제안** (Round 1 ITERATE Patch 7, CLAUDE.md §11.4 정합): trigger 발화 = "전략 재검토 제안" 생성. 손절 / 포지션 축소 제안 = Phase 1 ADR 0012 별도 영역. `ProposalType` enum 에 `STOP_LOSS` 부재 명시.
  - (iii) **Regime change 감지** (자산 변동성 24-month 평균의 2배) — **거부 (Phase 2 위임)** (Round 1 ITERATE Patch 12). 거부 사유: regime change = 실시간 변동성 신호 의존 → D1 원천 데이터 제약 (backtest OOS only) 위반 + §1.6 #2 invariant 충돌. L1 (실시간 차단기, Phase 2) 의 정확한 책임 영역.
  - (iv) **사람 ad-hoc 요청** — CLI command 진입.

  **권고 default**: **(i) AND (ii) AND (iv) 조합 AND-gate** (ADR 0008 D11 패턴 정합) + (iii) 거부 박제.

  **NULL proposal semantics**: trigger 발화 but sample size < 100 또는 AND-gate 조건 미충족 시 "데이터 부족으로 평가 보류" NULL proposal 생성 (Architect 권고 #1 흡수 — 자동 제안 차단 + 추적성 보존).
- **D2** — **L2/L3 trigger evaluation 주기 + catch-up 정책** (Round 1 ITERATE Patch 6, CLAUDE.md §11.3 정합):

  | 후보 | 주기 | 채택? |
  | --- | --- | --- |
  | (a) 일간 cron (의사결정 cron 동일) | 1 일 | ❌ — false positive 폭주 + 의사결정 cron 충돌 |
  | **(b) 주간 cron 별도** | 7 일 (토요일 UTC 00:00 고정) | **✅ default** |
  | (c) 월간 cron 별도 | 30 일 | ❌ — 시장 변화 지연 |
  | (d) 사람 trigger only | ad-hoc | ❌ — 자동 evaluation 부재 |

  **(b) 채택 근거**: Phase 1 안정화 momentum 보존 + 의사결정 cron 격리 (CLAUDE.md §10.1 단일 프로세스 cron 정합 — 별도 프로세스로 격리) + deterministic ordering (실행 요일 토요일 UTC 00:00 고정, D12 reproducibility 정합).

  **Catch-up 정책 hard** (Round 1 ITERATE Patch 6): CLAUDE.md §11.3 "자동 catch-up 금지" 정합 = L2/L3 evaluation cron 이 며칠 미실행 시 누락 주기 **자동 따라잡기 절대 금지**. 사람 명시 `--force-evaluate --date YYYY-MM-DD` 명령으로만 재평가. Enforcement: cron 진입 시 마지막 evaluation timestamp 와 현재 timestamp 차이 > 14 일 시 skip + 알림 + 사람 명시 trigger 대기.
- **D3** — **Proposal 도메인 객체 스키마 — 8 필드 (Round 1 ITERATE Patch 1 추가)**:

  순간 snapshot 보존 (cumulative history 는 D14 `ProposalHistory` 영역, 관심사 분리):
  1. `trigger_signal: TriggerSignal` (D1 trigger enum)
  2. `current_state_snapshot: dict` (proposal 시점 portfolio + parameter 상태)
  3. `proposed_change: ChangeSpec` (L2 = parameter delta, L3 = strategy/universe delta)
  4. `reasoning_json: dict` (CLAUDE.md §7.1 정합 — 결정 시 입력값 전체 JSON)
  5. `supporting_backtest_result: BacktestResult` (ADR 0008/0009 산출 직접 호출, D11)
  6. `state: ProposalState` (PENDING / APPROVED / ADR_FILED / APPLIED / REJECTED — 4-state 머신 + REJECTED terminal, D6)
  7. `adr_reference: str | None` (APPROVED→ADR_FILED 전이 시 박제)
  8. **`previous_proposal_ref: ProposalId | None`** (Round 1 ITERATE Patch 1 신규) — 직전 동일-parameter proposal 참조. D14 minimum interval / cumulative drift 검증의 입력.

  **R2 cascading history 추적 책임은 D14 `ProposalHistory` 도메인 객체로 분리** (옵션 β 채택 — D3 단일 proposal SRP 보존).
- **D4** — **사람 승인 인터페이스**: (a) CLI (`trading proposal list / approve / reject`), (b) Telegram bot, (c) 둘 다, (d) 외부 모니터링 도구 연동. **권고 default**: (a) — CLAUDE.md §10.1 "단일 프로세스 cron 기반" 정합, Telegram 은 Phase 1+ 위임.
- **D5** — **Reflexive overfitting 방지 — Phase 1 실거래 데이터 사용 정책**: (i) 사용 **절대 금지** (확정 OHLCV 데이터만), (ii) 6개월 lag 후 사용 허용, (iii) Walk-forward window 외부 데이터로만 사용. **권고 default**: **(i)** — Phase 1 안정화 전 가장 안전 + §1.6 Design Contract #3 "Reflexive overfitting 방지 hard" invariant 정합 + multi-asset-trading-system-design §8.3 "OOS 한 번만 — 결과 보고 재조정 금지" 정합.

  **Enforcement** (Round 1 ITERATE Patch 3, §1.6 #3 verification): Phase 1 trade log file path (`<TBD: Phase 1 ADR 0012 결정>`) 가 backtest input path 에 도달 불가능함을 `pytest -k reflexive_data_isolation` test 로 자동 검증.
- **D6** — **Proposal 4-state 상태 머신 + ADR enforcement** (Round 1 ITERATE Patch 5):

  ```
  PENDING ──사람 CLI 승인──> APPROVED ──ADR 파일 존재 + reference 박제──> ADR_FILED ──다음 cron pickup──> APPLIED
     │                          │
     └──사람 CLI 거부──────────> REJECTED (terminal)
  ```

  **전이 규칙**:
  - (a) PENDING → APPROVED: 사람 CLI 명령 `trading proposal approve <id>`.
  - (b) APPROVED → ADR_FILED: **자동 검증** = `os.path.exists(f"docs/decisions/{Proposal.adr_reference}.md")` (Critic default 수락 — file exists 수준, grep 박제는 0.11.e.2 sub-step 재검토 가능).
  - (c) ADR_FILED → APPLIED: 다음 weekly cron 실행 시 자동 pickup (D7 (c) 정합).
  - **APPROVED → APPLIED 직접 전이 = 차단 hard** (§1.6 #4 enforcement).
  - REJECTED = terminal state, 재진입 불가.

  **ADR 자동 template scaffolding** (하이브리드): `trading proposal approve <id>` 명령 시 ADR template 파일 자동 생성 (사람이 ADR 본문 채워야 file size > N 통과 → ADR_FILED 전이). 최소 필드 검증 = ADR 번호 + supersede 대상 + reasoning 비어있지 않음.
- **D7** — **변경 적용 메커니즘**: 사람이 APPROVED + ADR_FILED 후 — (a) `config/strategies.yaml` hot reload, (b) 재배포 (CLAUDE.md §9.1 "알고리즘 = 코드 + 재배포" 정합), **(c) 다음 cron 실행 시 자동 pickup**. **권고 default**: **(c)** — 단일 프로세스 cron 정합 + hot reload 의 race condition 회피 + D6 (c) 전이 trigger.
- **D8** — **L2 (파라미터) vs L3 (전략/종목) 권한 분리 + L1-shadow enum 강제** (Round 1 ITERATE Patch 10):

  ```python
  class ProposalType(Enum):
      PARAMETER_CHANGE = "parameter"           # L2 — 본 phase scope
      STRATEGY_CHANGE = "strategy"             # L3 — 본 phase scope
      UNIVERSE_CHANGE = "universe"             # L3 — 본 phase scope (종목 추가/제거, §14 영역과 분리 — 사람 인지 trigger only)
      # 다음 enum variant 부재 강제 — exhaustive match test 로 컴파일 타임 차단:
      # STOP_LOSS                              # ❌ Phase 1 ADR 0012 별도 영역 (§11.4 정합)
      # REALTIME_HALT                          # ❌ L1 = Phase 2 영역 (D9 NO 정합)
      # VIX_CIRCUIT_BREAK                      # ❌ L1 = Phase 2 영역 (D9 NO 정합)
      # SELL_STRATEGY_CHANGE                   # ❌ ADR 0010 D4 결정 부재 시 enum variant 자체 부재 (D12, §1.6 #6 정합)
  ```

  **권고 default**: (ii) 별도 클래스 — 각 tier 의 reasoning + backtest 요구사항이 다름. Enum exhaustive match test = L1 영역 type / STOP_LOSS / SELL_STRATEGY_CHANGE (ADR 0010 D4 미확정 시) 등 부재 컴파일 타임 차단.
- **D9** — **Phase 1 미구현 상태 fallback semantics**: L1 (Phase 2 차단기) 미구현 상태에서 L2 가 "VIX 40+, 신규 진입 정지 제안" 같은 *L1-shadow* 신호를 다룰 수 있는가? **권고 default**: **NO** — L2 는 backtest 통계 기반 trigger 만 (D1 원천 데이터 제약), L1-shadow 는 Phase 2 위임. 책임 경계 명확화. **Enforcement** (Round 1 ITERATE Patch 10): D8 `ProposalType` enum 에 L1 영역 type (REALTIME_HALT / VIX_CIRCUIT_BREAK) 부재 강제 — enum exhaustive match test 로 컴파일 타임 차단.
- **D10** — **namespace 위치 cross-check 구체화** (Round 1 ITERATE Patch 14):

  - **(a) `src/research/dynamic_adjustment/` (5th ring) 채택** — ADR 0007 §1.6 패턴 상속 + Phase 1 안정화 전 production 침범 zero.
  - **ADR 0010 D9 cross-check**: ADR 0010 D9 = `docs/analysis/` (코드 변경 zero phase). 본 phase D10 (a) = `src/research/dynamic_adjustment/` (코드 산출). 두 phase 의 namespace 영역 겹침 zero — cross-check = `src/research/dynamic_adjustment/` 가 `docs/analysis/` 를 import 하지 않음 (trivially 보장, Python import 불가).
  - **Intra-research cross-import 방향성** (ADR 0008 D9 + ADR 0009 D9 패턴 상속): `src/research/dynamic_adjustment/` → `src/research/dgt/` / `src/research/visualization/` 정방향 허용 (D11 산출 호출). 역방향 차단 = ADR 0008 D9 grep rule 으로 보장.
  - 거부: (b) `src/discovery/` 6-ring 신설 = architecture 모델 supersede 과도. (c) `src/use_cases/proposal/` = production 4-ring 진입 = 0.11.x invariant 위반.
- **D11** — **Backtest 인프라 재사용**: L2 제안의 supporting backtest 가 사용할 인프라 — (i) ADR 0009 (Phase 0.11.c) renderer 산출 직접 호출, (ii) ADR 0008 (Phase 0.11.b) optimization 산출 직접 호출, (iii) 본 phase 가 별도 백테스트 wrapper 박제. **권고 default**: **(i)+(ii)** — 중복 제거.
- **D12** — **회귀 invariant 박제 + sell strategy 종속 측정 가능 형태** (Round 1 ITERATE Patch 13):

  본 phase 산출이 0.11.a~d 의 모든 invariant 를 보존하는지 cross-check:
  - Phase 0.7.3 baseline (069500+132030 EQUAL + PriceDropStrategy).
  - 0.11.a/b namespace isolation 14 tests.
  - `BacktestResult` 스키마 (frozen dataclass 변경 zero, ADR 0010 §1.2 잠정 결론 정정 정합).
  - **단일 sell strategy 가정 종속** (CLAUDE.md §16.1 항목 #4 + ADR 0010 §1.6 #6) — **측정 가능 형태**: ADR 0010 D4 결정 부재 확인 = `grep -E "D4.*결정" docs/decisions/0010-*/§3` 부재 시 `ProposalType.SELL_STRATEGY_CHANGE` enum variant 자체가 코드에 **존재하지 않음** (enum exhaustive match 로 구조적 차단). `pytest -k sell_proposal_blocked_without_adr0010` test 로 검증.
- **D13** — **Lifecycle promote 조건 + Phase 2 진입 시 L2 trigger 재검토**: 본 phase 산출의 production rings 진입 조건 — (i) Phase 1 안정화 3개월 무사고 운영 후 promote 후보, **(ii) Phase 1 안정화 6개월 + Phase 2 (차단기) 완료 후 promote**, (iii) production rings 영구 진입 금지 (5th ring 영구 유지). **권고 default**: **(ii)** — L1 차단기와의 책임 경계가 명확해진 후 promote.

  **Phase 2 진입 시 L2 trigger 재검토 의무** (Architect 권고): Phase 2 의 L1 실시간 차단기 구현 시 본 phase 의 L2 trigger 일부 (예: Drawdown trigger) 가 L1 과 *책임 경계 겹침* 발생 가능 — Phase 2 ADR 진입 시점에 L2 trigger 정의 재검토 의무 박제.
- **D14** — **NEW: `ProposalHistory` 도메인 객체 + Structural Constraints (R2 mitigation 정면 박제, Round 1 ITERATE Patch 1)**:

  **사용자 결정 6 정합** (옵션 β 채택) — D3 7+1 필드는 *순간 snapshot* 책임만 유지, D14 = *cumulative history + structural bound* 책임 분리 (SRP 정합).

  **D14 `ProposalHistory` 도메인 객체**:
  - (a) 모든 `APPLIED` state 의 proposal 시간순 히스토리 보존.
  - (b) **Cumulative parameter drift 자동 계산** — 초기 설정 값 (Phase 0.7.3 baseline 또는 Phase 1 ADR 0012 진입 시점 박제) 대비 총 변동량.
  - (c) **자기-trigger 감지** — Proposal.previous_proposal_ref 체인 따라 "직전 변경이 현재 trigger 의 원인" 여부 grep.

  **Structural Constraints 3 축** (Critic Round 1 도출 default 수락):
  - **Minimum interval**: 동일 parameter 의 연속 변경 = **최소 12 주 간격** (분기 = D2 (b) 주간 cron × 12 = 분기 평가 1 회 정합). L2/L3 동일 적용 (사용자 결정 = Critic default 수락 — L3 별도 26 주 차별화 미채택, 0.11.e.2 sub-step 진입 시 재검토 가능 명시).
  - **Cumulative drift bound**: 초기 설정 대비 parameter 절대 변동 **±30% 한계** (분기 1 회 변경 × 최대 10% 변동 × 3 회 = 30% 산술). 초과 시 NULL proposal + 사람 명시 ADR (전체 재설정) 의무.
  - **Trigger cooldown**: trigger 발화 → APPLIED 후 **동일 trigger 재발화 차단 12 주** (minimum interval 동일).

  **Enforcement**: `pytest -k proposal_history_constraints` test 로 (a) minimum interval 위반 시 proposal 생성 차단, (b) cumulative drift bound 위반 시 NULL proposal 생성, (c) trigger cooldown 위반 시 trigger 발화 차단 검증.
- **D15** — **NEW: §1.6 6 invariant supersede 차단 governance (Round 1 ITERATE Patch 2)**:

  > **§1.6 6 invariant 의 *정신* (spirit) 은 ralplan amendment / Round 2 revision / 후속 phase 의 어떤 결정 라운드도 supersede 불가. 정신 변경 = L4 (알고리즘 교체, §1.2 table) 영역 — 새 phase 시작 의무.**

  Amendment 는 *문구 (wording) 정밀화 / 측정 가능 형태 추가* 만 허용. 정신 변경 (예: "시스템은 제안만 한다" → "시스템이 자동 변경 가능" 으로의 변경) = L4 trigger.

  Governance enforcement: 본 phase 회고 (0.11.e.5) commit 시점에 §1.6 6 invariant 의 *정신* 박제 verbatim 보존 확인 + 후속 phase 의 ralplan 진입 시 §1.6 6 invariant 정신 supersede 여부 검증 의무 (Phase 1 ADR 0012 진입 시 본 ADR 0011 D15 cross-reference 필수).

### 1.5 Gates G1~G4 (success criterion, ralplan #27 박제 대상)

- **G1 (PRIMARY)** — `Proposal` 도메인 객체 + L2/L3 trigger evaluator + 제안 박제 인프라 (D1+D3+D8). 코드 + tests 박제.
- **G2 (PRIMARY)** — 사람 승인 워크플로우 (CLI + ADR enforcement) 동작 검증 (D4+D6+D7). End-to-end test — trigger → proposal → human approve → ADR 박제 → 변경 적용 한 사이클.
- **G3 (PRIMARY)** — ADR header + 회고 + design contract (§1.6) 박제.
- **G4 (PRIMARY)** — namespace CI + mypy strict + 재현성 + Reflexive overfitting 방지 검증 (D5 invariant — Phase 1 실거래 데이터 미사용 자동 enforcement test).

### 1.6 Design Contract (본 phase 의 invariant — 다른 phase 보다 strict, 측정 가능 verification 형태)

본 phase 의 모든 코드는 다음 contract 를 만족해야 함. **정신 (spirit) supersede 불가 — D15 governance 정합** (Round 1 ITERATE Patch 2). Amendment 는 문구 정밀화 / 측정 가능 형태 추가만 허용.

1. **시스템은 제안만 한다** — 변경 적용은 사람 승인 + ADR 박제 후. 자동 적용 절대 금지.
   - **Verification** (Round 1 ITERATE Patch 3): `pytest -k proposal_auto_apply_blocked` — `Proposal.state != APPLIED` 시 config 변경 코드 실행 차단 자동 test.
2. **multi-asset-trading-system-design §1.3 "AI는 파라미터 튜닝 X, 종목 선정 X, 매매 결정 X" invariant 보존** — 본 phase 는 *통계 기반 trigger evaluator* 이지 *AI 결정 시스템* 이 아님. 차이 명확화 의무 (D1 trigger 정의가 통계 임계 기반 + 원천 데이터 = backtest OOS only).
   - **Verification**: `grep -rE "(실시간|VIX|뉴스|sentiment|twitter|news)" src/research/dynamic_adjustment/ --include="*.py" | grep -v "^#" | grep -v test` empty (D1 trigger 원천 데이터 제약 enforcement).
3. **Reflexive overfitting 방지 hard** — D5 invariant. Phase 1 실거래 데이터의 backtest 입력 사용 시 자동 차단 test 박제.
   - **Verification**: `pytest -k reflexive_data_isolation` — Phase 1 trade log file path 가 backtest input path 에 도달 불가능함 검증 (file path isolation + data source tag).
4. **ADR 박제 없이 변경 적용 차단 hard** — D6 (iii) invariant + 4-state 상태 머신 (PENDING → APPROVED → ADR_FILED → APPLIED). APPROVED→APPLIED 직접 전이 = **차단**.
   - **Verification**: `pytest -k adr_enforcement_blocks_apply` — `Proposal.state == APPROVED + adr_reference == None` 시 APPLIED 전이 차단 test.
5. **CLAUDE.md §13.3 "친절한 추가 금지" 보존** — 사용자 spec 외 trigger 신호 (예: 뉴스 sentiment, twitter trend) 추가 절대 금지.
   - **Verification**: `pytest -k trigger_signal_enum_exhaustive` — `TriggerSignal` enum 이 D1 (i)/(ii)/(iv) 3 종 + NULL proposal marker 외 추가 시 test FAIL (exhaustive match).
6. **CLAUDE.md §16.1 항목 #4 "sell strategy 단일 가정" 종속** — sell strategy 변경 제안은 ADR 0010 D4 (sell strategy 차별화 범위) 산출의 결정에 종속. ADR 0010 D4 미확정 시 sell strategy 제안 차단.
   - **Verification**: `pytest -k sell_proposal_blocked_without_adr0010` — ADR 0010 §3 회고에 D4 결정 박제 부재 시 `ProposalType.SELL_STRATEGY_CHANGE` enum variant 자체 부재 (D12 enum exhaustive match 정합).

### 1.7 Risks (R1~Rn, ralplan #27 박제 대상)

- **R1 (CRITICAL)** — **Reflexive overfitting**. §1.3 + D5 mitigation. 본 phase 가장 핵심 risk.
- **R2 (CRITICAL)** — **결정 누락 cascading**. §1.3 + D6 mitigation (ADR enforcement). 본 phase 두 번째 핵심 risk. **사용자 결정 (2026-05-12) 박제**: 추가 mitigation layer (change history domain object / structural constraint — minimum interval, cumulative drift bound, trigger cooldown) 는 ralplan #27 Round 1 Critic 의 antithesis 로 도출 유도 — 본 ADR skeleton 에 명시 박제 안 함. 0.11.a §7.5 효율적 종결 패턴 + antithesis 공간 보존 정신.
- **R3 (HIGH)** — **§1.3 invariant 충돌**. multi-asset-trading-system-design §1.3 "AI 일단위 튜닝 거부" 와 본 phase 의 *그어드 라인 경계* 모호. Mitigation: §1.6 Design Contract #2 — *통계 임계 evaluator* vs *AI 결정 시스템* 명확화.
- **R4 (HIGH)** — **Phase 1 진입 지연**. 본 phase 종료 의무로 인한 Phase 1 지연 cost. Mitigation: ralplan #27 의 D 결정 항목을 **15 개 이하** (Round 2 D14/D15 신규 후 한계 상향, §1.10 시나리오 C 정합) + sub-step 수 5 개 이하 + 코드 변경량 budget 박제 + 본 phase 산출이 Phase 1 promote 후 production rings 진입 = 지연 cost 의 대칭 가치 박제.
- **R5 (MEDIUM)** — **L1 미구현 상태에서의 책임 경계 모호**. D9 mitigation.
- **R6 (MEDIUM)** — **ADR 0010 미진행 상태 진입 risk**. §1.6 #6 invariant 박제 — ADR 0010 의 D4 (sell strategy 차별화) 결정 없이 본 phase 의 sell strategy 변경 제안 불가능. Mitigation: §Execution order (ADR 0010 완료 후 진입) hard.
- **R7 (MEDIUM)** — **번호 충돌 + §16.4 / §16.1 항목 #2 정정 시점 박제** (Round 1 ITERATE Patch 11). CLAUDE.md §16.4 "Phase 1 ADR = 가칭 ADR 0008" 박제 + 본 phase = ADR 0011 → **Phase 1 ADR = 가칭 ADR 0012 (재번호)** 의무. **Mitigation**: 0.11.e.5 회고 commit 후 **2 개 별도 commit (fold 금지)**:
  - **(A) §16.4 ADR 0008 → 0012 재번호 정정** (ADR 0011 §1.9 박제 의무).
  - **(B) §16.1 항목 #2 "AssetContext 의식 → 코드 추가 금지" 정정** (ADR 0010 §3 회고 옵션 Z 권고 정합 — `AssetContext` 기존 구현 발견 박제 후 §16.1 항목 #2 본문 현실 반영).

  두 commit 모두 0.11.e.5 회고 commit 과 fold 금지 (CLAUDE.md §12.1 commit 단위 원칙 + ADR 0010 §1.6 #5 옵션 Z 패턴 정합).
- **R8 (LOW)** — **namespace 침범**. 5th ring 보존, ADR 0010 D9 와 cross-check.
- **R9 (LOW)** — **사람 승인 워크플로우의 bottleneck 화**. 사람이 검토 늦으면 시장 변화 미반영. Mitigation: D2 (b) 주간 cron — 일~월 단위 적합.

### 1.8 Sub-step roadmap (D 결정 박제 완료 — 5 개 이하 R4 mitigation)

| Sub-step | 본질 | 산출 (예상) |
| --- | --- | --- |
| 0.11.e.1 | D1~D15 결정 + ADR §1 박제 + Design Contract 박제 + roadmap | ADR 0011 §1 commit |
| 0.11.e.2 | `Proposal` 도메인 객체 (4-state 머신 + 8 필드) + `ProposalHistory` (D14 cumulative + structural constraints) + `TriggerSignal` / `ProposalType` enum (exhaustive match) + L2/L3 trigger evaluator + intra-research cross-import grep rule + namespace test | `src/research/dynamic_adjustment/` (D10 default) + `scripts/check_namespace.sh` 확장 |
| 0.11.e.3 | 사람 승인 워크플로우 (CLI + ADR auto-template scaffolding + ADR file exists 검증 + APPROVED→ADR_FILED 자동 검증) + ADR enforcement 4-state 전이 test + End-to-end test | `src/research/dynamic_adjustment/cli.py` (`python -m src.research.dynamic_adjustment.cli proposal list / approve / reject`) |
| 0.11.e.4 | §1.6 6 invariant verification command 전체 (Reflexive data isolation / proposal auto apply blocked / ADR enforcement / sell proposal blocked / trigger signal enum / D1 원천 데이터 grep) + D14 structural constraints test + 회귀 invariant cross-check (Phase 0.7.3 baseline + ADR 0006 reporting layer 보존) | `tests/research/dynamic_adjustment/` (40~60 신규 tests) |
| 0.11.e.5 | 회고 + ADR §3 박제 + 게이트 판정 + Phase 1 진입 ready 선언 → **별도 2 commit** (R7 mitigation): (A) CLAUDE.md §16.4 ADR 0008→0012 재번호, (B) CLAUDE.md §16.1 항목 #2 정정 (ADR 0010 옵션 Z 정합) | `docs/retrospectives/phase-0.11.e.md` + 별도 2 commit |

### 1.9 References + ADR 번호 재번호 박제

**ADR 번호 재번호 — drift 정정 (Phase 0.11.a §7.4 pattern)**:

CLAUDE.md §16.4 (2026-05-11 박제) 는 "Phase 1 ADR = 가칭 ADR 0008" 로 박제. 그러나 Phase 0.11.b (ADR 0008) 가 본 번호 점유 — Phase 0.11.c = ADR 0009, Phase 0.11.d = ADR 0010, **Phase 0.11.e (본 phase) = ADR 0011** 진행. 따라서:

> **Phase 1 ADR = 가칭 ADR 0012 (재번호)**. CLAUDE.md §16.4 갱신 의무 — 본 phase 회고 (0.11.e.5) commit 후 **별도 commit (fold 금지)** 으로 CLAUDE.md §16.4 의 "Phase 1 ADR = 0008" 을 "Phase 1 ADR = 0012" 로 정정 박제 (R7 mitigation 정합).

**별도 commit 정합 명시** (Round 1 ITERATE Patch 11, ADR 0010 §1.6 #5 옵션 Z 패턴 정합): 0.11.e.5 회고 commit 후 **2 개 별도 commit** (fold 금지):
- **(A) §16.4 ADR 0008→0012 재번호 정정 commit** — 본 ADR 0011 §1.9 박제 의무.
- **(B) §16.1 항목 #2 "AssetContext 의식 → 코드 추가 금지" 정정 commit** — ADR 0010 §3 회고 옵션 Z 권고 정합 (`AssetContext` 기존 구현 발견 박제 후 §16.1 항목 #2 본문 현실 반영).

두 commit 모두 0.11.e.5 회고 commit 과 fold 금지 (CLAUDE.md §12.1 commit 단위 원칙).

**References**:

- ADR 0003 §11.3 — Phase 1 ADR 후보 박제 원본.
- ADR 0007 (Phase 0.11.a) — `src/research/` 5th ring 격리 패턴 + 박제 primary 정신.
- ADR 0008 (Phase 0.11.b) — DGT optimization 산출 (본 phase D11 입력) + D11 AND-gate trigger 패턴 + 조건부 supersede 절차 패턴.
- ADR 0009 (Phase 0.11.c) — Visualization renderer 산출 (본 phase D11 입력) + D9 intra-research cross-import 정방향 허용 + 역방향 차단 grep rule 패턴.
- ADR 0010 (Phase 0.11.d) — 자산별 전략 차별화 진단 (본 phase 의 D4 sell strategy 종속 — §1.6 #6) + §1.6 #5 옵션 Z 패턴 (§16.1 항목 #2 정정 시점) + `AssetContext` 기존재 발견.
- CLAUDE.md §7.1 — 모든 의사결정 reasoning JSON 보존 (D3 (iv) 정합).
- CLAUDE.md §10.1 — 단일 프로세스 cron (D7 (c) 정합).
- CLAUDE.md §11.3 — 자동 catch-up 금지 (D2 catch-up 정책 정합).
- CLAUDE.md §11.4 — 손실 한도 / 자동 손절 안 함 (D1 (ii) drawdown ≠ 손절 분리 정합).
- CLAUDE.md §13.3 — 친절한 추가 금지 (§1.6 #5 정합).
- CLAUDE.md §14 "Phase 1 진입 전 작성 금지" — "score-based 종목 우선순위 / Hot reload — Phase 1+".
- CLAUDE.md §16.1 — 의식 박제 (항목 #2 정정 시점 = 0.11.e.5 후 별도 commit, 옵션 Z 정합).
- CLAUDE.md §16.4 — Phase 1 ADR 재번호 갱신 의무 박제 (0.11.e.5 후 별도 commit).
- `docs/multi-asset-trading-system-design.md` §1.2 + §1.3 + §8.1 + §8.3 — Design Contract 원본.
- Bailey & López de Prado (2014) — Deflated Sharpe Ratio (D1 trigger 임계 검증 후보).
- López de Prado (2018) — CPCV, PBO (D5 OOS invariant 검증 후보).

### 1.10 Pre-mortem (deliberate mode 의무, 0.11.a §1.12 / 0.11.b §1.10 / 0.11.c §1.10 / 0.11.d §1.10 패턴 계승 — Round 1 ITERATE Patch 8)

**시나리오 A — Reflexive overfitting feedback loop**:
- 발생: L2 trigger evaluator 가 Phase 1 실거래 결과 (trade log) 를 backtest 입력으로 사용 → 자기 거래 결과를 학습 → overfitting spiral. Phase 1 6 개월 후 trigger 가 자기 변경 결과를 입력으로 사용하는 feedback loop 발생.
- 원인: D5 (i) "사용 절대 금지" invariant 가 enforcement test 없이 유명무실화 + file path isolation 부재.
- Mitigation: §1.6 #3 verification (`pytest -k reflexive_data_isolation`) + Phase 1 trade log file path 가 backtest input path 에 도달 불가능함을 test (`src/research/dynamic_adjustment/` 가 Phase 1 trade log directory import 차단).

**시나리오 B — 결정 누락 cascading drift**:
- 발생: L2 변경 3 회 누적 후 초기 설정 대비 parameter 가 50% 이상 변동 → baseline 의미 상실 + 추적 불가. 6 개월 후 "왜 현재 설정이 이 값인가?" 질문에 답 못함.
- 원인: D3 7+1 필드만으로는 순간 snapshot 만 보존 + cumulative drift 추적 부재.
- Mitigation: D14 `ProposalHistory` + structural constraints (minimum interval 12 주, cumulative drift bound ±30%, trigger cooldown 12 주). `pytest -k proposal_history_constraints` enforcement.

**시나리오 C — Phase 1 진입 지연 cost 폭증**:
- 발생: 본 phase sub-step 5 개 + §1.6 6 invariant enforcement test 40-60 개 + D14 structural constraints test + ralplan consensus 2 round = 코드 변경량 폭증 → Phase 1 진입 2-4 주 지연 → "Phase 1 진입 지연 비용 의식" (사용자 명시) 위반.
- 원인: R4 (HIGH) Phase 1 진입 지연 + Design Contract strictness 의 본질적 trade-off (Round 1 Architect Tension 1).
- Mitigation: R4 mitigation (D 결정 ≤ 15 개 — D14/D15 신규 후에도 15 개 한계 보존 + sub-step 5 개 이하 + 코드 변경량 budget 박제) + **본 phase 산출이 Phase 1 운영 중 evaluation infra 로 직접 활용 가능** (지연 cost 의 대칭 가치 — Phase 1 안정화 6 개월 후 promote 시 본 phase 산출이 그대로 production rings 진입).

### 1.11 Expanded Test Plan (deliberate mode 의무, 0.11.a §1.13 / 0.11.b §1.11 / 0.11.c §1.11 / 0.11.d §1.11 패턴 계승 — Round 1 ITERATE Patch 9)

본 phase 의 **CRITICAL strict** 성격 → 다른 phase 보다 정밀 + 4-tier 모두 enforcement test:

| 계층 | 대상 | 검증 항목 |
| --- | --- | --- |
| Unit | `Proposal` 도메인 객체 (4-state 머신) + `ProposalHistory` (cumulative drift + structural constraints) + `TriggerSignal` / `ProposalType` enum | (a) 4-state 머신 전이 (PENDING→APPROVED→ADR_FILED→APPLIED, REJECTED terminal) / (b) `previous_proposal_ref` chain 따라 cumulative drift 계산 정확성 / (c) minimum interval (12 주) / cumulative bound (±30%) / trigger cooldown (12 주) constraint 위반 시 proposal 생성 차단 / (d) enum exhaustive match (D8 `ProposalType` + D1 `TriggerSignal`) |
| Integration | trigger → proposal → state transition → ADR enforcement → APPLIED | (a) PENDING→APPROVED→ADR_FILED→APPLIED 전체 사이클 정상 동작 / (b) **APPROVED→APPLIED 직접 전이 = 차단** (§1.6 #4 enforcement) / (c) ADR_FILED 자동 검증 = `os.path.exists` (Critic default) / (d) ADR template scaffolding + 최소 필드 검증 |
| E2E | L2 trigger 발화 → proposal 생성 → 사람 승인 (CLI) → ADR 파일 존재 확인 → 다음 cron pickup → 변경 적용 | (a) D11 재사용 infra (ADR 0008/0009 산출 직접 호출) + Phase 0.7.3 baseline 보존 회귀 / (b) NULL proposal (sample size 부족 시) 정상 생성 + 변경 적용 zero / (c) 2 회 실행 byte-identical (D14 reproducibility) |
| Observability | §1.6 6 invariant verification + intra-research cross-import grep + figure-leak invariant (D11 호출 시 0.10.aa 패턴 계승) | (a) `pytest -k proposal_auto_apply_blocked` / `reflexive_data_isolation` / `adr_enforcement_blocks_apply` / `sell_proposal_blocked_without_adr0010` / `trigger_signal_enum_exhaustive` / `proposal_history_constraints` 6 invariant 전부 통과 / (b) `bash scripts/check_namespace.sh` intra-research grep (visualization→dgt 정방향 + dynamic_adjustment→dgt/visualization 정방향 허용, 역방향 차단) / (c) `plt.get_fignums() == []` figure-leak invariant (D11 호출 시 ADR 0006 §17.8 패턴) |

**신규 test 추가량 추정**: ~40-60 tests (unit 25 + integration 15 + e2e 10 + observability 10). 회귀 invariant: 0.11.a 1050+ tests + 0.11.b namespace 14 tests + ADR 0006 reporting layer tests + Phase 0.7.3 baseline 모두 trivially 보존 — production rings 변경 zero 자동 보장 (본 phase 산출 = `src/research/dynamic_adjustment/` 5th ring 단일 + Phase 0.7.3 baseline yaml/code 변경 zero).

---

## 2. Sub-step 0.11.e.2 ~ 0.11.e.4 진행 박제 (2026-05-14)

> 본 §2 = sub-step .2 / .3 / .4 의 실제 산출 박제. ralplan #27 Round 2
> 는 APPROVE (commit `7586337`, 2026-05-12) 시점에 종결 — 별도 amendment
> 부재. 본 §2 는 sub-step 진행 박제 형식.

### 2.1 sub-step 0.11.e.2 — 도메인 layer + trigger evaluator + namespace test

- 산출 commit: `0a4ae64`.
- 신규 파일 (5th ring `src/research/dynamic_adjustment/`):
  * `_proposal_type.py` — `_TriggerSignal` (D1, 4 variant) + `_ProposalState` (D6, 4-state + REJECTED terminal) + `_ProposalType` (D8, L2/L3 영역 3 variant — STOP_LOSS / SELL_STRATEGY_CHANGE / L1-shadow 부재 강제) StrEnum.
  * `_change_spec.py` — `_ChangeSpec` frozen + PARAMETER_CHANGE delta_pct Decimal 필수 (D14 drift 입력).
  * `_proposal.py` — `_Proposal` frozen 8 필드 + identity + `_can_transition` + `_transition` 순수 함수. State invariants 5종 (ADR_FILED/APPLIED adr_reference 필수, NULL_PROPOSAL state 제한, self-loop 차단, UTC tzinfo, 4-state 머신).
  * `_proposal_history.py` — D14 `_ProposalHistory` + 3 축 constraints (minimum_interval 12주 / cumulative_drift ±30% / trigger_cooldown 12주) + `self_trigger_chain` 추적.
  * `_trigger_evaluator.py` — D1 AND-gate + NULL proposal semantics + sample size threshold 100 + diagnostic metrics.
- 신규 tests (77 tests) — 6 파일 (test_proposal_type / test_change_spec / test_proposal / test_proposal_history / test_trigger_evaluator / test_namespace_isolation).
- 인프라 변경: `scripts/check_namespace.sh` — visualization → dynamic_adjustment 역방향 차단 + dynamic_adjustment outgoing rule (intra-self + dgt + visualization 만 허용).

### 2.2 sub-step 0.11.e.3 — CLI + ADR template scaffolding + end-to-end state machine

- 산출 commit: `d2e2feb`.
- 신규 파일:
  * `_store.py` — `_ProposalStore` JSON atomic write (Path.replace) + tagged serialization (Decimal/str/None round-trip).
  * `_adr_template.py` — `_render_adr_template` 마크다운 scaffolding (`<TBD>` placeholder) + `_verify_adr_content` content gate (file_exists + size ≥ 2000 bytes + TBD marker 부재).
  * `cli.py` — argparse 5 subcommand (`list / create-pending / approve / reject / verify-adr / apply`). APPROVED → APPLIED 직접 전이 차단 (§1.6 #4 enforcement at CLI level).
  * `__main__.py` — `python -m src.research.dynamic_adjustment` entry shim.
- 신규 tests (31 tests) — 3 파일 (test_store / test_adr_template / test_cli_e2e).
- End-to-end test: `test_full_state_machine` — `PENDING → APPROVED → ADR_FILED → APPLIED` 전체 cycle 통과 (G2 PRIMARY 충족).

### 2.3 sub-step 0.11.e.4 — §1.6 Design Contract verification + D14 + regression cross-check

- 산출 commit: `879b926`.
- 신규 tests (33 tests) — 3 파일:
  * `test_design_contract_invariants.py` (16 tests) — §1.6 6 invariant 각각의 verification command 코드화:
    - #1 `proposal_auto_apply_blocked` — APPLIED 유일 전이 경로 = ADR_FILED + config mutation surface 부재.
    - #2 `no_realtime_data_identifiers` (identifier-level grep) + `_TriggerEvaluationInput` 시그니처 OOS-only structural verification.
    - #3 `reflexive_data_isolation` — Phase 1 trade log path 부재.
    - #4 `adr_enforcement_blocks_apply` — APPROVED → APPLIED 차단 + ADR_FILED 시 adr_reference 필수.
    - #5 `trigger_signal_enum_exhaustive` — 4 variant 외 추가 시 FAIL.
    - #6 `sell_proposal_blocked_without_adr0010` — `SELL_STRATEGY_CHANGE` enum variant 부재 + ADR 0010 §3 D4 결정 박제 부재 cross-reference.
  * `test_proposal_history_constraints.py` (7 tests) — D14 3 축 통합 시나리오 + 경계값 (12 weeks / ±30%) inclusive + self-trigger chain detection.
  * `test_regression_cross_check.py` (10 tests) — Phase 0.7.3 baseline 시그니처 (`per_asset_strategy_overrides` 인자 + None default) + ADR 0006 reporting layer (`StrategyRenderer` Protocol + MarkerStyle/Panel 필드) + ADR 0009 `_VisualizationRenderer` Protocol frozen + Production rings 7 ring 의 dynamic_adjustment import 부재 + namespace script success message.

### 2.4 회귀 invariant 보존 박제 (sub-step .2 ~ .4)

- Production rings (`src/{domain,application,adapters,use_cases,cli,infrastructure,ports}/**`) 변경 zero (Phase 0.11.x invariant).
- `pyproject.toml` 변경 zero (stdlib only — argparse/json/uuid/pathlib/tempfile/re/datetime/decimal).
- `bash scripts/check_namespace.sh` exit 0 (확장 rule 포함 — dgt/visualization/dynamic_adjustment intra-research isolation 보존).
- `uv run mypy src/research/dynamic_adjustment/` Success (10 source files).
- `uv run ruff check` All checks passed.
- 1307 + 신규 141 = 1448 tests 통과 (regression zero, +0.11.b 의 1227 → 1448).

### 2.5 §1.6 Design Contract 6 invariant 측정 가능 verification 완료 박제

| # | Invariant | Verification command 코드화 | 결과 |
|---|-----------|---------------------------|------|
| 1 | 시스템은 제안만 한다 | `pytest -k proposal_auto_apply_blocked` (5 tests) | ✅ PASS |
| 2 | AI 결정 시스템 아님 | identifier grep + Input field signature OOS-only (2 tests) | ✅ PASS |
| 3 | Reflexive overfitting 방지 hard | `pytest -k reflexive_data_isolation` (2 tests) | ✅ PASS |
| 4 | ADR 박제 없이 변경 적용 차단 hard | `pytest -k adr_enforcement_blocks_apply` (3 tests) | ✅ PASS |
| 5 | 친절한 추가 금지 | `pytest -k trigger_signal_enum_exhaustive` (2 tests) | ✅ PASS |
| 6 | sell strategy 단일 가정 종속 | `pytest -k sell_proposal_blocked_without_adr0010` (2 tests) | ✅ PASS |

**모든 invariant 측정 가능 형태 박제 + tests 통과** — D15 governance "정신 supersede 불가" enforce.

---

---

## 3. Phase 0.11.e 시리즈 종료 박제 (sub-step 0.11.e.5, 2026-05-14)

> 회고 정본: `docs/retrospectives/phase-0.11.e.md` (본 commit 동시).
> 본 §3 = 게이트 판정 종합 + Design Contract 6 invariant 완성 + Phase 1
> 진입 ready 선언 + 별도 2 follow-up commit 의무 박제 + D15 governance
> 보존 확인.

### 3.1 게이트 판정 종합

ADR 0011 §1.4 G1~G4 + 회고 §3 정본 인용.

| Gate | 종류 | 결과 | 근거 commit |
|------|------|------|-------------|
| G1 (Proposal 도메인 + L2/L3 trigger evaluator) | PRIMARY | ✅ PASS | `0a4ae64` (sub-step .2, 77 tests) |
| G2 (사람 승인 워크플로우 e2e) | PRIMARY | ✅ PASS | `d2e2feb` (sub-step .3, 31 tests + full state machine cycle) |
| G3 (ADR + 회고 + Design Contract 박제) | PRIMARY | ✅ PASS | `7586337` (§1) + `879b926` (Design Contract verification) + 본 commit (§3 + 회고) |
| G4 (namespace + mypy + 재현성 + Reflexive 방지) | PRIMARY | ✅ PASS | `879b926` (sub-step .4, 33 tests — 16 Design Contract + 7 D14 + 10 regression) |

**4/4 PRIMARY PASS** — *비범한 위험성 phase* (§1.3) 의 측정 가능 박제
완성. 0.11.c (visualization) 와 동일 success pattern (코드 + tests + e2e
검증) + §1.6 Design Contract 6 invariant + D14 structural constraints +
D15 governance 의 추가 strict layer.

### 3.2 §1.6 Design Contract 6 invariant 완성 박제

본 phase 의 핵심 산출 — *측정 가능 verification* 코드화 + D15 governance
정신 supersede 불가:

| # | Invariant | Test class / 함수 | 결과 |
|---|-----------|------------------|------|
| 1 | 시스템은 제안만 한다 | `TestProposalAutoApplyBlocked` (5 tests) | ✅ PASS |
| 2 | AI 결정 시스템 아님 | `TestDataSourceConstraint` (2 tests) | ✅ PASS |
| 3 | Reflexive overfitting 방지 hard | `TestReflexiveDataIsolation` (2 tests) | ✅ PASS |
| 4 | ADR 박제 없이 변경 적용 차단 hard | `TestAdrEnforcementBlocksApply` (3 tests) | ✅ PASS |
| 5 | 친절한 추가 금지 | `TestTriggerSignalEnumExhaustive` (2 tests) | ✅ PASS |
| 6 | sell strategy 단일 가정 종속 | `TestSellProposalBlockedWithoutAdr0010` (2 tests) | ✅ PASS |

**D15 governance**: 본 6 invariant 의 *정신 (spirit) supersede 불가* —
ralplan amendment / Round 2 revision / 후속 phase 어떤 결정 라운드도
변경 X. 정신 변경 = L4 (algorithm 교체, §1.2 table) 영역 — 새 phase
시작 의무. 본 §3 박제 시점 verbatim 보존 의무.

### 3.3 D14 Structural Constraints 완성 박제

| 축 | 한계 | Test 함수 | 결과 |
|----|------|----------|------|
| Minimum interval | 12 주 (분기 1회) | `test_minimum_interval_violation_blocks_new_proposal` + boundary inclusive | ✅ PASS |
| Cumulative drift bound | ±30% 한계 | `test_cumulative_drift_bound_violation_triggers_null` + boundary inclusive | ✅ PASS |
| Trigger cooldown | 12 주 (동일 trigger) | `test_trigger_cooldown_violation_blocks_trigger` | ✅ PASS |
| Self-trigger chain | `previous_proposal_ref` 추적 | `test_cascading_history_chain_detection` | ✅ PASS |

R2 cascading 추적 책임 + D3 SRP 분리 정합 — `_Proposal` (순간 snapshot)
vs `_ProposalHistory` (cumulative + structural bound) 분리.

### 3.4 회귀 invariant cross-check 완성 박제

| 항목 | Test 함수 | 결과 |
|------|----------|------|
| Phase 0.7.3 baseline 시그니처 (`per_asset_strategy_overrides` 인자 + None default) | `test_backtest_runner_has_per_asset_strategy_overrides` | ✅ PASS |
| `BacktestRunner.__init__` 9 핵심 인자 시그니처 | `test_backtest_runner_signature_stable` | ✅ PASS |
| `BacktestResult` 11 필드 (Phase 0.10.bb 박제) | `test_backtest_result_frozen_dataclass_fields` | ✅ PASS |
| ADR 0006 `StrategyRenderer` Protocol 3 method + MarkerStyle/Panel 필드 | `TestAdr0006ReportingLayerFrozen` 3 tests | ✅ PASS |
| ADR 0009 `_VisualizationRenderer` Protocol 2 method | `test_visualization_renderer_protocol_methods` | ✅ PASS |
| Production rings 7 ring 의 dynamic_adjustment import 부재 | `test_no_dynamic_adjustment_imports_in_production_rings` | ✅ PASS |
| `check_namespace.sh` exit 0 + success message 박제 | `TestNamespaceIsolationStillPasses` 2 tests | ✅ PASS |

ADR 0011 §1.3 D12 완성 — 본 phase 산출이 0.11.a~d 의 모든 invariant 보존.

### 3.5 별도 2 Follow-up Commit 박제 의무 (R7 mitigation)

본 §3 박제 commit (sub-step 0.11.e.5) **직후** 별도 2 commit 박제 의무
— **fold 금지** (CLAUDE.md §12.1 commit 단위 원칙 + ADR 0010 §1.6 #5
옵션 Z 패턴 정합):

**(A) CLAUDE.md §16.4 ADR 0008→0012 재번호 정정 commit**:
- 현 §16.4 박제: "Phase 1 ADR 0008 (가칭)" — ADR 0008 = Phase 0.11.b
  점유 후 stale.
- 정정: "Phase 1 ADR 0012" — ADR 0012 §1 박제 (`e066554`, ralplan #28)
  정합.
- ADR 0011 §1.9 박제 의무 (R7 mitigation 정합).

**(B) CLAUDE.md §16.1 항목 #2 정정 commit**:
- 현 §16.1 항목 #2 박제: "AssetContext 기반 데이터 흐름 (코드 추가 금지)"
  — 의식 표현.
- 코드 현실 (ADR 0010 §3.7 발견): `AssetContext` 기존 구현 + 사용처
  *이미 구현*.
- 정정 시점 = 본 commit + (A) 직후 별도 commit (ADR 0010 §3.7 정정 권고
  + 옵션 Z 정합).
- 본 commit 의 별도 follow-up commit (B) 가 정본 정정 — 후속 결정 라운드
  (D3 (ii) AssetContext 후보 채택 라운드) 와 *독립* 시점.

두 commit 모두 본 §3 commit 과 **fold 금지** — commit 의미 단위 분리 +
정정 추적성 보존.

### 3.6 Phase 1 진입 Ready 선언 (ADR 0012 D16 (ii) 조건 충족)

ADR 0012 D16 (ii) 진척 (본 commit + follow-up 2 commits 완료 시점):

- **Phase 0.11.b sub-step .2~.5 실행**: ✅ 완료 (라운드 #24, 2026-05-13)
- **Phase 0.11.c sub-step .2~.5 실행**: ✅ 완료 (라운드 #25, 2026-05-13)
- **Phase 0.11.d sub-step .2~.5 실행**: ✅ 완료 (라운드 #26, 2026-05-13)
- **Phase 0.11.e sub-step .2~.5 실행**: ✅ 완료 (라운드 #27, 2026-05-14
  — 본 commit)
- **별도 2 commit** (R7 mitigation): 본 commit 직후 박제 예정 (A: §16.4
  / B: §16.1 항목 #2)
- **Paper trading**: 운영 측면 — Phase 1 진입 결정 라운드 영역
- **NTP 동기화**: 운영 측면 — Phase 1 진입 결정 라운드 영역

→ **ADR 0012 D16 (ii) 5/6 충족** (잔여 2 항목 = 운영 측면, paper trading
+ NTP). **Phase 1 진입 ready 선언** — 코드 / tests / 박제 측면 모두 완성.

### 3.7 핵심 학습 박제 (회고 §5 정본 인용)

1. **Design Contract 6 invariant 측정 가능 verification 박제 layer**
   (§5.1) — 다른 phase 의 invariant 보존보다 strict + D15 정신 supersede
   불가 governance. 본 phase 의 *비범한 위험성* mitigation core.
2. **4-state 머신 + ADR enforcement = 자동 적용 차단의 측정 가능 구현**
   (§5.2) — §1.6 #1 invariant 의 code level 강제 (APPROVED→APPLIED 직접
   전이 차단 hard).
3. **D14 Structural Constraints = 누적 cascading 차단의 정량 박제**
   (§5.3) — minimum interval / cumulative drift / trigger cooldown 3 축
   + boundary inclusive 정확성.
4. **Reflexive overfitting 방지 = file path + identifier isolation 구조적
   분리** (§5.4) — multi-asset-trading-system-design §1.2 "샘플 부족"
   risk 의 mitigation.
5. **별도 2 commit = fold 차단의 commit 단위 박제** (§5.5) — R7 mitigation
   + ADR 0010 §1.6 #5 옵션 Z 패턴 + commit 의미 단위 분리.

### 3.8 다음 Trajectory

- **즉시 다음** (사용자 결정 대기):
  - **Phase 1 직진**: ADR 0012 §1 본격 박제 + KIS API 어댑터 + paper
    trading 1~2 개월 + NTP 동기화.
  - **Phase 0.11.f (가칭)**: D3/D4 후보 채택 결정 라운드 (ADR 0010 §2.5
    진입 절차 정합). Phase 1 진입 전 결정 vs 진입 후 결정 사용자 선택.
- **Phase 2 (AI 차단기 / L1 실시간 차단기)**: Phase 1 안정화 후 진입.
  본 phase 의 L2 trigger 와 L1 책임 경계 재검토 (Architect 권고 D13
  정합).
- **D11 backtest 호출 자동화**: `_TriggerEvaluator` 가 ADR 0008
  optimization + ADR 0009 visualization comparison 산출 직접 호출 wrapper
  — 후속 sub-step.

### 3.9 회귀 invariant 보존 확인

- `src/{domain,application,adapters,use_cases,cli,infrastructure,ports}/**` 변경 zero (Phase 0.11.x invariant + §1.6 #1 measurable).
- `pyproject.toml` 변경 zero (stdlib only).
- CLAUDE.md §16.4 정정 = 별도 follow-up commit (A) (본 commit 과 fold 금지).
- CLAUDE.md §16.1 항목 #2 정정 = 별도 follow-up commit (B) (본 commit 과 fold 금지).
- CLAUDE.md §14 변경 (Phase 0.11.e 완료 박제 — 본 commit 동시).
- 기존 1307 tests + 신규 141 = 1448 tests 통과 (regression zero).
- ADR 0006/0009 Protocol frozen 보존 (D7 (iii) Registry 후보 미채택 정합).
- ADR 0010 §3 D4 결정 부재 → `ProposalType.SELL_STRATEGY_CHANGE` enum
  variant 부재 invariant 보존 (§1.6 #6).

---

**본 ADR §1 + §2 + §3 박제 완료 (Phase 0.11.e 시리즈 종료, 2026-05-14). ralplan #27 Round 1 ITERATE 14 patches 흡수 + 사용자 결정 6 정합 D14 `ProposalHistory` 정식 도출 + D15 invariant supersede 차단 governance 신규 → Round 2 APPROVE (`7586337`, 2026-05-12) → sub-step .2 도메인 + trigger evaluator + namespace test (`0a4ae64`, 2026-05-13) → sub-step .3 CLI + ADR template + e2e (`d2e2feb`, 2026-05-13) → sub-step .4 §1.6 verification + D14 + regression (`879b926`, 2026-05-14) → 회고 + §2 + §3 박제 (본 commit, 2026-05-14) → 별도 2 follow-up commits 예정 (A: §16.4 재번호 / B: §16.1 항목 #2 정정, R7 mitigation). 회귀 invariant: 기존 1307 tests + 신규 141 = 1448 total tests 모두 통과 (regression zero). Phase 0.11.e 산출 = 5th ring `src/research/dynamic_adjustment/` 10 파일 (~1403 LOC) + tests 12 파일 (141 tests). Lifecycle = 5th ring 영구 유지 (D13). **Phase 1 진입 ready 선언 — ADR 0012 D16 (ii) 5/6 충족** (잔여 = paper trading + NTP, 운영 측면). 다음: 사용자 결정 대기 (Phase 1 직진 vs Phase 0.11.f).**

---

**본 ADR §1 박제 완료 (ralplan #27 Round 1 ITERATE 14 patches → Round 2 APPROVE + R4 budget 정정 13→15, 2026-05-12). 본 phase 종료 후 Phase 1 ADR 0012 (재번호) 진입. 회귀 invariant: 0.11.a~d 의 모든 invariant 보존 + §1.6 Design Contract 6 개 invariant 측정 가능 verification 박제 (D15 governance — 정신 supersede 차단 자기참조 closure) + D14 R2 mitigation 정면 박제 (ProposalHistory + structural constraints 12 주 / ±30% / 12 주 cooldown). 핵심 risk = R1 (Reflexive overfitting) + R2 (결정 누락 cascading) — 다른 phase 보다 strict mitigation 의무 + §1.6 6 verification command (pytest -k / grep) enforceable. 0.11.e.5 회고 commit 후 **별도 2 commit (fold 금지)**: (A) CLAUDE.md §16.4 ADR 0008→0012 재번호, (B) §16.1 항목 #2 정정 (ADR 0010 §3 회고 옵션 Z 권고 정합).**