# ADR 0010 — Phase 0.11.d: Asset-Specific Strategy Differentiation — Diagnosis & Design

> **Status**: §1 박제 완료 (Round 2 close-out, 2026-05-13, ralplan #26 라운드 #26). Phase 0.11.d sub-step 0.11.d.2 진입 ready. §2 / §3 후속 박제 대기.
> **Date**: 2026-05-12 (draft) / 2026-05-12 (§1 Round 1 ITERATE 14 patches 흡수 + 옵션 Z CLAUDE.md §16.1 항목 #2 정정 시점 박제 + Architect critical finding 흡수 — `per_asset_strategy_overrides` + AssetContext 기존 구현 발견) / 2026-05-12 (§1 Round 1 APPROVED commit `af7549a`) / 2026-05-13 (§1 Round 2 close-out — Round 1 14 patches 정합 보존, 추가 amendment 없음) / `<TBD>` (§3 박제).
> **Supersedes**: 없음 (본 phase 산출 자체는 supersede 안 함). **Supersede 후보**: ADR 0003 §19.4 + ADR 0004 §7.4.2 의 "종목별 다른 정책 (자산별 다른 정책) 보류" 결정 — 본 phase 의 산출이 후속 결정 라운드에서 supersede 정당화의 근거로 사용됨.
> **Related**: CLAUDE.md §14 "Phase 1 진입 전 작성 금지 통합 목록" 의 "종목별 다른 정책 (자산별 다른 정책) — Phase 1+" / CLAUDE.md §16.1 항목 #4 "sell strategy 단일 가정" / `docs/multi-asset-trading-system-design.md` §2.2 + §3.1.
> **Execution order**: ADR 0008 (Phase 0.11.b) + ADR 0009 (Phase 0.11.c) 와 **독립 병렬** (의존성 zero — 코드 변경 zero).

---

## 1. Decision Drivers (ralplan #26 라운드 1 박제 대상)

### 1.1 사용자 spec verbatim

> "현재 구조가 멀티 자산 운용시 자산별로 전략과 전략의 세부 파라미터를 다양하게 가져갈수 있는 구조야?"
>
> 사용자 confirmation (2026-05-12): **자산별 전략 차별화는 잠재 요구사항으로 이미 존재**. (a) Phase 0.11.d 진단/설계 phase 별도 도입 trajectory 채택.

### 1.2 현재 구조의 단서 박제 (사전 진단 — 본 phase 가 정밀화 대상)

**증거 1 — CLAUDE.md §14 "작성 금지" 통합 목록**:
> "종목별 다른 정책 (자산별 다른 정책) — Phase 1+ (ADR 0003 §19.4 / ADR 0004 §7.4.2 보류)"

**증거 2 — CLAUDE.md §16.1 항목 #4 단일 sell strategy 가정 invariant**:
> "sell strategy 단일 가정 — `ProfitTargetSell` 단일 sell strategy 가정 유지. 손절 (StopLoss) 은 Phase 1 ADR 에서 sell strategy 추가 형태로 도입."

**증거 3 — `docs/multi-asset-trading-system-design.md` §2.2 + §2.3**:
설계 의도는 자산별 파라미터 분리 (KR 7%/7분할, US 8-10%/5-7분할, BTC 15-20%/5분할) + "AI 차단기가 한 자산 진입을 정지해도 다른 자산은 독립 운영". 그러나 *파라미터 자산별 차별화* ≠ *전략 객체 자산별 차별화*.

**증거 4 — Phase 0.7 ~ 0.9 회고 패턴**:
Phase 0.7.3 baseline (069500 + 132030 EQUAL) = *동일 전략 (PriceDropStrategy) 을 두 자산에 적용 + weight 만 다름*. ADR 0005 §9.6.2 "자산군 분산 = H3 충분 조건" 은 자산-weight 분산의 효과를 입증했을 뿐, 자산-전략 분산은 미검증 영역.

**증거 5 — Phase 0.11.a §8.1 한계**:
> "DGT 단독 069500 vs Phase 0.7.3 069500+132030 의 자산 1차원 차이 — 분산 효과의 분리 측정 불가."

→ 비교조차 *자산 = 전략* 짝짓기 단위로 못 함. 구조적 단서.

**증거 6 — `BacktestRunner` 의 `per_asset_strategy_overrides` 기존재 (Round 1 ITERATE Architect critical finding)**:

`src/application/backtest_runner.py:187` 에 **`per_asset_strategy_overrides: dict[str, SplitStrategyConfig] | None` 이미 존재** — 파라미터 차별화 경로가 backtest layer 에 *기존 구현*. `BacktestRunner` 가 자산별로 다른 `SplitStrategyConfig` 를 수용 가능.

**증거 7 — `AssetContext` 기존 구현 (CLAUDE.md §16.1 항목 #2 괴리)**:

`src/use_cases/asset_context.py:37-57` 의 `AssetContext` frozen dataclass 는 **이미 자산별 `strategy` + `config` + `sell_strategy` + `sell_config` 번들링**. CLAUDE.md §16.1 항목 #2 가 "AssetContext 기반 데이터 흐름 의식 (코드 추가 금지)" 으로 박제 — 그러나 실제 코드에는 `AssetContext` dataclass + `DailyOrchestrator` / `composition.py` 사용처 *이미 구현*. **CLAUDE.md §16.1 항목 #2 와 코드 현실 괴리 박제 의무** (Round 1 ITERATE Patch 2 + 11, 옵션 Z = §3 회고에 정정 권고만, 정정 commit 안 함).

**증거 8 — 정책 동일성 강제 지점**:

`src/cli/composition.py:230-243` 에서 모든 자산에 **동일 strategy 인스턴스 공유 주입** + `src/infrastructure/yaml_strategy_config_loader.py:157-201` 의 `_check_policy_uniformity` Pydantic validator 가 **정책 동일성을 yaml load 시점 강제**. 자산-전략 type 차별화의 차단 지점 = composition + validator 2 곳.

**잠정 결론 정정** (Round 1 ITERATE Patch 1, Architect critical finding 흡수):

현재 구조는 **3-layer 구조**:
- (a) Strategy *type* = **단일 공유** (composition.py:230-243 + `_check_policy_uniformity` 강제)
- (b) Strategy *config* (parameter) = **자산별 차별화 이미 가능** (`per_asset_strategy_overrides`, backtest_runner.py:187)
- (c) `AssetContext` dataclass = **이미 자산별 strategy + config + sell_strategy + sell_config 번들링 구조** (asset_context.py:37-57)

→ 원하는 구조 `{asset_i: (strategy_i, params_i)} for each i` 와의 거리는 **D3 후보별로 다름**: (i) Mapping = `per_asset_strategy_overrides` 일반화 (최소 변경) / (ii) AssetContext = composition 시점 공유 제약 해제만 필요 (`composition.py:232-243` 수정 + `_check_policy_uniformity` 완화) / (iii) Strategy Registry = renderer dispatch 와 본질 다름 (stateful position 변경 vs stateless read-only — renderer_registry.py 패턴 직접 정합 zero) / (iv) Portfolio composition = capital allocation (ADR 0003 §17) 과 결합 시 cascading 최대.

**이전 잠정 결론** ("multi-asset / single-strategy") 은 (a) layer 만 본 부정확 표현 — (b)/(c) layer 의 기존 구현 누락. 정정.

### 1.3 Decision Questions (D1~Dn, ralplan #26 박제 대상)

> 본 절은 **skeleton**. ralplan #26 Round 1 Critic/Architect 가 D 항목을 추가/병합/거부할 수 있음.

- **D1** — **현재 구조 정밀 진단의 산출 형식** — **(c) 채택 + commit hash + 절대 line range 박제 의무** (Round 1 ITERATE Patch 6, ADR 0008/0009 file:line 인용 패턴 정합):

  진단 보고서 = Markdown + 변경 surface 표 + 객체 시그니처 dump. 시그니처 dump 형식:
  - **Commit hash**: 본 phase 진단 박제 시점의 git commit hash (예: `d2112ff` — Phase 0.11.a 종료 시점, 이후 commit 발생 시 갱신).
  - **절대 경로 + 절대 line range**: 4 파일 기준
    - `src/application/backtest_runner.py:67-97` (`BacktestResult` frozen dataclass)
    - `src/application/backtest_runner.py:172-231` (`BacktestRunner.__init__` 시그니처 — `per_asset_strategy_overrides` 포함)
    - `src/use_cases/asset_context.py:37-57` (`AssetContext` frozen dataclass)
    - `src/cli/composition.py:230-243` (strategy 인스턴스 공유 주입 지점)
    - `src/infrastructure/yaml_strategy_config_loader.py:157-201` (`_check_policy_uniformity` Pydantic validator)

  (a) Markdown only 거부 사유: 후속 라운드에서 정량 근거 부족. (b) UML/diagram 거부 사유: stale risk 동일 + 도구 의존성 추가.
- **D2** — **진단 대상 객체 범위 — 5 layer 전체** (Round 1 ITERATE Architect 권고 — infra layer 추가):

  4 layer (domain / use_cases / application / cli) + **infra layer** (`src/infrastructure/yaml_strategy_config_loader.py:157-201` — 정책 동일성 강제 지점). 5 layer 전체 진단 surface 박제.
- **D3** — **자산-전략 결합 모델 4 후보 + trade-off 표** (Round 1 ITERATE Patch 3, 본 phase 는 *채택 안 함 — 후보 박제 only* invariant §1.6 #2 유지):

  | 후보 | 단순성 | 현 가정 정합 | Cascading risk | OCP | **현 코드 변경량** | 채택? |
  | --- | --- | --- | --- | --- | --- | --- |
  | (i) `Mapping[AssetId, StrategyConfig]` | ✅ 최단 | ✅ `per_asset_strategy_overrides` 일반화 | 낮음 (config 차원만) | 부분 | **최소** (`per_asset_strategy_overrides` 확장 + composition.py 1 지점) | 후보 |
  | (ii) `AssetContext` 기반 strategy 주입 | 중 | ✅ AssetContext 기존재 | 중 (composition.py 변경) | ✅ | **중간** (composition.py:232-243 단일 strategy 공유 제약 해제 + `_check_policy_uniformity` 완화) | 후보 |
  | (iii) Strategy Registry + asset-keyed lookup | 중 | ❌ renderer dispatch 와 본질 다름 (stateful position 변경 vs stateless read-only) | 중 (registry 도입) | ✅ | **큼** (신규 Registry + dispatch + asset-keyed lookup 인프라) | 후보 |
  | (iv) Portfolio-level strategy composition | 낮음 | ❌ capital allocation (ADR 0003 §17) 결합 시 cascading | 높음 (BacktestRunner.from_run 까지 영향) | ✅ | **최대** (Portfolio + Strategy 추상화 재설계) | 후보 |

  **본 phase 는 채택 안 함** (§1.6 #2 invariant). 후속 결정 라운드 (가칭 Phase 0.11.f / Phase 1 ADR 0012) 가 이 표 + cascading 영향 surface 박제 + ADR 0008 D11 결과 + ADR 0009 산출을 입력으로 사용.

  **거리 정보 박제**: (i)/(ii) 는 *기존 구현 활용* 경로 (`per_asset_strategy_overrides` 일반화 또는 `AssetContext` composition 제약 해제) — Architect 권고 패턴. (iii)/(iv) 는 *신규 인프라 도입* 경로 — cascading risk 높음.
- **D4** — **Sell strategy 차별화 범위 3 후보 + cascading 영향 surface** (Round 1 ITERATE NON-BLOCKING Architect 권고, CLAUDE.md §16.1 항목 #4 단일 sell strategy 가정 invariant 해제 시 영향):

  | 후보 | `OrderRequest` | `OrderResult` | partial fill | reconciliation | invariant 해제 비용 |
  | --- | --- | --- | --- | --- | --- |
  | (a) buy 만 차별화, sell 공유 | 변경 zero | 변경 zero | 변경 zero | 변경 zero | LOW (CLAUDE.md §16.1 항목 #4 보존) |
  | (b) buy + sell 자산별 차별화 | 시그니처 변경 (asset-specific sell strategy 식별 필드) | 시그니처 변경 (sell strategy id 박제) | partial fill 처리 시 asset 별 sell strategy lookup | reconciliation 시 sell strategy id 비교 | MEDIUM (§16.1 항목 #4 해제 + Phase 1 ADR 0012 §2 손절 정책과 동시 결정 필요) |
  | (c) sell strategy stack (ProfitTarget + StopLoss + TrailingStop 조합) | 시그니처 변경 (stack 식별) | 시그니처 변경 (어느 stack rule 발화 박제) | partial fill 시 stack 순서 보존 | reconciliation 시 stack state 비교 | HIGH (§16.1 항목 #4 + §13.3 "친절한 추가 금지" 경계, 사용자 spec 외) |

  **본 phase 는 채택 안 함 — 후보 박제 only** (§1.6 #2). (c) sell strategy stack 은 사용자 spec 외 — *후보 박제 자체* 가 §13.3 경계. ADR 본문에 *trade-off 만 박제 + 채택 후속 라운드* invariant 강조.

  **구조적 비용 vs 정책적 비용 괴리** (Round 1 ITERATE Architect tension 1): `AssetContext.sell_strategy` (`asset_context.py:55`) 이 이미 `SellStrategyPort` type → (b) 의 구조적 비용은 낮음 (`composition.py:239` `ProfitTargetSell()` 하드코딩 + `_check_policy_uniformity` `sell_strategy`/`sell_parameters` 필드 비교 완화). 그러나 *정책적* 비용 (CLAUDE.md §16.1 항목 #4 해제) 은 구조와 무관하게 높음.
- **D5** — **기존 ADR supersede 시나리오 박제 + verbatim template** (Round 1 ITERATE Patch 7):

  후속 결정 라운드 (가칭 Phase 0.11.f / Phase 1 ADR 0012) 가 ADR 0003 §19.4 + ADR 0004 §7.4.2 의 "종목별 다른 정책 (자산별 다른 정책) 보류" 결정을 supersede 할 때 인용할 verbatim template:

  **ADR 0003 §19.4 인용 정본** (`docs/decisions/0003-phase-0.7-decisions.md:2983-2993`):
  - 결정 3 = (α) 보류 — "미래 옵션 보존" 문구 + "박제 일자 박제" 정신.

  **ADR 0004 §7.4.2 인용 정본** (`docs/decisions/0004-phase-0.8-decisions.md:1588-1601`):
  - Phase 0.9 default 박제 — "PriceDropStrategy 단일 default + 자산별 정책 차별화 = Phase 0.9.x 후속 / Phase 1+ 보류".

  **Supersede 절차 template** (ADR 0008 §1.7 조건부 supersede 패턴 정합):
  1. 후속 phase 의 진단 산출 + cascading 영향 surface 박제 commit hash 인용.
  2. ADR 0003 §19.4 + ADR 0004 §7.4.2 verbatim 인용 + supersede 사유 (구체적 trigger 조건 충족).
  3. 회귀 invariant 보장: Phase 0.7.3 baseline (069500 + 132030 EQUAL + PriceDropStrategy 동일) 정확 재현 의무 — `per_asset_strategy_overrides=None` 시 single-strategy 동작이 baseline 보존 *측정 가능 형태*.
  4. CLAUDE.md §14 보존 목록의 "종목별 다른 정책" 항목 정정 + §16.1 항목 #4 invariant 해제 (D4 후보 채택 시).
- **D6** — **Portfolio-level cash management 영향 분석** (변경 없음, trade-off only): 자산별 전략 차별화 시 cash flow 비대칭 (예: DGT 빈번 매매 vs B&H 1회). Portfolio-level cash pool 공유 vs 자산별 격리 — 본 phase 는 trade-off 만 박제, 결정 안 함. 후속 라운드 영역.
- **D7** — **Backtest 호환성 16-cell 매트릭스** (Round 1 ITERATE NON-BLOCKING Patch 10):

  4 후보 (D3) × 4 reporting 객체 (Phase 0.10.bb 박제 layer) — 변경 영향 정량화 (zero / minor / major / blocker):

  | 후보 \ 객체 | `TradeView` | `DrawdownEpisode` | `StrategyRenderer` Protocol | `risk_metrics` |
  | --- | --- | --- | --- | --- |
  | (i) Mapping | zero | zero | zero | zero |
  | (ii) AssetContext | zero | zero | minor (annotation enrichment 패턴 확장 가능) | zero |
  | (iii) Registry | minor (strategy_id 표시 변경) | zero | **major** (Protocol 시그니처 변경 — strategy_id 추가) | zero |
  | (iv) Portfolio composition | **major** (TradeView 의 portfolio-level metric 재정의) | **major** (drawdown 의 portfolio-level vs asset-level 구분) | major | minor |

  **ADR 0009 comparison mode 와의 연결성**: (ii)/(iii) 채택 시 ADR 0009 `_align_results_for_overlay` 어댑터 함수가 자산별 strategy_id 분기 처리 필요 — 0.11.c.4 sub-step 산출 갱신 의무.
- **D8** — **회귀 invariant 박제** (변경 없음): 본 phase = 코드 변경 zero → 회귀 risk zero. 후속 phase 의 회귀 invariant 박제 — Phase 0.7.3 baseline (069500 + 132030 EQUAL + PriceDropStrategy 동일) 정확 재현 의무 + ADR 0003 §19 가독 의무. **측정 가능 형태**: `per_asset_strategy_overrides=None` 시 single-strategy 동작이 baseline 보존 (D1 시그니처 dump 정합).
- **D9** — **namespace 위치 — (c) `docs/retrospectives/phase-0.11.d.md` + ADR 분리 + 산출 3 분리** (Round 1 ITERATE Architect 권고):

  Phase 0.11.a pattern 정합. **산출 3 건 분리 박제 위치**:
  - **진단 보고서** (D1+D2): `docs/analysis/phase-0.11.d-current-structure-diagnosis.md` (신규 `docs/analysis/` 디렉토리 첫 박제 — D9 (a) 후보 선택적 채택).
  - **자산-전략 결합 모델 후보 박제** (D3+D4): `docs/analysis/phase-0.11.d-coupling-model-candidates.md`.
  - **회고 + 게이트 판정**: `docs/retrospectives/phase-0.11.d.md`.
  - ADR 0010 본문 = D1~D11 결정 + supersede template (D5 verbatim) + Independence Declaration + Lifecycle + Pre-mortem + Test Plan.

  거부: (b) ADR 인라인 = 길이 폭증.
- **D10** — **Lifecycle deferred reference** (변경 없음, staleness tripwire 강화): 본 phase 산출 = archive 아님 + abandon 아님 + promote 아님 — 후속 결정 라운드의 정본 입력. **Staleness tripwire**: 후속 phase 진입 시 본 ADR 의 D3~D7 후보 박제 commit hash cross-check + `git diff <commit_hash>..HEAD src/application/backtest_runner.py src/use_cases/asset_context.py src/cli/composition.py src/infrastructure/yaml_strategy_config_loader.py` 가 empty (변경 zero) 인지 확인 의무.
- **D11** — **후속 phase 트리거 조건 정량화** (Round 1 ITERATE NON-BLOCKING Architect 권고):

  본 phase 산출이 다음 조건을 *모두* 만족하면 후속 결정 라운드 (가칭 Phase 0.11.f / Phase 1 ADR 0012) 트리거:

  - **(i) 진단 보고서 사용자 명시 confirmation** — 본 ADR §3 회고 commit 시 사용자 confirmation 박제 verbatim.
  - **(ii) D3 trade-off 표 + cascading 영향 surface 박제 commit hash 박제** — `docs/analysis/phase-0.11.d-coupling-model-candidates.md` commit hash 본 ADR §3 회고에 박제.
  - **(iii) ADR 0008 D11 trigger 결과 (DGT promote 권고 or archive 확정) 가 본 phase 의 D3 후보 식별에 미치는 영향 *positive / neutral / negative* 박제**:
    - positive: DGT promote → DGT 가 특정 자산에 합류 → D3 (ii) AssetContext 후보가 positive 영향 (자산별 전략 차별화 trigger 강화).
    - neutral: DGT informational only → D3 후보 식별 영향 zero.
    - negative: DGT archive 확정 → D3 후보 식별이 *DGT 없이* 진행 (단일 PriceDropStrategy/SupportLevel 가정 유지).
  - **(iv) ADR 0009 산출 (visualization comparison mode + `_align_results_for_overlay`) 가 자산-전략 결합 시뮬레이션 입력으로 사용 가능한 상태** — `BacktestResult` ↔ `_DGTBacktestResult` 공통 축 매핑 박제 commit hash 인용.

### 1.4 Gates G1~G4 (success criterion, ralplan #26 박제 대상)

> 본 phase 는 *분석 phase 의 분석 phase* — Phase 0.11.a 의 "박제 primary, runner secondary" pattern 을 **극단까지 적용**. 모든 게이트가 박제 산출의 정합성으로만 정의됨. **실행 게이트 없음**.

- **G1 (PRIMARY)** — 현재 구조 진단 보고서 박제 (D1+D2 산출, 5 증거 + 변경 surface 표 + 객체 시그니처 dump).
- **G2 (PRIMARY)** — 자산-전략 결합 모델 후보 박제 (D3+D4 의 4+3 옵션 trade-off 표).
- **G3 (PRIMARY)** — ADR header + 회고 + supersede 시나리오 template 박제 (D5+D9).
- **G4 (PRIMARY)** — namespace CI + mypy strict (코드 변경 zero 이므로 0.11.a invariant 자동 보존) + D10 lifecycle 정의 명시.

**G2 가 INFORMATIONAL 강등 없음** — 본 phase 의 모든 산출은 박제 자체가 목적. Phase 0.11.a 의 G2 (DGT IRR baseline 비교) 같은 *실행 검증* 차원이 부재.

### 1.5 Risks (R1~Rn, ralplan #26 박제 대상)

- **R1 (HIGH)** — **진단 보고서가 박제 후 stale 되는 risk**. 본 phase 와 후속 결정 라운드 사이에 다른 phase (0.11.b/c, Phase 1) 가 코드를 변경하면 D2 의 변경 surface 가 즉시 stale. Mitigation: D10 staleness tripwire + 후속 phase 진입 시 cross-check 의무화.
- **R2 (HIGH)** — **후보 박제가 prematurely 결정으로 굳는 risk**. D3 (i)~(iv) 후보를 박제하면 후속 라운드 Critic 의 antithesis 공간이 좁아짐. Mitigation: §1.3 D3 의 "본 phase 는 채택 안 함 — 후보 박제 + trade-off 분석만" 문구를 ADR 본문에 invariant 로 박제.
- **R3 (MEDIUM)** — **Sell strategy 단일 가정 해제의 cascading risk**. CLAUDE.md §16.1 항목 #4 invariant 해제는 `OrderRequest` / `OrderResult` 시그니처, partial fill 처리, reconciliation 흐름 전반에 영향. Mitigation: D4 의 (a)~(c) 후보 각각에 대해 cascading 영향 surface 박제.
- **R4 (MEDIUM)** — **ADR 0008 (Phase 0.11.b) D11 trigger 와의 충돌**. 0.11.b 가 DGT promote 권고를 산출하면 본 phase 의 D3 후보 식별이 *이미 결정된 것*처럼 흐를 risk (DGT 가 특정 자산에 합류). Mitigation: §1.6 Independence Declaration.
- **R5 (MEDIUM) — 의미 재정의** (Round 1 ITERATE Patch 8): **composition.py:232-243 단일 strategy 인스턴스 공유 패턴 해제 시 cascading risk**. 이전 표현 "`AssetContext` 의식 → 코드 승격" 은 부정확 — `AssetContext` 는 이미 `asset_context.py:37-57` 에 구현 + `composition.py` / `DailyOrchestrator` 에서 사용 중 (증거 7 + 잠정 결론 정정). 실제 변경 surface = (a) `composition.py:232-243` 단일 strategy 인스턴스 공유 제약 해제, (b) `yaml_strategy_config_loader.py:157-201` `_check_policy_uniformity` 완화, (c) Phase 0.7.3 baseline 보존 invariant 측정 (`per_asset_strategy_overrides=None` single-strategy 동작 회귀 검증). Mitigation: §1.6 Independence Declaration + D11 트리거 + 본 phase 진단 보고서가 `AssetContext` 기존 구현 발견 명시 박제.
- **R6 (LOW)** — **회귀 invariant 박제의 자기참조**. D8 이 "Phase 0.7.3 baseline 보존" 을 박제하는데, baseline 자체가 single-strategy 가정 하의 산출 → multi-strategy 도입 시 비교 의미 자체 재정의 필요. Mitigation: D8 후속 phase 박제 대상으로 위임.
- **R7 (LOW)** — **본 ADR 의 박제 자체가 작성 금지 위반 risk**. CLAUDE.md §14 "Phase 1 진입 전 작성 금지" 목록에 "종목별 다른 정책" 이 명시되어 있음. 본 phase = *진단 + 설계 박제 only / 코드 변경 zero* 이므로 §14 위반 아님 (§13.3 "친절한 추가 금지" 정신 정합). Mitigation: §1.6 + §1.7 명시.

### 1.6 Independence Declaration (본 phase 의 정체성 박제 — 측정 가능 형태)

본 phase 의 정체성을 다른 phase 와 명시적으로 분리하기 위한 invariant + **측정 가능 verification command** (Round 1 ITERATE Patch 9 + 10):

1. **코드 변경 zero** — `src/**` 의 어떤 파일도 변경하지 않음. `docs/` + 본 ADR 만 박제.
   - **측정**: `git diff <start_commit>..<end_commit> -- src/` empty (본 phase 진입 commit ~ 0.11.d.5 회고 commit 범위).
2. **결정 안 함** — D3/D4 의 후보 박제는 *선택지 나열* + trade-off 표 + cascading 영향 surface 박제. 채택은 후속 결정 라운드의 영역.
   - **측정**: §1.3 D3/D4 본문에 "본 phase 는 채택 안 함" 문구 + trade-off 표 의 "채택?" 열에 *모든 후보 = "후보"* (default 식별 zero).
3. **다른 phase 와 의존성 zero** — Phase 0.11.b/c 의 산출과 본 phase 진행은 독립. 후속 결정 라운드는 본 phase + 0.11.b + 0.11.c 산출 모두를 입력으로 사용 가능.
   - **측정**: 본 phase sub-step 진행이 ADR 0008/0009 commit 미완료 상태에서도 가능 — `docs/decisions/0008-...` / `0009-...` commit 부재 시에도 진단 보고서 작성 가능.
4. **CLAUDE.md §14 + §16.4 보존** — 본 phase 산출은 §14 목록의 "종목별 다른 정책 — Phase 1+" 항목 + §16.4 Phase 1 ADR 0012 트리거 9 항목 supersede 하지 않음. Supersede 는 후속 결정 라운드의 영역.
   - **측정**: `git diff <start_commit>..<end_commit> -- CLAUDE.md` 의 §14 + §16.4 영역 diff = zero.
5. **CLAUDE.md §16.1 항목 #2 정정 = 옵션 Z** (Round 1 ITERATE Patch 11 사용자 결정 2026-05-12) — 본 phase 진단 보고서는 *§16.1 항목 #2 와 코드 현실 괴리 발견* 을 §1.2 증거 7 로 박제 + §3 회고에 정정 권고만 박제. **CLAUDE.md §16.1 항목 #2 본문 정정 commit 은 안 함** — 후속 결정 라운드 (가칭 Phase 0.11.f 또는 Phase 1 ADR 0012 진입) 의 영역.
   - **측정**: `git diff <start_commit>..<end_commit> -- CLAUDE.md` 의 §16.1 항목 #2 영역 diff = zero + 본 ADR §1.2 증거 7 + §3 회고 정정 권고 박제 존재.

### 1.7 Lifecycle — deferred reference

본 phase 산출 = **deferred reference**:

- archive 아님 (Phase 0.11.a D10 pattern 과 차이) — 산출이 후속 phase 의 정본 입력.
- abandon 아님 — 사용자 잠재 요구사항 (§1.1) 으로 검증됨.
- promote 아님 — 본 phase 산출은 코드가 아니므로 promote 대상 자체가 없음.

후속 결정 라운드 (가칭 Phase 0.11.e — 자산별 전략 결정 라운드, 또는 Phase 1 ADR 0008 진입 시점) 가 본 ADR 의 D3~D7 후보를 입력으로 사용 + 1 개 선택 + supersede 권고. 그 시점에 본 ADR §1.5 R1 staleness tripwire 가 작동.

### 1.8 Sub-step roadmap (D 결정 박제 완료)

| Sub-step | 본질 | 산출 (예상) |
| --- | --- | --- |
| 0.11.d.1 | D1~D11 결정 + ADR §1 박제 + roadmap | ADR 0010 §1 commit |
| 0.11.d.2 | 현재 구조 진단 보고서 작성 (D1+D2 — 8 증거 + 변경 surface 표 + 시그니처 dump + commit hash, Round 1 ITERATE Patch 1/3/6 정합) | `docs/analysis/phase-0.11.d-current-structure-diagnosis.md` |
| 0.11.d.3 | 자산-전략 결합 모델 후보 박제 (D3 4 후보 trade-off 표 + 5 dimension + D4 3 후보 cascading 영향 surface + D7 16-cell 매트릭스) | `docs/analysis/phase-0.11.d-coupling-model-candidates.md` |
| 0.11.d.4 | Supersede 시나리오 verbatim template (D5 — ADR 0003:2983-2993 + ADR 0004:1588-1601 인용) + 회귀 invariant 박제 (D8) + D11 트리거 정량화 | ADR 0010 §2 |
| 0.11.d.5 | 회고 + ADR §3 박제 + 게이트 판정 + 후속 phase 트리거 조건 (D11) + **CLAUDE.md §16.1 항목 #2 정정 권고 박제 (옵션 Z)** | `docs/retrospectives/phase-0.11.d.md` |

### 1.9 References

- ADR 0003 §19.4 — "종목별 다른 정책 보류" 박제 원본 (`docs/decisions/0003-phase-0.7-decisions.md:2983-2993` — D5 verbatim 인용 대상).
- ADR 0004 §7.4.2 — Phase 0.9 default 박제 (`docs/decisions/0004-phase-0.8-decisions.md:1588-1601` — D5 verbatim 인용 대상).
- ADR 0005 §9.6.2 — "자산군 분산 = H3 충분 조건" 핵심 학습 (`docs/decisions/0005-phase-0.9-decisions.md:1445-1454`).
- ADR 0007 (Phase 0.11.a) — 본 phase 의 "박제 primary" pattern 원본 + 5-ring namespace.
- ADR 0008 (Phase 0.11.b) — DGT 튜닝 산출 (D11 trigger positive/neutral/negative 입력) + 조건부 supersede 패턴 (`0008:158-164`) 정합.
- ADR 0009 (Phase 0.11.c) — Visualization comparison mode + `_align_results_for_overlay` (D7 16-cell 매트릭스 (iii)/(iv) 영향 입력).
- CLAUDE.md §14 "Phase 1 진입 전 작성 금지 통합 목록" — "종목별 다른 정책 (자산별 다른 정책) — Phase 1+" 항목.
- CLAUDE.md §16.1 항목 #4 "sell strategy 단일 가정" invariant.
- CLAUDE.md §16.1 항목 #2 "AssetContext 기반 데이터 흐름" — **의식 표현과 코드 현실 괴리** (증거 7, 옵션 Z 정정 권고).
- CLAUDE.md §16.4 — Phase 1 ADR 0012 트리거 9 항목 (본 phase scope 외, 후속 결정 라운드 영역).
- `src/application/backtest_runner.py:67-97,172-231` — `BacktestResult` + `BacktestRunner.__init__` (`per_asset_strategy_overrides` 기존재).
- `src/use_cases/asset_context.py:37-57` — `AssetContext` frozen dataclass (자산별 strategy + config + sell_strategy 번들).
- `src/cli/composition.py:230-243` — strategy 인스턴스 공유 주입.
- `src/infrastructure/yaml_strategy_config_loader.py:157-201` — `_check_policy_uniformity` Pydantic validator.
- `docs/multi-asset-trading-system-design.md` §2.2 자산별 파라미터 + §3.1 핵심 추상화.
- `docs/retrospectives/phase-0.11.a.md` §8.1 자산 1차원 차이 한계.

### 1.10 Pre-mortem (deliberate mode 의무, 0.11.a §1.12 / 0.11.b §1.10 / 0.11.c §1.10 패턴 계승 — Round 1 ITERATE Patch 4)

**시나리오 A — 진단 보고서가 `per_asset_strategy_overrides` 발견 누락 → D3 후보 거리 왜곡**:
- 발생: 0.11.d.2 진단 보고서 작성 시 `backtest_runner.py:187` 의 `per_asset_strategy_overrides` 기존재 누락 → D3 (i) Mapping 후보가 실제로는 *최소 변경 (`per_asset_strategy_overrides` 일반화)* 임을 모르고 *신규 인프라 도입* 으로 잘못 평가.
- 원인: 진단 surface 가 cli + application + use_cases + domain 4 layer 만 다루고 infra layer (`yaml_strategy_config_loader.py`) + backtest layer 의 `per_asset_strategy_overrides` 인자 미발견.
- Mitigation: D1 (c) 시그니처 dump 형식 = 5 layer 전체 + commit hash + 절대 line range 박제 (Patch 1 + 6). 0.11.d.2 산출 검증 = 본 ADR §1.2 증거 6/7/8 인용 정합 cross-check.

**시나리오 B — 후보 정밀 박제가 D3 (i) Mapping 으로 편향 → R2 prematurely 결정 굳음**:
- 발생: D3 trade-off 표의 "현 코드 변경량" 열에서 (i) Mapping = "최소" 로 평가되면 *후속 결정 라운드의 default 후보* 처럼 흐를 risk. 다른 후보들의 정당화 공간 좁아짐.
- 원인: 정밀 박제와 후보 박제 only invariant (§1.6 #2) 의 본질적 긴장 (Round 1 ITERATE Architect tension 2).
- Mitigation: §1.6 #2 invariant "모든 후보 = '후보' default 식별 zero" 측정 형태 명시 + D11 트리거 조건의 (iii) ADR 0008 D11 결과 영향 박제로 *외부 변수 명시* (DGT promote 시 (ii) AssetContext 가 positive 영향).

**시나리오 C — CLAUDE.md §16.1 항목 #2 괴리가 미정정 상태로 후속 phase 에 전파**:
- 발생: 옵션 Z (정정 commit 안 함) 채택 시 본 phase 진단 보고서에만 괴리 기록 → 후속 phase 가 본 ADR 미참조 시 §16.1 항목 #2 정본 그대로 *의식 (코드 추가 금지)* 으로 오인 + AssetContext 기존 구현을 *추가 금지 대상* 으로 오해.
- 원인: 옵션 Z 의 본질적 trade-off (정정 commit 회피 vs 정본 추적성).
- Mitigation: 본 ADR §3 회고에 "**Phase 0.11.f (가칭) 또는 Phase 1 ADR 0012 진입 시 CLAUDE.md §16.1 항목 #2 정정 의무**" verbatim 박제 + ADR 0011 §1.9 §16.4 정정 패턴 (별도 commit, fold 금지) 정합. 후속 phase ralplan 시 본 ADR §3 회고 인용 의무.

### 1.11 Expanded Test Plan (deliberate mode 의무 — 박제 산출 정합성 test, Round 1 ITERATE Patch 5)

본 phase = **코드 변경 zero** → 일반적 unit / integration / e2e / observability 대신 **박제 산출의 정합성 test**:

| 계층 | 대상 | 검증 항목 |
| --- | --- | --- |
| Markdown 정합성 | ADR 0010 본문 + `docs/analysis/phase-0.11.d-*.md` | (a) 모든 file:line 인용의 실제 코드와 일치 (`grep -nE "<pattern>" <file>`) + commit hash 정확성. (b) §16.1 참조가 "항목 #2 / #4" 형식 일관 (§16.1.2 / §16.1.4 부재 — Critic 발견 정합). |
| Cross-ref 통합 | ADR 0010 ↔ ADR 0007/0008/0009/CLAUDE.md | (a) ADR 0008 §1.7 D11 trigger 결과 인용이 ADR 0008 본문 (`0008:158-164`) 일치. (b) ADR 0009 `_align_results_for_overlay` 인용이 ADR 0009 §1.3 D6 본문 일치. (c) CLAUDE.md §14 + §16.4 항목이 본 ADR §1.6 #4 측정 가능 형태와 정합. |
| `_check_policy_uniformity` 상태 일치 | `yaml_strategy_config_loader.py:157-201` ↔ ADR 0010 §1.2 증거 8 | 본 phase 진입 commit ~ 종료 commit 범위에서 `_check_policy_uniformity` validator 코드 변경 zero (구조적 보장 — `git diff src/infrastructure/yaml_strategy_config_loader.py` empty). |
| Commit hash 박제 | 0.11.d.2 진단 보고서 + 0.11.d.5 회고 | (a) 진단 보고서의 commit hash 가 0.11.d.2 commit 시점 git hash 와 일치. (b) D11 트리거 조건 (ii) commit hash 박제가 0.11.d.3 산출 commit 와 일치. |

**신규 test 추가량 추정**: ~0 (코드 변경 zero — `pytest` 신규 test 부재). **회귀 invariant**: 0.11.a 1050+ tests + 0.11.b namespace 14 tests + ADR 0006 reporting layer tests + Phase 0.7.3 baseline + CLAUDE.md §14/§16 모두 trivially 보존 — `src/` + CLAUDE.md §14/§16 + §16.1 항목 #2 영역 변경 zero (§1.6 측정 가능 형태 의무).

---

## 2. Supersede Template + 회귀 Invariant + D11 트리거 정량화 (sub-step 0.11.d.4, 2026-05-13)

> 본 §2 = 후속 결정 라운드 (가칭 Phase 0.11.f / Phase 1 ADR 0012) 가 *기존
> ADR supersede 시점* 인용할 정본. 본 phase 자체는 supersede 하지 않음 —
> §1.1 박제. 본 §2 는 *템플릿 + 트리거 정량화* 만, 실행 zero.

### 2.1 Sub-step 진행 박제 (0.11.d.2 + 0.11.d.3)

| Sub-step | Commit | 산출 | 본질 |
|----------|--------|------|------|
| 0.11.d.1 | `af7549a` | ADR 0010 §1 박제 (Round 1 APPROVED) | D1~D11 결정 + ralplan #26 박제 |
| 0.11.d.1 (Round 2 close) | `cdb6e4d` (fold into .2) | Status header → "§1 박제 완료" | Round 2 close-out (option B 정합) |
| 0.11.d.2 | `cdb6e4d` | `docs/analysis/phase-0.11.d-current-structure-diagnosis.md` | 5 layer 진단 surface + 8 증거 + 시그니처 dump |
| 0.11.d.3 | `01b3867` | `docs/analysis/phase-0.11.d-coupling-model-candidates.md` | D3 4 후보 + D4 3 후보 + D7 16-cell 매트릭스 + D6 cash + D11 trigger |
| 0.11.d.4 | (본 commit) | ADR 0010 §2 박제 | D5 supersede template + D8 회귀 invariant + D11 트리거 정량화 |
| 0.11.d.5 | (예정) | 회고 + ADR §3 + CLAUDE.md §16.1 항목 #2 정정 권고 | 게이트 판정 + 후속 phase 트리거 + 옵션 Z 정정 |

회귀 invariant 보존 (sub-step .2 + .3 누적):
- `git diff <baseline>..HEAD -- src/` empty (코드 변경 zero, §1.6 #1 measurable).
- `bash scripts/check_namespace.sh` exit 0 (4 sub-steps 누적).
- 1307 tests 통과 (regression zero).

### 2.2 D5 — Supersede 시나리오 Verbatim Template

ADR 0010 §1.3 D5 정본 인용. 후속 결정 라운드 (가칭 Phase 0.11.f / Phase 1
ADR 0012) 가 ADR 0003 §19.4 + ADR 0004 §7.4.2 의 "종목별 다른 정책 (자산별
다른 정책) 보류" 결정 supersede 시 인용 정본.

#### 2.2.1 ADR 0003 §19.4 정본 verbatim (`docs/decisions/0003-phase-0.7-decisions.md:2983-2993`)

```
### 19.4 결정 3 = (α) — §14.7 γ (자산별 다른 정책) Phase 0.8+ 후속 보류

| 옵션 | 채택 | 근거 |
|---|---|---|
| **(α) Phase 0.8+ 후속 보류** | **채택** | 가장 안전 — 미래 옵션 보존. Phase 0.8/0.9 진입 시 1 차원 변경 정신과의 정합성 재검토 |
| (β) Phase 0.7 종료 시 명시 거부 | 거부 | strict — 미래 변경 차단 |
| (γ) Phase 0.8 진입 ADR 시 검토 | 거부 | (α) 와 사실상 동일이지만 시점 명시화는 미박제 placeholder 으로 충분 |

채택 효과 — §14.7 γ placeholder 보존. Phase 0.8 (지지선) / Phase 0.9
(개별 주식) / Phase 1 (실거래) 진입 ADR 박제 시 정책 동일성 vs 자산별
정책 trade-off 재검토 가능.
```

**Supersede 사유 박제 형식** (후속 라운드 입력):
- 본 §19.4 (α) "Phase 0.8+ 후속 보류" 결정 = Phase 0.7 종료 시점 (2026-05-05) 박제.
- Supersede 정당화: Phase 0.7.3 baseline (069500+132030 EQUAL, PriceDropStrategy 동일) 이후 Phase 0.8/0.9/0.10/0.11.a/0.11.b/0.11.c/0.11.d 진단 + 후보 박제 산출 누적 → "1 차원 변경 정신 정합성 재검토" 조건 충족 박제 시.
- 본 §2 본문은 supersede 명시 안 함 — *템플릿 only* (§1.1 invariant).

#### 2.2.2 ADR 0004 §7.4.2 정본 verbatim (`docs/decisions/0004-phase-0.8-decisions.md:1588-1601`)

```
#### 7.4.2 (사용자 §3.2) Phase 0.9 진입 default

| 항목 | Phase 0.9 default | 근거 |
|---|---|---|
| **buy_strategy** | **PriceDropStrategy** | 검증된 가치 (Phase 0.7.3 게이트 3/3 strict PASS + Phase 0.8.1 H3 우월). 박영옥 원전 정신 직접 매핑 |
| **SupportLevelStrategy** | **보존** (Phase 0.9.x 결합 검토 가능) | 매수 패러다임 비교 결과 코드 + ADR 보존. 폐기 아님 (Phase 0.8.x 가 아닌 Phase 0.9.x 후속 결합 검토 가능) |
| **자산** | **개별 주식** (3 ~ 5 종목) | Phase 0.9 본질 — ETF → 개별 주식 차원 변경 |
| 자본 배분 | EQUAL default | Phase 0.7.2 §17.4 학습 — 정책-자산 부정합 회피 |
| 매도 정책 | profit_target=10% / max_sells=7 | Phase 0.7.3 + Phase 0.8.1 baseline |
| 재진입 정책 | F (HybridTimeBasedReentry, cooldown=60) | Phase 0.7.3 baseline |
| 게이트 | Phase 0.7.3 baseline strict (H1=0.3270 / H2=13.2280 / H3=0.5255) 그대로 | 변수 통제 strict |

본 §7.4.2 = Phase 0.9 진입 시점의 default 박제 (Phase 0.9 본격 ADR 0005
박제 시 변경 가능, 박제 일자 박제).
```

**Supersede 사유 박제 형식** (후속 라운드 입력):
- 본 §7.4.2 = Phase 0.9 진입 시점 (2026-05-06) default 박제 — *PriceDropStrategy 단일 default* + *SupportLevelStrategy 보존* (Phase 0.9.x 결합 검토 영역).
- Supersede 정당화: Phase 0.11.d.2 진단 보고서 + 0.11.d.3 후보 박제 + ADR 0008 D11 trigger 결과 + ADR 0009 visualization comparison 산출 + 사용자 명시 confirmation 합치.
- 자산별 다른 정책 차별화 채택 시 본 §7.4.2 의 "buy_strategy: PriceDropStrategy 단일" + "SupportLevelStrategy 보존 (Phase 0.9.x 결합 검토)" → "자산별 strategy mapping" 으로 supersede.

#### 2.2.3 Supersede 절차 Template (ADR 0008 §1.7 조건부 supersede 패턴 정합)

후속 결정 라운드가 본 supersede 시 다음 4 단계 의무 박제:

1. **선행 산출 commit hash 인용**:
   - 본 ADR 0010 §1+§2+§3 commit hash.
   - sub-step 0.11.d.2 진단 보고서 commit (`cdb6e4d`).
   - sub-step 0.11.d.3 후보 박제 commit (`01b3867`).
   - ADR 0008 D11 trigger 결과 commit (현 = neutral, 2026-05-13).
   - ADR 0009 visualization comparison 산출 commit (`3af0415`).

2. **Verbatim 인용 + supersede 사유**:
   - ADR 0003 §19.4 (lines 2983-2993) verbatim 인용 (§2.2.1 정합).
   - ADR 0004 §7.4.2 (lines 1588-1601) verbatim 인용 (§2.2.2 정합).
   - Supersede 사유 = *구체적 trigger 조건 충족 박제* (D11 4 조건 §2.4 정합).
   - 사용자 명시 confirmation verbatim (이전 ralplan 라운드 박제).

3. **회귀 invariant 보장** (§2.3 정합):
   - Phase 0.7.3 baseline (069500 + 132030 EQUAL + PriceDropStrategy 동일) 정확 재현 의무.
   - 측정 가능 형태: `per_asset_strategy_overrides=None` 시 single-strategy 동작 baseline 보존.

4. **CLAUDE.md 정정** (옵션 Z 정합):
   - §14 보존 목록의 "종목별 다른 정책" 항목 정정 (Phase 1+ → 채택 phase).
   - §16.1 항목 #4 invariant 해제 (D4 후보 채택 시).
   - §16.1 항목 #2 "AssetContext 의식 (코드 추가 금지)" 정정 — sub-step 0.11.d.2 §3 증거 7 정합.
   - 정정 commit 은 supersede 결정 라운드와 동일 commit (fold) OR 별도 commit — 후속 ralplan 시 결정.

### 2.3 D8 — 회귀 Invariant 박제

ADR 0010 §1.3 D8 정본 — 후속 phase 자산별 차별화 도입 시 회귀 invariant.

#### 2.3.1 측정 가능 형태 (Phase 0.7.3 baseline 보존)

후속 phase (자산별 차별화 채택) 진입 시 다음 조건 *측정 가능 형태* 의무:

```python
# 회귀 검증 의사 코드 (후속 phase 의 test suite 박제 의무):
def test_phase_073_baseline_regression():
    """Phase 0.7.3 baseline 정확 재현 — per_asset_strategy_overrides=None."""
    # 069500 + 132030 EQUAL + PriceDropStrategy 동일 + 5y window.
    result = BacktestRunner(
        assets=[asset_069500, asset_132030],
        strategy_config=phase_073_default_config,
        initial_capital=Money(amount=Decimal("100_000_000"), currency=Currency.KRW),
        ohlcv_by_asset=...,
        per_asset_strategy_overrides=None,  # ← single-strategy 동작 보존
    ).run(date(2020, 1, 2), date(2024, 12, 30))
    # Phase 0.7.3 baseline 정본 4 metric (ADR 0003 §18.12.1):
    assert result.cagr_pct == Decimal("2.5779")  # ± tolerance
    assert result.max_drawdown_pct == Decimal("-8.2737")
    assert result.sharpe_ratio == Decimal("0.5255")
    assert result.calmar_ratio == Decimal("0.3116")
```

#### 2.3.2 회귀 surface — 5 파일 변경 zero 검증

후속 phase 진입 시 본 ADR 0010 박제 시점 (commit `01b3867`) 부터의 변경
zero 검증:

```bash
# 5 surface 파일 변경 없음 — 변경 시 §1 진단 line range 갱신 의무
git diff 01b3867..HEAD -- \
    src/application/backtest_runner.py \
    src/use_cases/asset_context.py \
    src/cli/composition.py \
    src/infrastructure/yaml_strategy_config_loader.py \
    src/ports/strategy_renderer.py
```

empty 시 본 ADR §1 진단 + §2 supersede template 정합 유지. non-empty 시
영향 surface 재진단 의무 (ADR 0010 §1.3 D10 staleness tripwire).

#### 2.3.3 D8 self-reference paradox 명시 (R6 정합)

ADR 0010 §1.5 R6 LOW 정합 — "회귀 invariant 박제의 자기참조":
- Phase 0.7.3 baseline 자체가 single-strategy 가정 하의 산출.
- 자산별 차별화 도입 시 baseline 비교의 *의미 자체* 재정의 필요 (자산별 다른 strategy 동작은 baseline 과 *질적으로 다름*).
- Mitigation (본 §2.3): `per_asset_strategy_overrides=None` 조건부 baseline 재현 — *option 제공* 만 회귀 invariant. 자산별 차별화 *활성화* 시점의 비교는 후속 phase 의 신규 baseline 박제 영역.

### 2.4 D11 — 후속 Phase 트리거 4 조건 정량화

ADR 0010 §1.3 D11 정본 — 후속 결정 라운드 (가칭 Phase 0.11.f / Phase 1
ADR 0012) 트리거 조건 4 항목. 본 §2.4 = *정량 측정 가능 형태* 박제.

#### 2.4.1 (i) — 진단 보고서 사용자 명시 confirmation

**조건**: 본 ADR §3 회고 commit 시 사용자 confirmation verbatim 박제.

**측정 가능 형태**:
- ADR 0010 §3 회고 본문에 사용자 confirmation 인용 (예: "Phase 0.11.d 진단 산출 수락 + 자산별 차별화 trajectory 진입 권한 부여" verbatim).
- 부재 시 후속 라운드 진입 차단.

#### 2.4.2 (ii) — D3 trade-off 표 + cascading 영향 surface 박제 commit hash

**조건**: `docs/analysis/phase-0.11.d-coupling-model-candidates.md` commit hash 본 ADR §3 회고에 박제.

**측정 가능 형태**:
- commit `01b3867` (sub-step 0.11.d.3) 박제 — ADR 0010 §3 회고에 verbatim 인용 (§2.1 표 정합).
- D3 4 후보 + D4 3 후보 + D7 16-cell 매트릭스 부재 시 후속 라운드 input 미충족.

#### 2.4.3 (iii) — ADR 0008 D11 trigger 결과별 D3 후보 식별 영향

**조건**: ADR 0008 D11 trigger 결과 (DGT promote / archive / informational) 가 본 phase D3 후보 식별에 미치는 영향 박제.

**현 상태** (2026-05-13, ADR 0008 §3 정본):
- ADR 0008 D11 trigger = **INFORMATIONAL FAIL** (DSR 0/95 구조적 unattainable, D10 archive 확정).
- → **neutral** path (sub-step 0.11.d.3 §6.2 정합) — DGT 후보 식별 영향 zero.
- → D3 후보 식별은 PriceDropStrategy + SupportLevelStrategy 2 strategy 가정 하에 진행.

**측정 가능 형태**:
- 후속 라운드 진입 시점에 ADR 0008 D11 결과 재확인 + sub-step 0.11.d.3 §6 표 (positive / neutral / negative) cross-reference.
- 재확인 결과가 *현 상태 변경* 시 (예: Phase 1 ADR 0012 D11 분봉 DGT 재검토 결과 positive 전환) sub-step 0.11.d.3 §6 정정 의무 + D3 후보 식별 영향 재평가.

#### 2.4.4 (iv) — ADR 0009 산출 가용 상태

**조건**: ADR 0009 산출 (`_align_results_for_overlay` + visualization comparison mode) 가 자산-전략 결합 시뮬레이션 입력으로 사용 가능 상태.

**현 상태** (2026-05-13, ADR 0009 §3 정본):
- ADR 0009 §1+§2+§3 박제 완료 (Phase 0.11.c 종료, 게이트 4/4 PRIMARY PASS).
- commit `3af0415` (sub-step 0.11.c.4) — `_align.py` + `_comparison.py` + `cli.py` 산출.
- → 자산-전략 결합 시뮬레이션 입력 가용 (sub-step 0.11.d.3 §4.3 영향 정합).

**측정 가능 형태**:
- 후속 라운드 진입 시점에 ADR 0009 산출 commit hash 재확인 + sub-step 0.11.d.3 §4.3 연결성 표 cross-reference.
- ADR 0009 `_align.py` strategy_id 분기 처리 가능 여부 검증 (D3 (ii)/(iii) 채택 시 ADR 0009 산출 갱신 의무).

#### 2.4.5 AND-gate 조건 종합

**트리거 조건 AND-gate**: (i) AND (ii) AND (iii) AND (iv) **모두** 충족 시 후속
결정 라운드 진입 가능. 일부 미충족 시 후속 라운드 차단.

| 조건 | 현 상태 (2026-05-13) | 충족? |
|------|---------------------|------|
| (i) 사용자 confirmation | 본 ADR §3 회고 commit 시 박제 예정 | 미충족 (sub-step .5 영역) |
| (ii) 후보 박제 commit hash | `01b3867` 박제 완료 | ✅ 충족 |
| (iii) ADR 0008 D11 결과 | INFORMATIONAL FAIL → neutral path | ✅ 충족 (neutral path 박제) |
| (iv) ADR 0009 산출 가용 | commit `3af0415` 산출 완료 | ✅ 충족 |

**현 상태 종합**: 3/4 충족 (sub-step 0.11.d.4 시점). (i) = sub-step
0.11.d.5 회고 commit 시 충족 가능 — 후속 라운드 진입 자격 완성은 본 phase
종료 후.

### 2.5 후속 결정 라운드 진입 절차 박제

D11 AND-gate 4 조건 모두 충족 후 후속 결정 라운드 (가칭 Phase 0.11.f /
Phase 1 ADR 0012) 진입 시 절차:

1. **선행 산출 commit hash 인용 검증**: ADR 0010 §1+§2+§3 + sub-step 0.11.d.2/.3 + ADR 0008 §3 + ADR 0009 §3 commit hash 박제 + cross-reference.
2. **D11 AND-gate 4 조건 재확인**: §2.4.5 표 verbatim 인용 + 재확인 결과 박제.
3. **D3 후보 선택**: §1.3 D3 + sub-step 0.11.d.3 §2 + §6 (ADR 0008 D11 path) cross-reference 후 사용자 명시 채택.
4. **D4 후보 선택**: §1.3 D4 + sub-step 0.11.d.3 §3 cross-reference 후 채택 (구조적 비용 vs 정책적 비용 괴리 §3.4 인용).
5. **회귀 invariant 활성화**: §2.3 측정 가능 형태 채택 + `per_asset_strategy_overrides=None` baseline 재현 test suite 박제.
6. **Supersede 박제**: §2.2 D5 template 정합 — ADR 0003 §19.4 + ADR 0004 §7.4.2 verbatim 인용 + supersede 사유 박제.
7. **CLAUDE.md 정정**: §14 + §16.1 항목 #2 / #4 정정 (옵션 Z 정합).

**진입 차단 조건** (R1 staleness tripwire 발동):
- §1.5 R1 (HIGH) 정합: 본 ADR 박제 후 5 surface 파일 변경 (`git diff 01b3867..HEAD -- src/` non-empty) 시 진단 stale → sub-step 0.11.d.2/.3 재진단 의무 후 후속 라운드 진입.
- 후속 phase (예: Phase 1 ADR 0012 §2 진입) 가 surface 파일 변경 후 본 ADR 0010 §1+§2 미참조 시 *strict 차단* — ralplan 의무화.

---

> 박제 의도: 후속 결정 라운드가 ADR 0003 §19.4 (`0003:2983-2993`) + ADR 0004 §7.4.2 (`0004:1588-1601`) 보류를 supersede 할 때 인용할 verbatim template + Phase 0.7.3 baseline 보존 invariant + ADR 0008 D11 결과 positive/neutral/negative 박제.

---

## 3. Phase 0.11.d 시리즈 종료 박제 (sub-step 0.11.d.5, 2026-05-13)

> 회고 정본: `docs/retrospectives/phase-0.11.d.md` (본 commit 동시).
> 본 §3 = 게이트 판정 종합 + D11 AND-gate 4 조건 최종 충족 박제 + 사용자
> confirmation verbatim + 후속 trajectory + CLAUDE.md §16.1 항목 #2 정정
> 권고 (옵션 Z).

### 3.1 게이트 판정 종합

ADR 0010 §1.4 G1~G4 + 회고 §3 정본 인용.

| Gate | 종류 | 결과 | 근거 |
|------|------|------|------|
| G1 (현재 구조 진단 보고서 박제) | PRIMARY | ✅ PASS | `docs/analysis/phase-0.11.d-current-structure-diagnosis.md` (sub-step .2, commit `cdb6e4d`, 380 lines) — 5 layer × 8 증거 × 시그니처 dump |
| G2 (자산-전략 결합 모델 후보 박제) | PRIMARY | ✅ PASS | `docs/analysis/phase-0.11.d-coupling-model-candidates.md` (sub-step .3, commit `01b3867`, 398 lines) — D3 4 후보 + D4 3 후보 + D7 16-cell + D6 + D11 trigger |
| G3 (ADR header + 회고 + supersede template 박제) | PRIMARY | ✅ PASS | ADR §1 (Round 1+2) + §2 (D5+D8+D11) + §3 (본 commit) + 회고 |
| G4 (namespace + mypy + D10 lifecycle) | PRIMARY | ✅ PASS | check_namespace.sh exit 0 + mypy Success + `git diff 38da6bf..HEAD -- src/` empty + 1307 tests 통과 |

**4/4 PRIMARY PASS** — Phase 0.11.c 와 동일 패턴 (infrastructure / diagnosis
phase 본질 정합). 0.11.c (코드 +1535 / tests +84) vs 0.11.d (코드 zero /
tests zero) — 박제 산출만으로 게이트 4/4 PASS 의 극한 적용.

### 3.2 D11 AND-gate 4 조건 최종 충족 박제

ADR 0010 §1.3 D11 + §2.4.5 정합. sub-step 0.11.d.5 commit 시점 최종 상태:

| 조건 | 현 상태 (2026-05-13) | 충족? | 박제 위치 |
|------|---------------------|------|----------|
| (i) 사용자 명시 confirmation | 사용자 verbatim ("Phase 0.11.d sub-step .2 진입 (ADR 0010 §1 박제 후) — Asset-Specific Strategy Diagnosis" + 4 sub-step 진행 confirmation) | ✅ 충족 | §3.3 + 회고 §1 |
| (ii) 후보 박제 commit hash | `01b3867` (sub-step .3 — D3 4 후보 + D4 3 후보 + D7 16-cell) | ✅ 충족 | §2.1 표 + 회고 §2 |
| (iii) ADR 0008 D11 결과 | INFORMATIONAL FAIL → neutral path (sub-step .3 §6.2 정합) | ✅ 충족 | sub-step .3 §6 |
| (iv) ADR 0009 산출 가용 | `3af0415` (sub-step 0.11.c.4 — `_align.py` + `_comparison.py` + `cli.py`) | ✅ 충족 | sub-step .3 §4.3 |

**최종 종합**: **4/4 충족** — 후속 결정 라운드 (가칭 Phase 0.11.f / Phase 1
ADR 0012) **진입 자격 완성**. ADR 0010 §2.5 진입 절차 7 단계 박제 정합.

### 3.3 사용자 Confirmation Verbatim 박제

본 phase 진행 중 사용자 명시 verbatim (D11 (i) 정합):

> "Phase 0.11.d sub-step .2 진입 (ADR 0010 §1 박제 후) — Asset-Specific Strategy Diagnosis"
> (2026-05-13, Phase 0.11.c 종료 직후 trajectory 선택 시점)

후속 sub-step 진행 confirmation (각 sub-step entry 시점):
- sub-step .2 → "b" (option B 선택, Round 2 close-out 방식)
- sub-step .3 → "네 진행해줘"
- sub-step .4 → "네 진행해줘"
- sub-step .5 → "네 진행해줘"

→ 본 phase 진단 + 후보 박제 + supersede template + D11 정량화 + 시리즈
종료 박제 모두 사용자 명시 confirmation 하에 진행. **D11 (i) 조건 충족**.

### 3.4 핵심 학습 박제 (회고 §5 정본 인용)

1. **"박제 primary" pattern 의 극한 적용** (§5.1) — 코드 변경 zero / 신규 test zero 로 게이트 4/4 PASS. *분석 phase 의 분석 phase* 의 측정 가능 정합성 입증.
2. **Architect critical finding — 기존 구현 발견의 가치** (§5.2) — `per_asset_strategy_overrides` + `AssetContext` 기존재 발견. 진단 phase 의 핵심 가치 = "구조는 있지만 게이트로 차단" 패턴 정밀화.
3. **CLAUDE.md §16.1 항목 #2 의식/현실 괴리 박제** (§5.3) — "AssetContext 의식 (코드 추가 금지)" vs 코드 현실 ("이미 구현"). 옵션 Z 정합 — 정정 commit 안 함, 권고만.
4. **구조적 비용 vs 정책적 비용 괴리** (§5.4 / Architect tension 1) — D4 (b) 의 본질적 긴장. 두 비용 차원 분리 평가의 필수성.
5. **AND-gate 4 조건 정량화 = 명확한 trigger** (§5.5) — "언제 다시 다룰까?" 모호함 제거. 후속 결정 라운드 입력 정본 5 점 박제.

### 3.5 다음 Trajectory

- **즉시 다음** (사용자 결정 대기):
  - **Phase 0.11.e** = Dynamic Adjustment (ADR 0011 §1 박제 대상). 본 phase 산출과 독립.
  - **Phase 0.11.f** = D3/D4 후보 채택 결정 라운드 (본 ADR §2.5 진입 절차 의무 박제). ADR 0008/0009 정합 필요 + 사용자 명시 final confirmation.
- **Phase 1 ADR 0012 D11 분봉 DGT 재검토 영향** = 본 phase sub-step .3 §6 cross-reference 의무 (분봉 결과 positive 전환 시 D3 후보 식별 재평가).
- **ADR 0012 D16 (ii) 진척**: Phase 0.11.d 완료 → 9/16 → 13/16.

### 3.6 회귀 Invariant 보존 확인

- `src/{domain,application,adapters,use_cases,cli,infrastructure,ports}/**` 변경 zero (Phase 0.11.x invariant — §1.6 #1 measurable).
- `pyproject.toml` 변경 zero.
- CLAUDE.md §16 변경 zero (Phase 0.11.b 와 동일 — registry 미합류 정합).
- CLAUDE.md §14 변경 (Phase 0.11.d 완료 박제 — 본 commit 동시 갱신).
- 기존 1307 tests 모두 통과 (Phase 0.11.c 종료 commit `38da6bf` 시점 invariant 보존, regression zero).
- ADR 0006 `StrategyRenderer` Protocol frozen 보존 (D7 (iii) Registry 후보 채택 시 영향 — 본 phase 박제 only).
- ADR 0009 `_VisualizationRenderer` Protocol frozen 보존 (D7 (ii) AssetContext 후보 채택 시 minor 영향 — 본 phase 박제 only).

### 3.7 CLAUDE.md §16.1 항목 #2 정정 권고 박제 (옵션 Z 정합)

ADR 0010 §1.10 시나리오 C mitigation 정합 — 옵션 Z 채택 (정정 commit 안 함, 권고만).

**정정 권고 박제 (회고 §8 정본 인용)**:
- 현 §16.1 항목 #2: "AssetContext 기반 데이터 흐름 (코드 추가 금지)" — *의식* 표현.
- 코드 현실: `src/use_cases/asset_context.py:37-57` AssetContext + `composition.py:230-243` 사용처 *이미 구현*.
- **정정 시점**: 후속 결정 라운드 (D3 (ii) AssetContext 후보 채택 시) 와 동일 commit 영역.
- **정정 commit 본 phase 영역 아님** — supersede 결정과 단일 commit fold 권장.

**제안 정정 문구** (회고 §8.3 verbatim, 후속 라운드 채택 시 참조):

> 2. **종목별 잔고 분리 의식** — 단일 kill switch 가정 유지하되, 자산 격리
>    정지 분기 가능. `AssetContext` (`src/use_cases/asset_context.py:37-57`)
>    기존 구현 활용 path: `composition.py:230-243` 단일 strategy instance
>    broadcast 해제 + `yaml_strategy_config_loader.py:157-201`
>    `_check_policy_uniformity` 완화. ADR 0010 §1.3 D3 (ii) AssetContext
>    후보 정합 (sub-step 0.11.d.3, commit `01b3867`).

후속 phase ralplan 시 본 ADR §3.7 + 회고 §8 인용 의무 (Pre-mortem 시나리오 C mitigation).

---

**본 ADR §1 + §2 + §3 박제 완료 (Phase 0.11.d 시리즈 종료, 2026-05-13). ralplan #26 Round 1 ITERATE 14 patches 흡수 → Round 1 APPROVED (`af7549a`, 2026-05-12) → Round 2 close-out fold into sub-step .2 (`cdb6e4d`, 2026-05-13) → sub-step .2 진단 보고서 (`cdb6e4d`) → sub-step .3 후보 박제 (`01b3867`) → sub-step .4 ADR §2 박제 (`1fa12a6`) → 회고 + §3 박제 (본 commit, 2026-05-13). 회귀 invariant: 0.11.c 종료 commit `38da6bf` 시점 1307 tests 모두 통과 (regression zero, 코드 변경 zero — §1.6 #1 measurable 충족). Phase 0.11.d 산출 = `docs/decisions/0010` + `docs/analysis/` 신규 2 파일 + `docs/retrospectives/phase-0.11.d.md` = ~1711 docs lines (코드 변경 zero). D11 AND-gate 4/4 충족 — 후속 결정 라운드 진입 자격 완성. Lifecycle = deferred reference (후속 결정 라운드의 정본 입력). 다음: Phase 0.11.e (Dynamic Adjustment, ADR 0011) 또는 Phase 0.11.f (D3/D4 후보 채택 결정 라운드) — 사용자 결정 대기.**