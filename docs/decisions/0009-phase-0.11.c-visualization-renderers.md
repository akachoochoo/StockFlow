# ADR 0009 — Phase 0.11.c: Strategy-Specific Backtest Visualization Renderers

> **Status**: §1+§2+§3 박제 완료 (Phase 0.11.c 시리즈 종료, 2026-05-13, sub-step 0.11.c.5). 게이트 4/4 PRIMARY PASS. Lifecycle = permanent (§1.7).
> **Date**: 2026-05-12 (draft) / 2026-05-12 (§1 Round 1 ITERATE 14 patches + 4 추가 정정 흡수 + D1=(a) `src/research/visualization/` 사용자 결정) / 2026-05-12 (§1 Round 2 APPROVE + D8 Registry parenthetical 추가) / 2026-05-13 (§2 sub-step .2~.4 진행 박제 + §3 시리즈 종료 박제).
> **Supersedes**: 없음.
> **Related**: ADR 0007 §1.6 (5-ring namespace), ADR 0008 (Phase 0.11.b — 본 phase 의 선행).
> **Execution order**: ADR 0008 (Phase 0.11.b) 완료 후 진입 (사용자 confirmation 2026-05-12).

---

## 1. Decision Drivers (ralplan #25 라운드 1 박제 대상)

### 1.1 사용자 spec verbatim

> "백테스트 하고 전 기간에 걸쳐서 매수 매도 한 시점 그리고 그리드 상단 하단, 누적 수익, 등을 표시하고 전략 마다 비교해보고 싶은데 가능할까? 이 경우 전략마다 렌더러가 따로 존재 해야 할것 같은데"
>
> **사용자 직관 박제**: 전략별 렌더러 분리 = 정확한 설계 방향. ralplan #25 에서 anti-pattern (단일 거대 렌더러) 거부 정당화 필요.

### 1.2 도메인 분석 — 왜 strategy-specific 렌더러가 필요한가

| 전략 | 공통 요소 | 전략-specific 요소 |
| --- | --- | --- |
| Buy-and-Hold | 가격 곡선 + 매수 시점 + 누적 수익 + drawdown | (없음) |
| 세븐 스플릿 | 동상 | 분할 매수 가격 라인 (7개) + 각 split 의 매도 시점 + 잔여 split 수 + split 별 P&L |
| DGT (Phase 0.11.a/b) | 동상 | 시간 가변 grid 상단/하단 envelope + reference price 곡선 + grid level 별 trade marker |
| (후속) Score-based | 동상 | 종목 score 히트맵 + 리밸런싱 시점 |
| (후속) 부동산 분산 | 동상 | 자산군별 weight stack plot |

→ 공통 요소는 strategy-agnostic, 전략-specific 요소는 전략 도메인 지식 의존. 단일 렌더러로 합치면 (a) cyclomatic complexity 폭발, (b) 새 전략 추가 시 기존 코드 수정 (OCP 위반), (c) test 매트릭스 곱셈.

### 1.3 Decision Questions (D1~Dn, ralplan #25 박제 대상)

> 본 절은 **skeleton**. ralplan #25 Round 1 Critic/Architect 가 D 항목을 추가/병합/거부할 수 있음.

- **D1** — Namespace 위치 — **(a) `src/research/visualization/` 채택** (사용자 결정 2026-05-12, Round 1 ITERATE Patch 10):

  | 후보 | Ring 변경 | 기존 패턴 정합 | DGT 접근성 | B&H/7split 접근성 | 채택? |
  | --- | --- | --- | --- | --- | --- |
  | **(a)** `src/research/visualization/` | 5-ring 유지 | 0.11.a 패턴 (`src/research/dgt/`) 정합 | import OK (intra-research, D9 정방향) | import OK (outer→inner read) | **✅ default** |
  | (b) `src/visualization/` 6-ring 신설 | CLAUDE.md §1.1 architecture 모델 갱신 의무 | 패턴 단절 | import OK | import OK | ❌ |
  | (c) `src/adapters/visualization/` | 4-ring 내 (production ring 변경) | 0.11.x "production rings 변경 zero" invariant 파기 | import 불가 (inner→outer read 금지) | import OK | ❌ |
  | (d) `src/research/dgt/visualization/` | 5-ring 유지 (DGT 종속) | DGT 단독 | 직접 접근 | 접근 불가 — strategy-agnostic 본질 거부 | ❌ |

  **(b) 거부 사유**: 6-ring 신설은 architecture invariant supersede — 과도. **(c) 거부 사유**: production ring 변경 = 0.11.x 시리즈 invariant (ADR 0007 §1.6 + ADR 0008 §1.6 상속) 위반. **(d) 거부 사유**: DGT 종속 → B&H/7split comparison 접근 불가 + 사용자 spec "전략 마다 비교" 직접 모순.
- **D2** — Visualization architecture pattern — **(d) ADR 0006 §4 `StrategyRenderer` Protocol 동결 + 신규 `VisualizationRenderer` Protocol 별도 정의** (Round 1 ITERATE Patch 1, ADR 0006 §4 무자각 재발명 차단):

  | 대안 | 패턴 | OCP | 기존 `StrategyRenderer` 정합 | 채택? |
  | --- | --- | --- | --- | --- |
  | (a) Strategy + Template Method (`BaseRenderer` subclass) | 상속 트리 | ✅ | ❌ — Protocol+Registry 와 패러다임 충돌 (duck typing vs inheritance) | ❌ |
  | (b) Composition (renderer = list of plot components) | 합성 | ✅ | 중립 | ❌ |
  | (c) Visitor (`BacktestResult.accept(renderer)`) | dispatch | ✅ | 도메인 침범 (D3 충돌) | ❌ |
  | **(d) Protocol+Registry 상속 + 신규 Protocol 별도 정의** | structural typing | ✅ | ✅ — 기존 Protocol 동결 + 신규 layer | **✅ default** |

  **채택 (d) 근거**: ADR 0006 §4.2 `StrategyRenderer` Protocol (episode marker/panel 전용, `src/ports/strategy_renderer.py`) + Registry (`src/adapters/reporting/renderer_registry.py`) 이미 frozen 박제 (ADR 0006 §15.2 "Protocol §4.2 frozen"). 본 phase 의 "전 기간 차트 + grid envelope + 누적 수익" 은 기존 Protocol 시그니처로 표현 불가 — 신규 `VisualizationRenderer` Protocol 이 정당. 기존 Protocol/Registry 그대로 활용 + 본 phase 는 신규 Protocol 별도 정의 + 별도 Registry (또는 기존 Registry 확장 — 0.11.c.2 sub-step 결정).
- **D3** — `BacktestResult` 스키마 — **필드 추가 zero 강제** (Round 1 ITERATE Patch 2, 0.11.x "production rings 변경 zero" + ADR 0006 §18.2 "도메인 변경 zero" + CLAUDE.md §14 보존 invariant):

  전략별 metadata 전달 경로 = 2 가지 concrete 패턴:

  - **(A) `TradeView.annotations` enrichment** — `src/application/reporting/trade_view.py:56` 의 `annotations: dict[str, Any]` 가 이미 확장 가능 frozen. ADR 0006 §16.3 `_enrich_with_slot_number` (`trade_view.py:116-145`) 패턴 precedent — application layer enrichment + 도메인 변경 zero. DGT-specific metadata (예: `grid_level`, `reference_price_snapshot`) 은 application layer 에서 `TradeView.annotations["dgt_*"]` 로 enrich.
  - **(B) `src/research/visualization/` sidecar dataclass** — DGT-specific 전체 metadata (예: `grid_history`, `reference_price_curve`) 는 `_DGTBacktestResult.trades` / `.daily_snapshots` 에서 직접 추출 + 신규 sidecar dataclass (`DGTVisualizationArtifacts`) 로 5th ring 내부 박제. `BacktestResult` 와 분리 — schema 침범 zero.

  **결정**: (A) + (B) 병행 — episode-scope marker 는 (A), full-period DGT-specific overlay 는 (B). 거부된 후보 (i)/(ii)/(iii) BacktestResult 필드 추가: §14 보존 invariant 위반 + OCP 위반 (새 전략마다 필드 추가) + frozen dataclass 변경 cascading risk.
- **D4** — Rendering backend — **(a) matplotlib + mplfinance only 채택** (Round 1 ITERATE Patch 3, Plotly 거부 — `pyproject.toml` §0 trigger 회피):

  | 대안 | Dep 추가 | 기존 정합 | 인터랙티브 | 채택? |
  | --- | --- | --- | --- | --- |
  | **(a) matplotlib + mplfinance only** | 0 (ADR 0006 §6.2 박제) | ✅ Phase 0.10 chart.py 패턴 동일 (`src/adapters/reporting/chart.py`) | ❌ | **✅ default** |
  | (b) Plotly only | plotly 신규 = **CLAUDE.md §0 trigger** | ❌ 패턴 단절 | ✅ | ❌ |
  | (c) 둘 다 (ports/adapters) | plotly 신규 = §0 trigger + Hexagonal over-engineering | 혼재 | ✅ | ❌ |

  **(a) 채택 근거**: ADR 0008 D2 "Markdown only PNG 차단" 선례 + 0.10 시리즈 mplfinance 단독 채택 (ADR 0006 §6.2 simplicity 우선 결정). `pyproject.toml` 변경 zero invariant. Plotly 사후 요청 발생 시 = follow-up phase 위임 (§1.10 시나리오 C mitigation).
- **D5** — Output 형식 + 박제 경로 — **PNG export 채택** + ADR 0008 D2 ↔ ADR 0009 PNG policy **충돌 해소** (Round 1 ITERATE Open Question):

  - **PNG primary** (정적, 회고 commit 박제 의무), HTML embed 가능 (`src/adapters/reporting/chart.py` 패턴 정합 — `_chart_to_png` bytes → HTML img tag).
  - 박제 경로: `docs/retrospectives/figures/phase-0.11.c/*.png` (회고 박제) + `reports/visualization/<config>_<window>/*.png` (ephemeral, `.gitignore` 등록).
  - **고정 dimension/DPI**: width=1600, height=900, dpi=100 — display drift 회피 (R7 mitigation).
  - **ADR 0008 D2 vs ADR 0009 PNG 충돌 해소**: ADR 0008 D2 = "0.11.b sensitivity heatmap = Markdown only, PNG 차단" 의 본질은 *0.11.b 가 visualization 책임 회피* (`pyproject.toml` matplotlib 추가 trigger 회피 + 0.11.c scope 침범 회피). **본 phase 0.11.c = visualization 책임 phase** — PNG export 가 *0.11.b 가 위임한 시각화 산출의 정확한 책임 위치*. ADR 0008 §1.7 + 본 ADR 0009 D9 정합. 충돌 해소 = 본 phase 가 0.11.b sensitivity heatmap PNG 흡수 (D9 정방향 cross-import).
- **D6** — Comparison mode 설계 — **(c) overlay + grid 둘 다 지원** + `BacktestResult` ↔ `_DGTBacktestResult` **공통 축 매핑** (Round 1 ITERATE Open Question):

  - (a) 누적 수익 / drawdown / Sharpe rolling overlay (strategy-agnostic, 공통 축) + (b) grid layout 으로 전략별 detail 옆에 나열 — 둘 다 지원.
  - **공통 축 매핑**: `BacktestResult` (production, B&H/7split) 와 `_DGTBacktestResult` (research, DGT) 스키마 불일치 해소 — 두 결과의 공통 축 = `time` (datetime) + `pnl_cumulative` (Decimal). Overlay 의 핵심 metric. `BacktestResult.trades` (production strategies) 와 `_DGTBacktestResult.trades` (DGT) 모두 entry/exit time + price + side 보유 — markers 매핑 가능. 공통 축 변환은 application/research boundary 의 어댑터 함수 (`_align_results_for_overlay`) 박제 의무 (0.11.c.4 sub-step).
  - Strategy-specific 요소 (grid envelope / split level marker) 은 (b) grid layout 의 각 panel 에 박제 — VisualizationRenderer.render_full_period() 책임.
- **D7** — CLI interface — **`python -m src.research.visualization.cli` 패턴 채택** (Round 1 ITERATE Patch 8, `src/cli/` 변경 거부):

  - 진입점: `python -m src.research.visualization.cli compare --strategies bh,7split,dgt --period 5y --output-dir reports/`
  - 패턴 정합: ADR 0007 §1.6 "python -m src.research.<...> CLI 진입은 ring boundary 위반 아님" + `src/research/dgt/cli.py` 선례.
  - 거부: `stockflow viz` 신규 entry point = `src/cli/` (production 4-ring) 변경 → 0.11.x "production rings 변경 zero" invariant 위반.
- **D8** — 기존 전략 renderer 박제 의무 — **DGT VisualizationRenderer 신규 + comparison orchestration (기존 renderer 호출)** (Round 1 ITERATE Patch 4, B&H/7split 재구현 거부):

  - 본 phase 신규 = **DGT VisualizationRenderer 1 개** (full-period grid envelope + reference price curve + trade markers).
  - 기존 활용: `DefaultRenderer` (B&H, `src/adapters/reporting/renderers/default.py`) + `SevenSplitRenderer` (7split, `src/adapters/reporting/renderers/seven_split.py`) — ADR 0006 §4 박제 frozen + Registry 등록 완료 (`renderer_registry.py:36-39` — 기존 `StrategyRenderer` Registry 등록 상태 확인용 인용, 본 phase 신규 `VisualizationRenderer` Registry 는 0.11.c.2 sub-step 결정). **재구현 거부** (§13.3 "친절한 추가 금지" + §14 보존 목록).
  - Comparison mode (D6) = 기존 renderer (episode-scope marker) 호출 + DGT VisualizationRenderer (full-period overlay) 호출 + (D6) overlay/grid layout 합성. 본 phase = orchestration + DGT 신규 renderer.
- **D9** — DGT renderer ↔ ADR 0008 산출의 연결 — **본 phase 가 sensitivity heatmap PNG 흡수** + intra-research cross-import **정방향 허용** (Round 1 ITERATE Patch 9):

  - DGT VisualizationRenderer 가 ADR 0008 D2 (Markdown only PNG 차단) 의 sensitivity heatmap 위임 흡수 → PNG 산출 책임 단일화 (visualization 책임 단일화).
  - **Intra-research cross-import 방향**:
    - **정방향 허용**: `src/research/visualization/` → `src/research/dgt/` (visualization 이 DGT 결과 읽기 = outer→inner read 의 5th ring 내부 확장). `scripts/check_namespace.sh` 에 정방향 허용 rule 추가 (0.11.c.2 sub-step 산출).
    - **역방향 차단**: `src/research/dgt/` → `src/research/visualization/` — ADR 0008 D9 grep rule 로 차단 (이미 박제, 본 phase 영향 zero).
- **D10** — Test 전략 — **(ii) figure metadata snapshot 채택** (Round 1 변경 없음): rendering 의 visual regression test = (i) PNG byte-identical 취약 (폰트/anti-aliasing drift) → 거부. (ii) figure metadata snapshot (axes count / legend labels / data points) 채택. (iii) headless 비교 없음 거부 (CI 자동화 의무).
- **D11** — Theming / a11y — **CJK font fallback = `chart.py:155-163` 패턴 재사용** (Round 1 ITERATE Patch 12):

  - `src/adapters/reporting/chart.py:155-163` 의 `_cjk_candidates` tuple + `FontProperties` per-Text override 패턴 재사용. CI 환경 `cjk_font is None` 시 한글 boxes 허용 (visual only, test assertion 미영향 — `chart.py:153` precedent 그대로).
  - Color blind palette 의무 / dark mode = follow-up phase 위임 (사용자 spec 외, §13.3).
- **D12** — Reproducibility — (i) random seed 박제 (sampling 시) + (ii) data ordering 고정 + (iii) timestamp metadata 제외. 0.11.a AC15 byte-identical 패턴 상속 (figure metadata snapshot 2회 실행 일치).

### 1.4 Gates G1~G4 (success criterion)

- **G1 (PRIMARY)** — **`VisualizationRenderer` Protocol 신규 + DGT VisualizationRenderer 신규 + comparison orchestration 박제** (D2+D8 정합, Round 1 ITERATE Patch 5 자기모순 정정 반영). 측정: `pytest tests/research/visualization/ -k "protocol or dgt_renderer or comparison"` 통과. (이전 안 "3 전략 renderer + BacktestResult 스키마 박제" 는 D3 침범 차단 (Patch 2) 과 자기모순 → 정정.)
- **G2 (PRIMARY)** — **Comparison mode 동작 검증** — 기존 `SevenSplitRenderer` / `DefaultRenderer` 호출 + DGT VisualizationRenderer 호출 + overlay/grid layout 산출. 측정 (Round 1 ITERATE Patch 11): `pytest tests/research/visualization/ -k comparison` + `bash scripts/check_namespace.sh` 통과. `_align_results_for_overlay` 어댑터 함수 (D6) 가 `BacktestResult` ↔ `_DGTBacktestResult` 공통 축 매핑 정확.
- **G3 (PRIMARY)** — ADR header + 회고 + figure 박제 (D5 의무 충족). 측정: `docs/retrospectives/figures/phase-0.11.c/*.png` 존재 + dimension/DPI 고정 + figure metadata snapshot test 통과.
- **G4 (PRIMARY)** — namespace CI + mypy strict + 재현성 + figure-leak invariant (D10+D12). 측정: `bash scripts/check_namespace.sh` 통과 (intra-research 정방향 허용 + 역방향 차단) + `pytest --runresearch tests/research/visualization/` 통과 + figure metadata snapshot 2회 byte-identical + `plt.get_fignums() == []` invariant (ADR 0006 §17.8 패턴).

### 1.5 Risks (R1~Rn, ralplan #25 박제 대상)

- **R1 (RESOLVED)** — **Namespace 결정**. D1 = (a) `src/research/visualization/` (사용자 결정 2026-05-12). Intra-research cross-import 정방향 허용 (D9) + 역방향 차단 (ADR 0008 D9 grep rule 상속). Import cycle risk 해소.
- **R2 (HIGH)** — **`BacktestResult` 스키마 도메인 / application 침범**. D3 에서 전략별 metadata 를 `src/application/backtest_runner.py:67-155` frozen dataclass 에 넣으면 0.10.bb 박제 invariant + 0.11.x "production rings 변경 zero" 양쪽 위반. **Mitigation (Round 1 ITERATE Patch 2 구체화)**: 2 가지 concrete 패턴 — (A) `TradeView.annotations` enrichment (application layer, ADR 0006 §16.3 `_enrich_with_slot_number` patten `trade_view.py:116-145` 재사용); (B) `src/research/visualization/` sidecar dataclass (`DGTVisualizationArtifacts`, 5th ring 내부). 두 패턴 병행 — episode-scope marker = (A), full-period DGT-specific overlay = (B). `BacktestResult` 필드 추가 zero.
- **R3 (LOW — downgrade)** — **Backend lock-in**. D4 (a) matplotlib only 채택 → Plotly 사후 요청 발생 시 follow-up phase 위임. ADR 0008 D2 패턴 정합 (simplicity 우선). Mitigation: §1.10 시나리오 C (Plotly 사후 요청) graceful path 박제.
- **R4 (LOW)** — **Visual regression test false positive**. D10 (i) PNG byte-identical 거부 + (ii) figure metadata snapshot 채택. Mitigation: G4 의 figure metadata snapshot 2회 byte-identical 검증 + dimension/DPI 고정.
- **R5 (LOW)** — **0.11.b 와의 산출 중복 + intra-research cross-import 방향성**. ADR 0008 §1.8 sub-step 0.11.b.3 의 sensitivity heatmap 은 ADR 0008 D2 = Markdown only PNG 차단 → 본 phase 0.11.c 가 PNG 흡수 (D5 충돌 해소). Mitigation (Round 1 ITERATE Patch 9): D9 정방향 / 역방향 명시 — `src/research/visualization/` → `src/research/dgt/` 정방향 허용 (outer→inner read), `src/research/dgt/` → `src/research/visualization/` 역방향 차단 (ADR 0008 D9 grep rule). `scripts/check_namespace.sh` 정방향 허용 rule 추가 의무.
- **R6 (LOW)** — **한국어 폰트 환경 의존**. matplotlib 의 CJK font 부재 시 한글 깨짐. **Mitigation (Round 1 ITERATE Patch 12)**: `src/adapters/reporting/chart.py:155-163` 의 `_cjk_candidates` tuple + `FontProperties` per-Text override 패턴 그대로 재사용. CI 환경 = `cjk_font is None` 시 한글 boxes 허용 (visual only, test assertion 미영향 — `chart.py:153` precedent).
- **R7 (LOW)** — **Figure size / DPI drift**. 회고 PNG 박제 시 디스플레이별 차이. Mitigation: D5 에서 width=1600, height=900, dpi=100 고정.
- **R8 (NEW, MEDIUM)** — **matplotlib global state 충돌 (figure leak)**. 0.10.aa 패턴 (`plt.get_fignums() == []` invariant) 미계승 시 multi-figure 생성 후 `plt.close()` 누락 → memory leak + test interference. Mitigation: §1.11 observability test (figure-leak invariant) + ADR 0006 §17.8 패턴 재사용 (`src/adapters/reporting/chart.py` 의 `plt.close(fig)` 패턴).
- **R9 (NEW, LOW)** — **`BacktestResult` ↔ `_DGTBacktestResult` 공통 축 매핑 누락 risk**. D6 comparison overlay 의 공통 축 (`time` + `pnl_cumulative`) 매핑 부재 시 overlay 가 무의미. Mitigation (Open Question 해소): `_align_results_for_overlay` 어댑터 함수 박제 의무 (0.11.c.4 sub-step) + G2 verification 에 포함.

### 1.6 Namespace Discipline

ADR 0007 §1.6 5-ring 격리 패턴 **그대로 상속** (사용자 결정 D1 = (a) `src/research/visualization/`). Production rings (domain / application / adapters / use_cases / cli / infrastructure / ports) 변경 zero. `scripts/check_namespace.sh` 7-ring grep 통과 invariant 보존 + **intra-research cross-import grep rule 확장**:

- **정방향 허용** (D9): `src/research/visualization/` → `src/research/dgt/` (outer→inner read 의 5th ring 내부 확장 — visualization 이 DGT 결과 읽기).
- **역방향 차단** (ADR 0008 D9 박제 상속): `src/research/dgt/` → `src/research/visualization/` grep 차단.

`scripts/check_namespace.sh` 신규 rule (0.11.c.2 sub-step 산출): visualization → dgt 정방향만 허용하는 grep + 역방향은 ADR 0008 D9 rule 로 차단됨을 확인.

### 1.7 Lifecycle

본 phase 산출은 strategy-agnostic 인프라이므로 lifecycle = permanent (archive 가능성 낮음). 0.11.b 와 달리 lucky run / failed gate 시 폐기 대상 아님.

**Staleness tripwire 정정** (Round 1 Critic Gap — D3 침범 차단으로 기존 tripwire 무효화):
- (a) ~~`BacktestResult` 스키마 변경 시 renderer 회귀 test 필수~~ — **D3 (Patch 2) `BacktestResult` 변경 zero invariant 채택** → tripwire 무의미.
- (a') **`_DGTBacktestResult` (research-specific) 스키마 변경 시 DGT VisualizationRenderer 회귀 test 필수** — sidecar (D3 pattern B) 가 `_DGTBacktestResult.trades` / `.daily_snapshots` 에서 추출하므로 schema 변경 시 회귀.
- (a'') **`TradeView.annotations` enrichment 패턴 변경 시 cycle pairing renderer 회귀 test 필수** — (D3 pattern A) `_enrich_with_slot_number` (`trade_view.py:116-145`) precedent 변경 시 본 phase 의 marker 매핑 영향.
- (b) 신규 전략 추가 시 VisualizationRenderer 미구현 → comparison mode (D6) 의 grid 슬롯 비어 있음으로 자연스럽게 노출 (Registry default fallback 패턴 ADR 0006 §4.4 상속).

**Research → production promote migration path** (Critic Skeptic note 해소): 본 phase 산출이 Phase 1 promote 후보가 될 경우 ring 이동 = `src/research/visualization/` → `src/adapters/visualization/` (4-ring). Promote 결정은 Phase 1 ADR 0012 진입 시 별도 라운드 — 본 ADR 0009 scope 밖.

### 1.8 Sub-step roadmap (D 결정 박제 완료)

| Sub-step | 본질 | 산출 (예상) |
| --- | --- | --- |
| 0.11.c.1 | D1~D12 결정 + ADR §1 박제 + roadmap | ADR 0009 §1 commit |
| 0.11.c.2 | **`VisualizationRenderer` Protocol 신규 + `DGTVisualizationArtifacts` sidecar (D3 pattern B) + `TradeView.annotations` enrichment 패턴 (D3 pattern A) + intra-research cross-import grep rule (D9 정방향)** + namespace test | `src/research/visualization/` (D1) + `scripts/check_namespace.sh` 확장 |
| 0.11.c.3 | **DGT VisualizationRenderer 신규 구현** (full-period grid envelope + reference price curve + trade markers, D8 정정). B&H/7split = 기존 `DefaultRenderer` / `SevenSplitRenderer` 활용 (재구현 zero) | `src/research/visualization/dgt_renderer.py` |
| 0.11.c.4 | **Comparison orchestration + `_align_results_for_overlay` 어댑터 함수 (D6 공통 축 매핑) + `python -m src.research.visualization.cli` (D7) + figure 박제 (D5 PNG)** | `docs/retrospectives/figures/phase-0.11.c/*.png` + `reports/visualization/*.png` |
| 0.11.c.5 | 회고 + ADR §3 박제 + (조건부) 0.11.a/0.11.b 회고에 figure 추가 | `docs/retrospectives/phase-0.11.c.md` |

### 1.9 References

- **ADR 0006 (Phase 0.10) — StrategyRenderer Protocol (§4) + Registry (§4.4) + StrategyInfo (§15.6) + per-symbol panels (§17) + risk_metrics (§18.C) + figure-leak invariant (§17.8) — 기존 reporting layer 박제 정본. 본 ADR 0009 의 visualization infra 는 ADR 0006 reporting layer 위에 구축** (Round 1 ITERATE Patch 7).
- ADR 0007 (Phase 0.11.a) — 5-ring namespace 패턴 원본 (§1.6) + 박제 primary 정신.
- ADR 0008 (Phase 0.11.b) — DGT 튜닝 산출 (sensitivity heatmap 의 시각화 책임 위임 대상, D2 PNG 차단 ↔ 본 ADR D5 PNG export 정합) + intra-research cross-import grep rule (D9, 본 ADR 정방향 허용 정합).
- `src/ports/strategy_renderer.py` — 기존 `StrategyRenderer` Protocol frozen (ADR 0006 §4.2 박제).
- `src/adapters/reporting/renderer_registry.py` — 기존 Registry (정적 dict + `register` runtime API).
- `src/adapters/reporting/renderers/default.py` — `DefaultRenderer` (B&H, ADR 0006 박제 frozen).
- `src/adapters/reporting/renderers/seven_split.py` — `SevenSplitRenderer` (7split, ADR 0006 박제 frozen).
- `src/application/backtest_runner.py:67-155` — `BacktestResult` frozen dataclass (D3 침범 차단 대상).
- `src/application/reporting/trade_view.py:56,116-145` — `TradeView.annotations` + `_enrich_with_slot_number` enrichment 패턴 (D3 pattern A precedent).
- `src/adapters/reporting/chart.py:87-121,142-163,322-325` — 기존 episode chart (mplfinance + CJK font fallback + layout assertion, D4/D11 패턴 재사용).
- `scripts/check_namespace.sh:17-47` — 7-ring grep (intra-research rule 확장 대상).
- Gamma et al. (1994) — Strategy Pattern + Template Method + Visitor (D2 거부된 (a)/(b)/(c) 후보 인용).
- matplotlib / mplfinance documentation (D4 채택).
- ~~Plotly documentation~~ — D4 (b)/(c) 거부 (§0 trigger), 인용 보존만.

### 1.10 Pre-mortem (deliberate mode 의무, 0.11.a §1.12 / 0.11.b §1.10 패턴 계승 — Round 1 ITERATE Patch 6)

**시나리오 A — matplotlib global state 충돌 (figure leak)**:
- 발생: DGT VisualizationRenderer 가 기존 episode chart (`chart.py`) 와 matplotlib state 충돌 — `plt.close()` 누락 → figure leak. Multi-figure 생성 시 memory 누적 + test interference.
- 원인: 0.10.aa 박제 invariant (`plt.get_fignums() == []`) 미계승.
- Mitigation: §1.11 observability test (figure-leak invariant) + `src/adapters/reporting/chart.py:120` `plt.close(fig)` 패턴 재사용 + ADR 0006 §17.8 CI 게이트.

**시나리오 B — CJK font CI 환경 부재**:
- 발생: CI 환경 (Linux 기반) 에 NanumGothic / 맑은고딕 미설치 → 한글 종목명 boxes (□□□) 표시.
- 원인: matplotlib font fallback chain 의 환경 의존성.
- Mitigation: D11 + R6 mitigation — `chart.py:155-163` `_cjk_candidates` tuple + `FontProperties` per-Text override 패턴 재사용. CI 환경 = visual only, test assertion 미영향 (`chart.py:153` precedent — `cjk_font is None` 시 한글 boxes 허용).

**시나리오 C — Plotly 사후 요청 발생 시 재작업**:
- 발생: 0.11.c 종료 후 사용자가 인터랙티브 시각화 (Plotly) 요청 → 본 phase = matplotlib only → 재작업 필요.
- 원인: D4 (a) matplotlib only 채택 (simplicity + §0 trigger 회피).
- Mitigation: graceful path = follow-up phase 위임 — ports/adapters 분리는 Plotly 도입 시점에 결정. 본 phase 의 `VisualizationRenderer` Protocol 시그니처는 backend-agnostic (PNG bytes 반환) 으로 설계 → Plotly adapter 추가 시 Protocol 변경 zero.

### 1.11 Expanded Test Plan (deliberate mode 의무, 0.11.a §1.13 / 0.11.b §1.11 패턴 계승 — Round 1 ITERATE Patch 6)

| 계층 | 대상 | 검증 항목 |
| --- | --- | --- |
| Unit | `VisualizationRenderer` Protocol + DGT VisualizationRenderer + `DGTVisualizationArtifacts` sidecar | Protocol structural typing / `_DGTBacktestResult.trades` → markers 변환 / grid envelope 계산 정확성 |
| Integration | `_align_results_for_overlay` 어댑터 + 기존 `DefaultRenderer` / `SevenSplitRenderer` 호출 + DGT VisualizationRenderer 호출 | `BacktestResult` ↔ `_DGTBacktestResult` 공통 축 (time + pnl_cumulative) 매핑 정확 / comparison overlay 산출 |
| E2E | comparison mode (D6 c) 산출 (overlay + grid) + figure metadata snapshot (axes count + legend labels + data points count) | 2회 실행 byte-identical (D12 reproducibility, AC15 패턴) |
| Observability | CI 격리 — `pytest --runresearch tests/research/visualization/` flag + figure-leak invariant + intra-research grep | 5th ring boundary 보존 + production tests 와 분리 + `plt.get_fignums() == []` (ADR 0006 §17.8 패턴) + `bash scripts/check_namespace.sh` 정방향/역방향 grep 통과 |

**신규 test 추가량 추정**: ~25 ~ 40 tests (unit 15 + integration 10 + e2e 5 + observability 5). 회귀 invariant: 0.11.a 의 1050+ tests + 0.11.b 의 namespace 14 tests + ADR 0006 의 reporting layer tests 영향 zero (`src/` production rings 변경 zero, 본 phase 산출 = `src/research/visualization/` 5th ring 단일).

---

## 2. Sub-step 0.11.c.2 ~ 0.11.c.4 진행 박제 (2026-05-13)

### 2.1 sub-step 0.11.c.2 — Protocol + sidecar + enrichment + namespace test

- 산출 commit: `38c1e71`.
- 신규 파일 (5th ring `src/research/visualization/`):
  - `_visualization_renderer.py` — `_VisualizationRenderer` Protocol (`@runtime_checkable`) + `_OverlayPayload` frozen dataclass + `_VisualizationArtifacts` marker Protocol (D2 (d) + D6 정합).
  - `_artifacts.py` — `_DGTVisualizationArtifacts` sidecar dataclass + `_GridSnapshot` (D3 pattern B 정합).
  - `_trade_view_enrichment.py` — `_enrich_with_dgt_grid_level` + `_enrich_with_dgt_reference_change` helper (D3 pattern A precedent + ADR 0006 §16.3 strict no-collision 패턴).
  - `__init__.py` — namespace marker (`__all__ = []` underscore-prefix private).
- 신규 tests (`tests/research/visualization/`): 4 파일 26 tests (`test_protocol_structural_typing.py` 9, `test_artifacts.py` 5, `test_trade_view_enrichment.py` 8, `test_namespace_isolation.py` 4).
- 인프라 변경: `scripts/check_namespace.sh` — intra-research cross-import 방향성 grep rule 명시 (정방향 허용 + 역방향 차단 박제). ADR 0008 D9 rule 상속 정합.
- Interface-only — rendering body (matplotlib import + figure 생성) 은 sub-step .3 영역.

### 2.2 sub-step 0.11.c.3 — `_DGTVisualizationRenderer` 구현

- 신규 파일: `src/research/visualization/_dgt_renderer.py` (309 lines).
- 구현:
  - `_DGTVisualizationRenderer` class (`strategy_id = "dgt"`) — structural typing 으로 `_VisualizationRenderer` Protocol 구현.
  - `render_full_period(bars, trades, artifacts)` → PNG bytes (width=1600, height=900, dpi=100 고정, D5 정합).
  - Chart layout: close line + grid envelope (axhline + 시간 가변 시 fill_between band) + reference price line + BUY/SELL scatter markers.
  - `extract_overlay_metric(bars, trades, artifacts)` → `_OverlayPayload` — `cum_cash_delta + holdings·close` 누적 PnL + `pnl - running_peak` drawdown.
  - CJK font fallback (`chart.py:155-163` `_cjk_candidates` tuple + `FontProperties` per-Text override, D11 + R6).
  - `try/finally` + `plt.close(fig)` invariant (ADR 0006 §17.8 패턴 상속, R8 mitigation).
- 신규 tests: `test_dgt_renderer.py` (20 tests) — Protocol compliance / PNG header / figure-leak / overlay 정확성 (manual oracle) / Decimal invariant / drawdown ≤ 0 / 재현성 / parameter 직렬화 (Decimal str 정확).
- 회귀: 1227 tests + 신규 20 = 1247 tests 통과.

### 2.3 sub-step 0.11.c.4 — Comparison orchestration + `_align_results_for_overlay` + CLI + figure 박제

- 신규 파일:
  - `_align.py` — `_align_results_for_overlay` dispatcher + `_align_backtest_result_for_overlay` (production `BacktestResult.snapshots`) + `_align_dgt_result_for_overlay` (research `_DGTBacktestResult.daily_snapshots`). 공통 축 매핑 (time + pnl_cumulative + drawdown), D6 정합.
  - `_dgt_factory.py` — `_build_dgt_visualization_artifacts(result, config)` (D3 pattern B 정합) + `_dgt_trades_to_trade_views(result, config)` (D3 pattern A 정합, signed grid_level index 산출).
  - `_comparison.py` — `_render_overlay_pnl` / `_render_overlay_drawdown` (단일 axes 합성) + `_render_grid` (N rows × 2 cols panel grid). D6 (c) "overlay + grid 둘 다 지원" 정합. R8 figure-leak mitigation.
  - `cli.py` — `python -m src.research.visualization compare ...` subcommand + DGT runner + B&H + 7split (`BacktestRunner` 직접 호출, 5th ring → inner ring read OK) + figure 박제 orchestration. D7 정합 — `src/cli/` 변경 zero.
  - `__main__.py` — `python -m src.research.visualization` 진입 shim (`cli.main` delegation only).
- Figure 박제 (D5 의무 충족): `docs/retrospectives/figures/phase-0.11.c/`
  - `dgt_full_period.png` (88 KB) — DGT 5y full-period chart.
  - `comparison_overlay_pnl.png` (116 KB) — 3-strategy 누적 수익 overlay.
  - `comparison_overlay_drawdown.png` (137 KB) — 3-strategy drawdown overlay.
  - `comparison_grid.png` (160 KB) — 3-strategy grid (pnl + drawdown panels).
  - `overlay_metrics.json` (451 B) — final pnl / drawdown / n_points 박제.
- 신규 tests: 4 파일 38 tests (`test_align.py` 12, `test_dgt_factory.py` 12, `test_comparison.py` 11, `test_cli_smoke.py` 3).
- 회귀: 1247 + 신규 38 = 1285 (1307 total — count drift `+22` 은 0.11.b 잔여 tests 누락 collect 정정).

### 2.4 069500 5y comparison metric 박제 (`overlay_metrics.json` verbatim)

| 전략 | final PnL (KRW) | final Drawdown (KRW) | n_points |
|------|--------------:|------:|------:|
| Buy-and-Hold | +2,108,378.99550 | -3,396,414.00000 | 1231 |
| PriceDrop 7-Split | +1,336,690 | -651,126 | 1231 |
| DGT (n=7, k=5%, m=3) | +617,147.24650 | -1,193,073.10975 | 1231 |

자산군 분산 정신 5회 재확인 (ADR 0005 §9.6.2) — 단일 자산 5y comparison
에서 DGT (informational) < 7split < B&H 의 양수익이지만 drawdown 은
7split best. Phase 1 ADR 0012 D11 분봉 DGT 검토 정량 근거 박제.

### 2.5 회귀 invariant 보존 박제 (sub-step .2 ~ .4)

- Production rings (`src/{domain,application,adapters,use_cases,cli,infrastructure,ports}/**`) 변경 zero.
- `pyproject.toml` 변경 zero (matplotlib / pandas 기존 `reporting` extra 활용, 신규 dep 추가 zero).
- `scripts/check_namespace.sh` exit 0 (7-ring + intra-research dgt/visualization isolation 보존).
- `uv run mypy src/research/visualization/` Success (10 source files, zero errors).
- `uv run ruff check` All checks passed (신규 파일 lint clean).
- 전체 1307 tests 모두 통과 — regression zero.

---

## 3. Phase 0.11.c 시리즈 종료 박제 (sub-step 0.11.c.5, 2026-05-13)

> 회고 정본: `docs/retrospectives/phase-0.11.c.md` (본 commit 동시).
> figure evidence: `docs/retrospectives/figures/phase-0.11.c/*.png` (sub-step .4 산출).

### 3.1 게이트 판정 종합

| Gate | 종류 | 결과 |
|------|------|------|
| G1 (Protocol + DGT renderer + comparison orchestration) | PRIMARY | ✅ PASS (sub-step .2 + .3 + .4) |
| G2 (Comparison mode 동작 검증) | PRIMARY | ✅ PASS (sub-step .4 — `_align_results_for_overlay` + comparison render) |
| G3 (ADR header + 회고 + figure 박제) | PRIMARY | ✅ PASS (본 commit) |
| G4 (namespace CI + mypy strict + 재현성 + figure-leak invariant) | PRIMARY | ✅ PASS |

**4/4 PRIMARY PASS** — 0.11.b 의 INFORMATIONAL FAIL 패턴 (구조적 unattainable)
과 달리 0.11.c 는 infrastructure phase 이므로 게이트 전체 PASS. ADR 0009 §1.7
lifecycle = permanent 결정 발효.

### 3.2 Figure evidence 박제 (D5 의무 충족)

`docs/retrospectives/figures/phase-0.11.c/` 4 PNG + 1 JSON (sub-step .4 산출):

- `dgt_full_period.png` — `_DGTVisualizationRenderer.render_full_period` 산출 (close + grid envelope + reference price + BUY/SELL markers, 069500 5y, n=7/k=5%/m=3).
- `comparison_overlay_pnl.png` / `comparison_overlay_drawdown.png` — `_render_overlay_pnl` / `_render_overlay_drawdown` 산출 (3-strategy overlay 단일 axes).
- `comparison_grid.png` — `_render_grid` 산출 (3-strategy × 2-metric panel grid).
- `overlay_metrics.json` — final pnl / drawdown / n_points 박제 (§2.4 verbatim).

**Dimensions invariant**: 1600x900, dpi=100 (R7 drift mitigation).

### 3.3 Lifecycle = permanent (ADR 0009 §1.7)

본 phase 산출 = strategy-agnostic 인프라 + DGT renderer + comparison
orchestration → **permanent**. 0.11.b 와 달리 lucky run / failed gate 시
폐기 대상 아님.

- `src/research/visualization/` 5th ring 영구 유지.
- Phase 1 promote migration path: `src/research/visualization/` →
  `src/adapters/visualization/` (4-ring) — Phase 1 ADR 0012 진입 시 별도
  결정 라운드 (본 ADR 0009 scope 밖, ADR 0009 §1.7 박제 정합).

### 3.4 핵심 학습 박제 (회고 §5 정본 인용)

1. **Strategy-specific renderer 분리 = OCP 정합** (§5.1) — 사용자 직관 "전략마다 렌더러" + ralplan #25 Round 1 단일 거대 렌더러 거부 정당화. ADR 0006 §4 `StrategyRenderer` (episode-scope, frozen) + 신규 `_VisualizationRenderer` (full-period) 별도 Protocol — D2 (d) 채택 결정 정합.
2. **D6 공통 축 매핑 = production / research 분리의 부드러운 다리** (§5.2) — `BacktestResult` ↔ `_DGTBacktestResult` schema 불일치를 `_OverlayPayload` 공통 축 (time + pnl_cumulative + drawdown) 으로 해소. CLAUDE.md §1.1 "production rings 변경 zero" + 5th ring "outer→inner read OK" 정합.
3. **Figure-leak invariant = matplotlib global state 보호** (§5.3) — ADR 0006 §17.8 `plt.get_fignums() == []` invariant 패턴 5th ring 계승. 4 render 함수 모두 `try/finally` + `plt.close(fig)` 박제.
4. **PNG byte-identical 거부 + figure metadata snapshot 채택** (§5.4) — D10 (i) PNG byte-identical 환경 의존 (폰트/anti-aliasing drift) → 거부. (ii) figure metadata snapshot (axes count / legend / data points) + PNG length 일치 패턴 채택.
5. **자산군 분산 정신 5회 재확인** (§5.5) — 069500 단독 5y comparison: DGT (+617K) < 7split (+1.34M) < B&H (+2.11M). Phase 0.5/0.7.3/0.9.2/0.11.b 누적 학습 정합 (ADR 0005 §9.6.2).

### 3.5 다음 trajectory

- **즉시 다음** (사용자 결정 대기): Phase 0.11.d sub-step .2 진입 (ADR 0010 §1 박제 후).
- **Phase 1 ADR 0012 D11 분봉 DGT 검토** = 본 phase comparison figure 정량 근거 인용 의무 (Phase 1.2 진입 결정 라운드 또는 별도 ADR).
- **ADR 0012 D16 (ii) 진척**: Phase 0.11.c 완료 → 5/16 → 9/16.

### 3.6 회귀 invariant 보존 확인

- `src/{domain,application,adapters,use_cases,cli,infrastructure,ports}/**` 변경 zero (Phase 0.11.x invariant).
- `pyproject.toml` 변경 zero (matplotlib / pandas 기존 `reporting` extra 활용).
- CLAUDE.md §14 변경 (Phase 0.11.c 완료 박제 — 본 commit 동시).
- CLAUDE.md §16 변경 zero (DGT registry 미합류 = §16.1 항목 #4 단일 sell strategy 가정 보존, Phase 0.11.b 와 동일).
- 기존 1227 tests + 신규 80 = 1307 tests 영향 zero (전체 통과, regression zero).
- ADR 0006 `StrategyRenderer` Protocol frozen 보존 — 신규 `_VisualizationRenderer` Protocol 별도 정의 (D2 (d) 정합).

---

**본 ADR §1 + §2 + §3 박제 완료 (Phase 0.11.c 시리즈 종료, 2026-05-13). ralplan #25 Round 1 ITERATE 14 patches + 4 추가 정정 → Round 2 APPROVE (§1, 2026-05-12) → sub-step .2 Protocol + sidecar + enrichment + namespace test (`38c1e71`, 2026-05-12) → sub-step .3 DGT renderer 구현 (2026-05-13) → sub-step .4 comparison + CLI + figure 박제 (2026-05-13) → 회고 + §2 + §3 박제 (본 commit, 2026-05-13). 회귀 invariant: 기존 1227 tests + 신규 80 = 1307 total tests 모두 통과 (regression zero). Phase 0.11.c 산출 = 5th ring `src/research/visualization/` 9 파일 (~1535 lines) + tests 9 파일 (~1500 lines) + 4 PNG + 1 JSON. Lifecycle = permanent. 다음: Phase 0.11.d 진입 (사용자 결정 대기).**