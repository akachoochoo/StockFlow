# Phase 0.11.b — DGT Parameter Tuning (ADR 0008).
# Parameter search infrastructure: WFO splitter + DSR + grid generator.
#
# All symbols underscore-prefix private (ADR 0007 §1.6.3).
# `from src.research.dgt.optimization import *` exposes zero symbols.

__all__: list[str] = []
